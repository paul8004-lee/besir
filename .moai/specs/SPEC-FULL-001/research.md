# SPEC-FULL-001 — research.md

> v0.3.0에서 신규 작성(plan-audit D3). 새로 리서치를 수행한 것이 아니라, plan.md M1(Day 8)에 이미 있던 조사 내용을 Tier L이 요구하는 정식 파일 위치로 옮긴 것이다. 내용은 plan.md M1과 동일하며, 이 파일이 SSOT다 — plan.md M1은 이 파일을 가리키는 요약만 남긴다.

## 조사 주제: 배달앱(요기요/배민) 연동 가능성 — Day 8, 2026-09-12

**배경**: `MealCategory`에 `.delivery`/`.cooking` 케이스가 이미 정의돼 있으나(Day 6, `Models.swift`), 실제로 이 값을 가진 `MealLog`를 만드는 UI·AI 경로는 아직 없다. 이 조사는 "만들어도 되는가"를 판단하기 위한 것이다 — REQ-017/REQ-018(구 REQ-040/041)의 근거 자료.

### 조사 방법

웹 검색으로 다음 두 경로를 확인했다:
1. 요기요·배민의 공식 오픈 API가 개인 개발자에게 열려 있는가.
2. 공식 API가 없다면, URL scheme 딥링크(`yogiyo://`, `baemin://` 등)로 특정 식당·메뉴에 연결할 수 있는가.

### 조사 결과

**공식 오픈 API**: 배달의민족·요기요 둘 다 개인 개발자 대상 공식 오픈 API 프로그램을 찾지 못했다. 존재하는 것은 제휴사 수준의 주문연동 API(`woowabros/OrderRelaySampleCode` 등)뿐이며, 이는 배달대행업체를 대상으로 하는 것이라 개인 앱 개발자가 신청 가능한 구조가 아니다.

**URL scheme 딥링크**: `yogiyo://` 스킴이 존재한다는 커뮤니티 자료(블로그)는 있으나, 경로·파라미터 규격을 공식 문서로 확인할 수 없었다. `baemin://`/`baedalmin://`은 존재 여부조차 신뢰할 수 있는 자료로 확인되지 않았다.

### 결론

두 경로 모두 "그때그때 작동하다 앱 업데이트로 깨질 수 있는 비공식 방식"이라 신뢰도가 낮다. 설령 스킴이 맞다 해도 "앱을 그냥 여는 것"만 가능해, 특정 식당·메뉴로 바로 연결하는 의미 있는 딥링크는 만들 수 없다(경로 규격이 비공개이기 때문).

**결정**: 배달·요리 카테고리는 이번 MVP 스코프에서 계속 제외한다. `MealCategory.delivery`/`.cooking`은 `Models.swift`에 열거형 케이스로 남겨두되(향후 공식 API가 열리거나 딥링크 규격이 공개되면 재검토), 이를 생성하는 UI·AI 경로는 추가하지 않는다 — 죽은 코드가 아니라 "의도적으로 보류된 예약 케이스"로 취급한다(REQ-017).

### 재검토 트리거

REQ-018이 선언하는 조건 — 요기요·배민 공식 오픈 API가 개인 개발자에게 개방되거나, 딥링크 경로·파라미터 규격이 공식 문서로 확인되는 상태가 되면 REQ-017의 생성 금지가 해제된다. 다만 그 경우에도 실제 구현 착수는 이 SPEC이 아니라 별도 후속 마일스톤/SPEC에서 다룬다.

### 참고

- 원 조사 기록: `plan.md` M1 (이 파일 작성 후에도 요약을 남겨 상호 참조)
- 관련 REQ: REQ-017, REQ-018(구 REQ-040, REQ-041)
