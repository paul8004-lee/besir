# t9 — UI통일 Day 닫기 전수 검사 보고서 (2026-09-24)

카드 t9 · 워크트리 `.claude/worktrees/t9` · 브랜치 `WT-day-close` · 베이스 `57f874f`(= 병합 시점 origin/master).
명세는 SPEC이 아니라 **CLAUDE.md "Day를 끝낼 때" 절차**와 리드 디스패치다. 작업 주체: run 레인 오케스트레이터
직접(게이트·판정·문서) + 전문가 셋(code-safety 검사 → swift-impl 1줄 수리 → ux-check 체크리스트 재생성).

## §1 주장 (Claim)

1. **기준선 게이트를 이 레인이 새로 재실측해 전부 통과했다** — 가드 드라이버 212/212(두 실행·exit 0·불변식 9/9 ✓)·
   iOS·macOS 빌드 BUILD SUCCEEDED swift 경고 0/0(수리 전후 각 1회)·proxy 7/7. 드라이버 실행 전후 실제 앱 데이터
   3파일 해시 무변경, 샌드박스 정상 삭제.
2. **Day 종료 절차의 버그·간결성 검사를 렌즈 4종 전수로 돌렸다**(code-safety). 구멍 1건을 그 자리에서 수리했다 —
   저장 버튼이 `saving`을 무시해 추가 모드 이중 탭이 일정 2건을 만드는 것(후속 20②, `AddEventView.swift:119` 1줄,
   본보기 `AddActivityView.swift:509`). 수리 후 양쪽 빌드 재게이트 통과. 신규 결함 2건을 발견해 카드로 이월했다.
3. **O-1(t8, 드라이버 네트워크)을 관측+코드 추적으로 종합 판정했다** — 네트워크 소켓 관측 0건(136샘플·0.1초 간격)이나
   이는 구조적 부재 증거가 아니다. 코드상 샌드박스 시드가 프록시를 끄지 않아 회당 최대 ~5회의 카카오/ODsay/ipwho.is
   호출이 가능하다. **"모델 없이 결정적으로 돈다"와 "모델 API 할당량을 안 쓴다"는 참이지만 "네트워크 0"은 참이 아니다.**
4. **O-1(t7, modifyEvent 출발지 대체)을 코드로 판정해 카드화 추천으로 확정했다**(아래 §6 항목 8b).
5. **n2 잔여 샌드박스 1개를 정리했다**(이름 패턴·UUID 형식 검증 후 삭제, 잔여 0).
6. **CHECKLIST.md를 전면 재생성했다**(ux-check — 인용 의미 전수 감사 포함, 결과는 §1-7·아래 §7).
7. **이월 목록 전 항목에 배치(카드화 추천/체크리스트 편입/기각)와 근거를 §6 표에 남겼다.** 카드 등록(`moai todo add`)은
   리드 소관이라 추천 묶음(§7)만 만든다.

## §2 증거 (Evidence) — 돌린 명령과 관측

| # | 명령 | 관측 |
|---|---|---|
| E1 | `git fetch origin master` · `git rev-parse --short origin/master`(진입 직후) | `57f874f` — 디스패치 베이스와 일치 |
| E2 | 드라이버 신선 컴파일: `cat Shared/EditCard.swift Shared/AIAssistant.swift Tools/GuardDriver.swift > /tmp/gd-t9-baseline.swift && swiftc -o /tmp/gd-t9-baseline …(CLAUDE.md 블록의 9파일) -parse-as-library` | exit 0 · 경고 12건 = 캐럿 연속줄 제외 시 12본(전부 기존 macOS 26 지원 중단: DirectionsService 8·LocationManager 3·PlaceSearch 1 — t8-sync 관측과 동일배분) · 연결 소스 경고 0 |
| E3 | `/tmp/gd-t9-baseline`(1차, 기본 900초) | **exit 0** · 마지막 줄 `212/212 통과` · `[실제 데이터] 대조 통과` · 불변식 9줄 전부 ✓ · ✗ 0 |
| E4 | 1차 실행 중 네트워크 샘플링: 실행 프로세스에 `lsof -nP -i -a -p <pid>`를 0.25초 간격 | 로그 0줄 — 관측된 소켓 없음 |
| E5 | `/tmp/gd-t9-baseline`(2차) + 0.1초 간격 샘플링 + 루프 계수 | **exit 0** · 136샘플 · 네트워크 줄 0 |
| E6 | 실제 지원 디렉터리 해시(실행 전·후): `shasum -a 256` 3파일 | 전후 동일 — `events.json 4f53cda1…`·`config.json e2698db8…`·`activities.json 4f53cda1…`(t8 M1 기록과 같은 값) |
| E7 | 샌드박스 잔존 검사: `ls -d $TMPDIR/besir-gd-*` | 이 레인 실행분 2개 모두 자동 삭제 확인 · 잔여 1개 = t8 run M3①의 `besir-gd-B5B669A3-…` |
| E8 | iOS 빌드(수리 전): `xcodebuild -scheme besir-iOS -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -derivedDataPath build build` | exit 0 · BUILD SUCCEEDED · `grep 'warning:' \| grep -c '\.swift'` = **0**(총 경고 2는 appintentsmetadataprocessor 계열) |
| E9 | macOS 빌드(수리 전): 같은 형식 `-scheme besir-macOS` | exit 0 · BUILD SUCCEEDED · swift 경고 **0** |
| E10 | proxy: `npm --prefix proxy test` | `7/7 통과` |
| E11 | 수리: `Shared/AddEventView.swift:119` `.disabled(!(card?.isReady ?? false))` → `… || saving)`(swift-impl; 편집 전 스펙 일치 Read 확인) | `git diff --stat` = `1 file changed, 1 insertion(+), 1 deletion(-)` — 정확히 1줄 |
| E12 | 수리 후 재빌드(iOS→macOS 순차) | ios-exit=0 swift 경고 **0** · mac-exit=0 swift 경고 **0** |
| E13 | 드라이버 집합 무변경 확인: `git diff --name-only -- <드라이버 컴파일 12파일>` | 출력 없음(무변경) → 수리 후에도 E3/E5의 212/212가 그대로 귀속됨(입력 바이트 동일) |
| E14 | n2 정리: `case "besir-gd-B5B669A3-…" in besir-gd-????????-????-????-????-????????????) rm -rf …` | `removed` · 재검사 `besir-gd-*` 0건 |
| E15 | t7판 O-1 실측: `grep -n 'newOrigin ?? ' Shared/Store.swift` · `func modifyEvent` :332 · `isSamePlace` :1334(create 경로)·주석 :1650("create_schedule 경로 전용") | 대체식 `origin: newOrigin ?? current.origin ?? current.destination`(:345) 관측 · 수정 경로에 isSamePlace 없음 확인 |
| E16 | SPEC-UIKIT-007 좌표: `grep -c 'plan\.md:533'` 4건 → `sed -i '' 's/plan\.md:533/plan.md:539/g'` | 치환 4건 · 잔여 :533 = 0 (후속 17의 병합 트리 좌표 = :539, 이 카드의 plan.md 증분 +5줄 반영) |

전문가 산출물: `.moai/reports/day-close-20260924-code-safety.md`(code-safety 전수 보고, 렌즈 4종·Part B 판정·간결성·갭·잔여).

## §3 귀속 (Baseline-attribution)

- 게이트 수치는 **이 레인이 2026-09-24에 워크트리 t9(57f874f + 표기된 수리)에서 직접 재 실행**한 값이다 — t8 sync의
  측정 인용이 아니다. 드라이버 수리 후 재실행을 하지 않은 것은 E13(컴파일 집합 바이트 무변경)으로 정당화된다.
- 빌드 경고 기준은 `grep 'warning:' | grep -c '\.swift'`(t3 확립 관례 — appintentsmetadataprocessor 노이즈 제외).
- CHECKLIST 옛 좌표의 새 값은 `SPEC-TEST-001/progress.md` §E.4.2(바이트 대조로 검증된 좌표표)를 출처로 썼다.
- code-safety의 판정은 그 보고서 각 항목의 file:line 증거에 귀속된다. 이 레인이 다시 읽어 확인한 것: 후속 20②
  대상 줄·본보기·`saving` 선언/설정 지점, t7판 O-1의 대체식과 가드 부재, 후속 17의 현재 좌표.

## §4 갭 (Gaps) — 검증하지 않은 것 (증거 없음 ≠ 통과)

- **실기기 배포·확인은 이 카드 범위 밖**(디스패치: 검사 뒤 리드가 운영자께). 시뮬레이터 운영자 세션도 이 카드에서
  돌리지 않았다 — F1(시각 칩 바퀴)·L12 등은 ux-check 재생성 목록의 운영자 스크립트 항목으로 넘어간다.
- 후속 20②의 편집 모드 중복 저장(좁은 창의 stale gid 경로)은 정적 추적만으로, 실행 재현은 카드에서 한다.
- 드라이버 exit 2(재지정 거부)·exit 3(실데이터 변화) 가지는 만들 방법이 없어 관측하지 않았다(t8과 같은 한계).
- E5의 네트워크 0건은 표집 관측이다 — 샘플 사이 짧은 접속을 놓칠 수 있다(§5 첫 항목 참조).
- 베타는 만들지 않는다(Day 종료 절차 5항).
- code-safety 보고의 Gaps(이번 우선 표면 밖 파일들 — ContentView·FullSirView·AIChatView 전체·ShareExtension 등)는
  그 보고서 그대로 이월된다.

## §5 잔여 위험 (Residual-risk)

- **드라이버 네트워크**: 관측 0건이 "표집 미스·DNS 환경"과 양립하므로, 프록시 차단(GuardDriver 머리말에서
  proxyBaseURL·appToken 비움) 전까지 "카카오/ODsay 할당량을 쓸 수 있다"는 가능성이 남는다(code-safety 추적: 최대 ~5회/실행).
  CLAUDE.md:58 "(API 할당량 안 씀)" 문구 정리 + 차단 코드는 카드 C1 몫(§7).
- **후속 20② 수리의 행동 검증**: 빌드만 증명했다. 실제 더블탭에서 1건만 생성·버튼 비활성 관찰은 실기기 목록에 추가.
- **서명 프로파일 9/25 만료**(내일!)·유령 팀 DFEME8ZQT9 — 실기기 배포 전 리드/운영자 재결정이 전제다.
- 편집 모드 stale gid 좁은 창(카드 C2 전까지 잔존), PlaceField cornerRadius 등 계약 6 잔여(C5 전까지).
- `RouteMode.car`는 살아있는 코드다(t8 스킬 정리에서 확인) — 향후 정리 카드가 지우지 않도록 보호 대상으로 기록한다.
  같은 이유로 `KoreanHolidays.dates`·`estimatedCost` 후보·`updateMeal`(SPEC-FULL-001 REQ-003)·`MealCategory.delivery/.cooking`
  (Day 8 결론 전) 보호 유지.

## §6 이월 목록 배치 — 전 항목 (카드화 추천 / 체크리스트 편입 / 기각)

원본: 리드 공용 메모리 `project_day_close_carry_list.md` + 카드 t9 본문 + t8 §E.4.8. **모든 항목에 배치가 붙었다.**

| # | 항목 | 배치 | 근거 |
|---|---|---|---|
| 1 | 후속 20② 저장 버튼 `saving` 무시 | **이 카드 수리 완료**(AddEventView:119 1줄) | code-safety FIX-NOW — 추가 모드 이중 탭 → `addEvent` 결정론적 2건(중복 제거 없음); 본보기 AddActivityView:509; Return 키 경로도 같은 버튼으로 막힘 |
| 2 | 후속 20① `recomputeEstimates` await 경합 | 카드 C2 | 표시만 틀림(저장은 updateEvent 재계산); t7 실기기 확인과 같은 빌드에 태움 |
| 3 | 후속 20④ `gatedRows` 인자 2벌 | 카드 C2 | 계약 5 모양; `gatedRows(seeding:)` 과부하 1개면 갚아짐 |
| 4 | 후속 20③ 빈 이름 선택 칩 | **기각**(잔여 위험으로 기록) | `currentPlaceName` 두 생산자 모두 빈 문자열 봉좄(LocationManager:107/:157); 이론적 공급처는 원격 파싱뿐 |
| 5 | 후속 19 셋(값 있는 줄 스피너·권한 거부+현재 위치 칩 탭 출발지 상실·주석 전칭) | 카드 C2 | 전부 현재 좌표 재확인 완료; 편집 시트 표시 묶음 |
| 6 | 신규 — `EventDetailView.loadTransitPath` 무취소 경합(:72/:153-168) | 카드 C3 | code-safety H1 변종; 후속 12(nearby)와 같은 부류 |
| 7 | 신규 — `Store.updateActivity` 조용한 실패(:270-279 `try? gcal.createEvent`) | 카드 C4 | pending/failed 기록 없이 삼켜짐; 다음 동기화 자가 치유 가능하나 편집 자리 무신호 |
| 8a | O-1(t8) 드라이버 네트워크 | **분리**: 문구는 리드/운영자 결정 + 차단 코드는 카드 C1 | 관측 0/136샘플 ≠ 구조적 부재; 코드상 최대 ~5회/실행; "모델 API 할당량 안 씀"은 참. 제안 문구: "(모델을 부르지 않는다. 카카오·ODsay 조회는 가능하다 — 과금은 OpenAI 아님)" 또는 C1 시행 후 현행 유지 |
| 8b | O-1(t7) `modifyEvent` nil 출발지→목적지 대체(Store.swift:345) | 카드 **C11**(신설) | 대체식·도달 사슬(출발지 없는 일정 + D4 시각만 고치는 호출)·가드 부재(:1650) 전부 코드 확인 — 실제 구멍. 수리 방향(폴백 제거·가드 확장)은 카드에서 |
| 9 | O-2 드라이버 백업 7곳 | 카드 C1 | t8 샌드박스 뒤 잉여(9쌍 전수 확인); 재게이트 필수 + CLAUDE.md 사고 절차와 결합이라 Day 닫기에서 못 옮김 |
| 10 | n2 샌드박스 잔여 | **이 카드 정리 완료**(§2 E14) | REQ-001 설계상 시한 실행 잔존은 허용되나 Day 닫기에 청소; 스위퍼 도입 여부는 C1에서 판단 |
| 11 | N1 `save()` `travelSecondsHint` 미전달 | 카드 C2 | 주장 유효(addEvent :601-605 미전달·updateEvent :930-940 인자 없음); 폼이 이미 estimates를 가짐; 카카오 할당량 절감 |
| 12 | CB 계약 6 우회(.secondary·.tertiary·cornerRadius 8) | 카드 C5 | 좌표 확정 AddEventView :627·:630·:635 + 같은 부류 7건(AddActivityView:574·ActivityDetailView :366·:374·:388·:391·:395) |
| 13 | 후속 11 `chooseTimePlain` 기본값 false | 기존 계획 유지(D-2와 함께 판단) | 배선 넷 확인; 잔여 위험은 미래 확장 시 |
| 14 | 후속 12 nearby `onChange` 무취소 | 카드 C3(신규 건과 묶음) | 좌표 갱신 ActivityDetailView:362·:409-415 |
| 15 | 후속 13 저장 게이트 약화 | **이 카드에서 종결**(plan.md 노트) | `executeCreateActivity:1449` 빈 제목 거부 → 도달 불가 확인(세 생성 경로 전부) |
| 16 | 후속 15 장소 확정 칩 감김 | 보류 — AC-009 (14) 시뮬 실측 후 | 기존 판단 유지(좌표 EditCardView:122-124 갱신됨) |
| 17 | 후속 16 Theme 위반(AIChatView:114·AddActivityView:574) | 카드 C5 | 좌표 재확인 |
| 18 | 후속 18 주석 2건 범위 과다 | 카드 C6(문구 묶음) | 좌표 갱신(AIAssistant:759-760) |
| 19 | proxy `/claude/messages` 데드 라우트 | 카드 C10 | 앱 호출 0건(AIAssistant:961은 /ai/chat만) |
| 20 | U-1 편집 카드 통일·삭제 연계·반토막 | 카드 C8(다음 Day 후보, 대형) | 운영자 3점 제안(2026-09-23) |
| 21 | U-2 장소 선택 강조 잔존 | 카드 C7 | 검색 열면 강조 취소 1줄 |
| 22 | U-3 AI 재질문 기억(대화 메모리) | 카드 C9(장기) | 카드가 입력을 전부 담으면 완화; 옛 일정 기억 덮어쓰기 가능성 지적 포함 |
| 23 | U-4 장소 지점 불일치(첫 검색 결과 무조건 채택) | 카드 C7 | '홍대점'→대학로점 관찰(2026-09-23) |
| 24 | AC-009 7·9 관측 FAIL(되묻기에 장소 줄 안 생김) | 카드 C7 | t6 완결 후 첫 사람 관측; U-3·U-4와 인접 |
| 25 | D-3 고지("과거 일정엔 알림이 안 걸린다" 화면 고지) | 카드 C6 | 사용자가 (i)를 골라 수용한 갭의 화면화 |
| 26 | B5 "이동 수단/이동수단" 표기·B7 계산 후 불가 수단 침묵 | 카드 C6 | t3/t4 소관 잔존 — 문구 묶음 |
| 27 | 후속 1~10·14 | **배치 범위 밖**(plan.md 기록, 리드 관리) | 카드 지시 범위 밖; 14는 t6 sync에서 강등·처리 완료 |
| 28 | AC-005·006 값 기록 결손 | 체크리스트 문서화 | 운영자 2026-09-24 수용 확정(재실행 없음) |
| 29 | 실기기 전용 셋·VoiceOver 5종·S4 shortTimeFmt·카드 크롬 질감·t7 실기기 셋 | 실기기 목록(리드→운영자) | CHECKLIST 재생성판 3분할 목록 |
| 30 | F1 시각 칩 재오픈→바퀴 확정 시각 | 시뮬레이터 스크립트 목록 | "화면 재현은 Day 닫기 때" 약속의 이행 |
| 31 | 13번(보임새 대조) | 리드 액션 | 리드가 재설명 후 판정받기로(t7 인계) |
| 32 | iOS 서명 팀·프로파일 9/25 만료 | 리드/운영자 결정(배포 전제) | DFEME8ZQT9 유령; Y54D2W4F4T 오버라이드 선례 |
| 33 | 보호 대상(삭제 금지) 기록 | 문서화(§5) | RouteMode.car·KoreanHolidays.dates·estimatedCost·updateMeal·delivery/.cooking |
| 34 | 워크트리 정리(t1~t8 트리·`t7-sync/dd` 305MB·t8 잠금 pid 57319) | 리드 몫 | 병합 뒤 — 기존 이월 그대로 |
| 35 | 이월 목록 메모리(`project_day_close_carry_list.md`) 갱신 | 리드 몫 | 본 표가 배치 근거 — 전 항목 처리 완료 상태로 닫을 수 있다 |

## §7 카드 추천 묶음 (등록은 리드 — `moai todo add`는 리드 소관)

- **C1 드라이버 위생**: O-1(t8) 프록시·appToken 비움(GuardDriver 머리말) + O-2 백업 7곳 정리 + 스위퍼 도입 여부. 재게이트 필수(212 기준선 변동 가능성 검증 포함). CLAUDE.md:58 문구는 이 카드와 함께 정리.
- **C2 AddEventView 경합·할당량 묶음**: 후속 20①·20④·N1·후속 19 셋 — 같은 파일·같은 빌드에 태울 것.
- **C3 무취소 Task 부류**: 후속 12 + 신규 `loadTransitPath` — `.task` 취소/태깅 패턴 하나로.
- **C4 수정 경로 조용한 실패**: `Store.updateActivity` pending/failed 기록(I1·N2 패턴 확장).
- **C5 Theme·계약 6 정리(ui-design 렌즈)**: CB 3건 + 같은 부류 7건 + 후속 16 두 건 + 후속 3·4(PlaceField radius·돋보기 접근성 라벨).
- **C6 문구·주석 묶음**: 후속 18·19③·B5·B7·D-3 고지 문구.
- **C7 장소 해석 UX**: U-4 + AC-009 7·9 + U-2 — AI 카드 장소 줄 계열.
- **C8 U-1 편집 카드 통일**(대형, 다음 Day 후보).
- **C9 U-3 대화 메모리**(장기).
- **C10 proxy 정리**: `/claude/messages` 제거.
- **C11 수정 경로 출발지 대체 제거**(t7판 O-1): `Store.swift:345` 폴백 정리 + 이 경로 isSamePlace 확장 검토.

## §8 CHECKLIST 재생성 결과 (ux-check)

- **판정 집계**: 114행 = ✅ 103 · ⚠️ 8 · ❌ 2 · — 1(옛판 113행 ✅102·⚠️8·❌2·—1에서 F7 신규 1행만 추가,
  기존 판정 한 칸 불변). 세는 명령: 파이썬 `re.match(r'^\| [A-Z][0-9]+ \|', line)`로 표 행 추출·4번째 필드 Counter
  집계(awk/grep 기호 세기는 로캘 문제로 ⚠️/❌ 줄을 삼키는 것을 확인하고 기각) — 문서 요약 절에도 기록됨.
- **인용 의미 전수 감사(t5 sync 인계 이행)**: 표 113행의 `:숫자` 인용 228조각을 줄 본문 덤프로 한 줄씩 판독,
  프롬프트 규칙 블록(AIAssistant:1032-1076)은 줄 지도 재작성. **정정 11행 13조각** — 이중 프롬프트 규칙 좌표
  7건은 전부 다른 규칙을 가리키고 있었다(A5·C2·D4·E1·G3·G10·G15), 그 외 F1 소비 좌표·G16 두 좌표·G10/G15
  선언부·P1 귀속 정정. GuardDriver 낡은 좌표 11 끝점 전부 §E.4.2 값으로 이동(본문 대조 SAME 확인).
- **드라이버 절 재작성**: "세 겹(현재)" → t8 샌드박스 실체(격리·비움·재설정·절 경계 단언·대조·시한·자체 삭제·
  종료 코드 5종), "실제 데이터에 쓴다" → 샌드박스 사실+수용 잔여(잔존 가능성; 현재 잔여 0건 실측 기록),
  "205/205 밀폐 아님" → "212/212 밀폐 아님"+O-1 코드 추적 실증(프록시 안 끔·최대 ~5회·모델 API 구조적 0회).
  새 기준선 블록은 이 레인의 2026-09-24 측정(§2)에 귀속.
- **확인 목록 3분할**: (a) 기계 검증 완료 — 게이트 수치+4렌즈+228조각 감사. (b) 시뮬레이터 스크립트 14항목
  (입력 문구+기대+초기화 시점 포함, F7 이중 탭·F1 시각 줄 바퀴 신규). (c) 실기기 전용 8항목(알림 수신+D-3(i),
  캘린더 앱·전파, 할당량 안내문, 직접입력 감각, VoiceOver 5종, S4, 크롬 질감, t7 셋). 목록 머리에
  **배포 전제(서명 프로파일 9/25 만료·유령 팀 재결정)** 명시.
- **카드 후보 10건·기각 2건·수용 갭**이 요약 절에 정리(§7과 대응). ux-check이 이월 규칙상 침묵 삭제하면 안
  된다고 판단해 t7판 O-1 가설을 카드 후보 7번에 살린 것은 이 레인 판정(C11)과 같은 방향이다.
- **정올(ux-check)**: 쌍둥이 좌표는 `AddActivityView:509`(plan.md·본 보고서의 :510은 정정 완료),
  잔여 샌드박스 0건 확인.

## §9 리드에게 넘기는 것

1. **병합·done**: 브랜치 `WT-day-close`(미푸시 — 기존 레인 패턴), 종결 커밋 **`841cf9a`**(백필 커밋 제외).
2. **카드 추천 11건**(§7) 등록 여부와 우선순위 — 운영자 승인 대상.
3. **CLAUDE.md:58 문구** — C1과 함께 결정(제안 문구 §6 8a).
4. **실기기 배포 전제**: 서명 프로파일 9/25 만료·팀 재결정 → 운영자 확인 목록은 CHECKLIST 재생성판 3분할.
5. **정리 몫**(§6 34·35): 워크트리·305MB·메모리 갱신.
6. 이 카드 diff: `Shared/AddEventView.swift`(1줄) · `plan.md`(t9 행·후속 13 종결·O-1(t7) 판정·t7 행 좌표 영향 없음) ·
   `CHECKLIST.md`(ux-check) · `.moai/specs/SPEC-UIKIT-007/`(좌표 4건) · `.moai/reports/`(본 보고서·code-safety 보고).
