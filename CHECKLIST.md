# besir 사용자 요구사항 체크리스트

사용자가 besir의 **전체 기능**을 써보면서 마주칠 수 있는 상황을 모아, **현재 코드가 실제로
처리 가능한지** 검증한 표. 2026-09-15 기준(SPEC-ONTIME-001 완료 시점 — 반복 일정 인자 가드
재작성, 저장된 기본값 0 문제, 목록 번호 다리, 쿼터 감지 교정까지 반영).

범례: ✅ 됨 · ⚠️ 부분적/우회 필요 · ❌ 안 됨

현재 AI 도구 11개: `create_schedule`, `create_activity`, `create_recurring_schedule`,
`update_recurring_schedule`, `update_schedule`, `list_schedules`, `recommend_meal`,
`check_travel_time`, `remember_fact`, `forget_fact`, `delete_schedule`

---

## A. 단발성 일정 만들기

| # | 사용자 요구 | 상태 | 근거 |
|---|---|---|---|
| A1 | "내일 3시까지 강남역 가야 해" | ✅ | `create_schedule(arrival_iso)` — 이동시간 역산해 출발 시각 계산. 출발지와 목적지가 사실상 같은 곳(50m)이면 등록 거부(`isSamePlace` — origin 누락으로 "집→집" 0분 일정이 생기던 사고 방지) |
| A2 | "6시에 집에서 바로 출발할래" | ✅ | `create_schedule(departure_iso)` — 출발 기준, 버퍼 0 |
| A3 | "8시부터 10시까지 강남에서 친구 만나" | ✅ | `create_activity` — 주황색 활동 블록 |
| A4 | "가서 놀고 집까지 오는 것도 잡아줘" | ✅ | `create_activity(travel_from_query, return_to_query)` — 활동+이동 2건이 **서로 묶여서** 생성. 툴 선언이 "왕복이면 return_to_query를 빠뜨리지 마라"로 못 박음(모델이 종종 빠뜨려 돌아오는 이동이 안 생겼던 적이 있다) |
| A5 | "다음 주 화요일 오후 2시 병원" | ✅ | 시스템 프롬프트에 현재 시각 주입 + `parseDate` |
| A6 | 카톡 일정표 스크린샷 공유 | ✅ | Share Extension → `SharedInbox.drain()` → `handleShared` → 이미지 파싱. 실기기 라이브 1회 성공 확인(2026-09-10) |
| A7 | 한 번에 여러 일정 등록 | ✅ | `runLoop`이 한 턴에 여러 툴 호출 처리(5회 상한) |
| A8 | "지금 출발하면 몇 시 도착해?" (등록 없이 조회만) | ✅ | `check_travel_time` — 자동차·대중교통·도보 **세 가지를 한 번에** 돌려주고 저장된 선호 수단에 ← 표시. 수단·출발지를 되묻지 않고 바로 호출 |
| A9 | 등록 결과에 적용된 여유·알림이 바로 보임 | ✅ | `executeCreateSchedule` 요약이 "도착 여유 N분 … 출발 N분 전 알림 예약됨"까지 출력(b303f41). 반복 생성 요약도 같은 형태. 값이 틀리게 들어가도 요약에 없으면 사용자가 그 자리에서 보지 못한다 — 0짜리 35건 사고의 남은 절반. (빌드로 검증, 이번 회차 실기기 미관찰) |

## B. 반복 일정

| # | 사용자 요구 | 상태 | 근거 |
|---|---|---|---|
| B1 | "매주 평일 9시~6시 회사" | ✅ | `create_recurring_schedule` — 출근·복귀·활동 블록 자동 생성. **요일 3개 이상인데 주기 인자(매월 n번째 주·격주)가 함께 오면 만들기 전에 되묻는다**(`recurringArgumentIssue`) — "평일"에 `nth_week_of_month:1`이 얹혀 한 달의 1~7일에만 일정이 생기던 사고(c728996) 방지. 확인(`confirm_recurrence`)은 앱이 **그 조합을 실제로 되물었을 때만** 유효(스스로 붙인 확인은 거부) |
| B2 | "월수금만" | ✅ | `weekdays` |
| B3 | "점심에 밖에서 먹어" | ✅ | `lunch_place_query` — 점심 왕복 이동 + 점심 활동 블록. 같은 건물이면(비워두면) 이동 구간 생략, 장소 검색에 실패하면 이동만 생략하고 활동은 만듦 |
| B4 | "6개월간 계속" | ✅ | 최대 26주(`Store.maxRecurrenceWeeks`, Store.swift:35). 상한을 넘는 요청은 이제 **조용히 깎지 않고 되돌려 보낸다**(`AIAssistant.swift:1159` — "최대 26주까지만 만들 수 있어요"). 2년치를 청한 사용자가 26주를 보고 스스로 눈치챌 리 없으므로 앱이 말해준다. 다만 2026-09-15 실기기에서 "최대 26주"라는 답은 툴 선언·시스템 프롬프트에 이미 적혀 있어 모델이 도구 없이 대답한 것 — **새 가드 자체는 아직 실전에서 안 걸림** |
| B5 | "매월 첫째 주 월요일" | ✅ | `nth_week_of_month`(1~4, -1=마지막) — `nthWeekArgument`가 유일한 읽기 지점(가드와 생성이 같은 값을 봄). 1~4·-1 밖의 값은 확인으로 통과시키지 않고 되돌린다(유효값 목록을 보여주면 모델이 아무거나 고르는 사고가 있었다). 0은 "매월 아님"으로 읽는다 — 이 모델은 선택 인자를 비우지 못해 INTEGER에 0을 채운다 |
| B6 | "격주로" | ✅ | `every_n_weeks`(2=격주) — `everyNWeeksArgument`가 유일한 읽기 지점 |
| B7 | "공휴일은 빼고" | ✅ | `skip_holidays` — `KoreanHolidays`. 음력 공휴일은 표가 아니라 `.chinese` 달력으로 계산해 연도 갱신 불필요(2026·2027 실제 값과 대조 검증 완료). 주기 인자와 조합돼도 B1의 조합 점검이 함께 돈다 |

## C. 조회

| # | 사용자 요구 | 상태 | 근거 |
|---|---|---|---|
| C1 | "지금 등록된 일정 뭐 있어?" | ✅ | `list_schedules` — 반복 그룹은 `[반복 n]` 번호 + 회차 수로 요약. 조건에 걸린 게 없으면 "없어요"로 끝내지 않고 전체 건수·가장 가까운 일정을 함께 알려 모델이 스스로 범위를 고치게 함(날짜 범위를 틀리게 잡아 "일정이 없다"고 잘라 말하던 사고 방지) |
| C2 | "오늘/이번 주 일정만 알려줘" | ✅ | `list_schedules(from_date, to_date)` — to는 그 날 23:59:59까지 포함. "이번 주"는 오늘이 들어가는 주로 잡으라고 프롬프트에 못 박음 |
| C3 | "내일 몇 시에 나가야 해?" | ✅ | 출력이 "8:30 출발 → 9:00 도착" 형태로 **출발 시각 포함**. 이동시간 계산 실패 회차는 ⚠️ 표시로 구분 |
| C4 | "이번 주에 몇 건 있어?" | ✅ | C2와 동일 |

## D. 수정

| # | 사용자 요구 | 상태 | 근거 |
|---|---|---|---|
| D1 | "방금 만든 반복 일정 자동차로 바꿔줘" | ✅ | `lastRecurrenceId`를 대화 기록(ai_history.json)과 함께 저장 — 앱을 껐다 켜도 유지. 재생성이 아니라 `update_recurring_schedule` → `updateRecurringSeries`로 그대로 갱신(다시 만들면 중복 등록) |
| D2 | "저번 주에 만들어둔 반복 일정 자동차로 바꿔줘" (옛 대화·앱 재시작 후) | ⚠️ | 목록 번호 다리(615d875): `list_schedules`가 그룹마다 `[반복 n]`을 붙이고 — 등원·복귀·점심이 **모두 같은 번호**를 달고(그룹 전체를 가리킨다), 재호출해도 같은 그룹은 같은 번호, 지워진 그룹의 번호는 재사용하지 않는다. `update_recurring_schedule(series_number)`이 그 그룹 전체를 고르고, 0이면 `lastRecurrenceId`로 폴백, 등록 안 된 번호·음수는 `list_schedules` 재조회로 되돌린다. 번호는 앱 재시작에 살아남지 않는다(무효 번호가 옛 그룹을 가리키는 쪽이 더 위험 — 저장 안 함은 의도적). 앱 쪽 동작은 가드 드라이버(F~I 섹션)로 결정적 검증됐으나 **모델이 [n]을 실제로 series_number에 넣는 것은 실기기에서 아직 관찰 안 됨** — 2026-09-15 대화에서는 series_number=0으로 와서 폴백이 우연히 맞았다 |
| D3 | "도착 여유 20분으로 늘려줘" (반복 그룹) | ✅ | `update_recurring_schedule` → `updateRecurringSeries`. **0이 오면 곧이곧대로 적용하지 않고 되묻는다**(`zeroUpdateIssue`) — "수단만 바꿔줘"에 0이 딸려와 통근 35건의 여유·알림이 통째로 0이 되던 사고 방지. 되묻기에는 현재값 복원 탈출구를 주고, 확인은 `confirm_zero` 왕복으로만 받는다(앱이 되묻은 조합에 한해 유효 — 스스로 붙인 확인은 무시). 번호로 온 호출의 되묻기 답에는 series_number를 그대로 두라고 덧붙인다(빼면 폴백이 엉뚱한 그룹을 고친다). 수정 결과는 어느 그룹을 고쳤는지 제목으로 말한다 |
| D4 | "그 약속 4시로 미뤄줘" | ✅ | `update_schedule(new_arrival_iso / new_departure_iso)`. 여러 건이 걸리면 날짜를 물어 **같은 인자에 date만 추가해** 재호출 |
| D5 | "장소 바꿔줘" / "제목 바꿔줘" | ✅ | `update_schedule(new_place_query, new_title, new_mode)`. 활동은 `new_start_iso`/`new_end_iso`로 옮기며 **묶인 이동도 따라 움직임** |
| D6 | 회차마다 드래그로 여유를 따로 바꾼 시리즈에 "전체 여유 0으로 바꿔줘" | ✅ | 0 판정이 **첫 회차가 아니라 전 회차**를 본다 — `zeroUpdateIssue`가 `recurringSeries` 전체에서 값을 수집해(AIAssistant.swift:1279-1302) 편차가 있으면 "지금은 회차마다 N~M분으로 제각각이라, 확인하면 전부 0분으로 통일돼요"라고 사실대로 알린다. 첫 회차가 0이라고 "이미 0분이라 바뀌는 게 없어요"라고 거짓을 말하면 사용자가 사실이 아닌 전제로 confirm_zero를 누르게 된다. 가드 드라이버 N 섹션 단언(빌드로 검증, 실사용 미관찰) |
| D7 | 모델이 말도 안 되는 값을 보내도 (여유 -5분 등) | ✅ | 반복 수정 경로에서 `Store.updateRecurringSeries`가 여유를 0~180으로 묶는다(Store.swift:682 — 수동 `adjustBuffer`와 같은 상하한, 출발 기준 구간은 0 강제). 가드 드라이버 N 섹션 "음수 buffer_minutes는 0으로 묶인다(-5 저장 아님)". 단발 생성 경로(`create_schedule`→`addEvent`)에는 이 클램프가 없다 — 음수를 만들어 보낸 적은 관찰된 적 없다(모델의 실패 패턴은 0이지 음수가 아니다) |
| D8 | 화면에서 블록 드래그로 시간 이동 | ✅ | 꾹 눌러 드래그. 반복이면 "전체/이 일정만" 선택(`confirmationDialog`) |

## E. 삭제

| # | 사용자 요구 | 상태 | 근거 |
|---|---|---|---|
| E1 | "강남 약속 취소해줘" | ✅ | `delete_schedule(title_query)` — 텍스트로 확인받은 뒤 1회만 호출(도구가 재확인 없이 실행) |
| E2 | "반복 일정 전체 삭제" | ✅ | 제목 일치분만 삭제 — 같은 그룹의 "점심 후 복귀" 같은 **다른 제목 구간까지 지워지는 사고 방지** |
| E3 | "이번 주 화요일 것만 빼줘" | ✅ | `date` + `whole_series:false` — 반복인데 날짜 없이 "이 회차만"이면 날짜를 되묻는다 |
| E4 | "오늘 일정 다 지워줘" | ✅ | `date`만으로도 삭제 가능(제목·날짜 중 최소 하나 필요). 반복 아닌 낱개가 5건 초과면 `confirm_many`로 재확인 |
| E5 | 삭제한 게 동기화로 되살아남 | ✅ | 삭제 묘비(`deleted_gcal_ids.json`) — 원격 부재를 다음 동기화가 확인한 뒤에만 묘비 정리(무한 누적 방지) |

## F. 충돌·시간 계산

| # | 사용자 요구 | 상태 | 근거 |
|---|---|---|---|
| F1 | 기존 일정과 겹치면 알려주기 | ✅ | `Store.conflicts`(Store.swift:457 — 활동·이동 모두 검사, 같은 반복 그룹 제외) — `create_schedule`이 등록 전에 검사하고 겹치면 선택지를 돌려줌 |
| F2 | "늦게 도착해도 되니 끝나고 바로 출발" | ✅ | `on_conflict:"late_arrival"` — 겹치는 일정이 끝난 시각에 출발하도록 전환, 몇 분 늦는지까지 요약에 명시 |
| F3 | "겹쳐도 그냥 등록해" | ✅ | `on_conflict:"ignore"` |
| F4 | 실시간 교통상황 반영 | ✅ | `refreshUpcomingEstimates` — 출발 2시간 이내 일정은 앱 진입 시 재계산. 반복 생성 시 첫 회차만 실제 조회하고 나머지는 그 값을 복사(임박한 회차는 그때 갱신) |
| F5 | 수동(+ 메뉴)으로 만들 때도 충돌 경고 | ✅ | `AddEventView` 겹침 배너(저장은 막지 않음 — 일부러 겹치게 잡는 경우가 있어서) |
| F6 | 활동 블록끼리 겹침 경고 | — | **일부러 넣지 않음**(2026-09-11 사용자 결정). `AddActivityView`에는 활동끼리 겹침 경고가 없고, AI `create_activity`도 이동 구간 겹침만 "막지 않고 알림"한다. 두 활동을 동시에 해야 하는 경우가 정상이라 경고가 방해가 된다 |

## G. 기억·개인화

| # | 사용자 요구 | 상태 | 근거 |
|---|---|---|---|
| G1 | "나 주로 차로 다녀" 기억 | ✅ | `remember_fact`가 값을 Config 선호 필드로 저장(인자가 비면 문장에서 직접 읽음), "기억/주로/앞으로" 등 신호어가 있는 문장은 앱이 모델과 무관하게 바로 반영(`applyStatedPreferences`). 저장값은 다음 요청의 시스템 프롬프트에 숫자로 주입돼 적용된다. **주의: 2026-09-15 사용자 승인으로 이 얕은 메모리 전체를 걷어내는 Phase 1.5가 plan.md에 예정돼 있다 — 다음 회차에 이 행이 바뀐다** |
| G2 | 설정값 임의 추측 방지 | ✅ | 저장된 여유·알림이 모델이 보낸 0으로 덮이던 문제(b303f41 — 여유 0짜리 35건 등록)를 세 겹으로 막는다: ① 프롬프트 "저장된 숫자를 그대로 채워라"(절대 규칙 2) ② 툴 선언 설명의 3요소(저장값 그대로 / 없으면 물어보기 / 0은 비움 대신 금지) ③ 실행부 기본값(`defaultBuffer`/`defaultNotify` — 모델이 인자를 빼도 저장값이 적용됨). 이동수단은 방향이 반대로 `mode_this_time`을 **비워야** 저장값이 쓰인다 |
| G3 | "그거 잊어버려" | ✅ | `forget_fact(fact_query / all)` + 설정 화면의 기억 목록 관리 UI |

## H. 알림

| # | 사용자 요구 | 상태 | 근거 |
|---|---|---|---|
| H1 | "출발 30분 전에 알려줘" | ✅ | `notify_lead_minutes` — 모델이 인자를 빼면 저장값(`preferredNotify` ?? 30)이 실행부에서 적용된다. 등록 요약에 "출발 N분 전 알림 예약됨"으로 **적용된 값**을 보여준다(b303f41 — 요약에 없으면 틀린 값이 조용히 들어가도 그 자리에서 보지 못한다) |
| H2 | "이 일정은 알림 필요 없어" | ✅ | `ScheduledEvent.notifyEnabled` + AI `notify_enabled` + `AddEventView` 토글. 알림 예약 코드가 `scheduleDepartureNotification` 한 곳에 통합돼 한 곳에서 처리 |
| H3 | 반복 일정 뒤쪽 회차 알림 누락 | ✅ | iOS 64건 제한 대응 — `rescheduleNearestNotifications`(가까운 60건만 실제 예약)을 반복 생성·수정 직후와 포그라운드 진입 때 다시 실행 |
| H4 | besir에서만 알림(캘린더 앱 중복 제거) | ✅ | 구글 이벤트 `reminders` 해제 + 기존 항목도 동기화 시 `clearReminders`(PATCH)로 정리 |

## I. 연동

| # | 사용자 요구 | 상태 | 근거 |
|---|---|---|---|
| I1 | 구글 캘린더 자동 등록 | ✅ | `autoAddToCalendar` |
| I2 | "이건 캘린더에 올리지 마" (건별 선택) | ✅ | `syncToCalendar`/`wantsCalendarSync`(이동·활동 양쪽) + AI `add_to_calendar` + 화면 토글 |
| I3 | 앱 안 켜도 동기화 | ⚠️ | `BGTaskScheduler` 백그라운드 동기화 있으나 **iOS가 실행 시점 결정**, 강제 종료 시 미실행 |
| I4 | "근처 맛집 추천해줘" | ✅ | `recommend_meal` 툴(J10) + 활동 상세 "주변 맛집"(J1) + be full sir 화면. 반경은 `radius_meters`가 0(=안 정함)이면 **기본 1000m로 찾는다**(545704c — 0을 그대로 두면 카카오 하한 100m 검색이 돼 결과가 거의 안 나왔다) |

## J. 맛집 추천 (be full sir)

| # | 사용자 요구 | 상태 | 근거 |
|---|---|---|---|
| J1 | 일정 상세에서 목적지 주변 맛집 보기 | ✅ | **활동 상세로 이동됨**(2026-09-12 실기기 피드백 — "식사하는 곳"인 활동 쪽이 "이동하는 중"인 쪽보다 자연스럽다는 의견): `ActivityDetailView` "주변 맛집" 섹션 → `PlaceSearch.nearbyPlaces`(카카오 `category_group_code=FD6`, 거리순). 이동 일정 상세에는 더 이상 없음 |
| J2 | 카페도 보기 | ✅ | 맛집/카페 세그먼트(`MealCategoryFilter` FD6/CE7) |
| J3 | 업종·거리·주소 확인 | ✅ | "한식 · 230m" + 도로명 주소 |
| J4 | 카카오맵에서 자세히 보기 | ✅ | 각 행의 `place_url` 링크 |
| J5 | 주변에 결과가 없을 때 | ✅ | "찾지 못했어요" 빈 상태(크래시 없음) — AI 경로도 "반경을 넓히거나 다른 메뉴로 다시 찾아볼까요?"로 제안 |
| J6 | 추천을 골라 일정으로 등록 | ✅ | `FullSirView` → "일정으로 추가" → `addActivityWithTravel` + `MealLog.activityId` 연결. AI 추천 경로는 `create_activity(log_as_meal:true)`로 같은 연결을 만든다 |
| J7 | 먹은 것 기록(`MealLog`) | ✅ | `FullSirView` "먹었어요" + "최근 먹은 것" 목록(삭제 가능). 일정을 지우면 아직 안 먹은 기록도 같이 사라진다(`removeUpcomingMeals` — 이미 지난 식사는 실제 먹은 기록이라 남긴다) |
| J8 | 예산 기반 추천 | ❌ | be rich sir(Phase 3) 이후 — 지출 데이터가 생겨야 의미가 있다 |
| J9 | 배달·요리 카테고리 | ❌ | Day 8(조사 Day)이 여전히 남음 — 요기요·배민 개인 개발자 API 개방 여부 확인 후 결정. `MealCategory.delivery`·`.cooking`은 정의만 있고 이걸 만드는 UI·AI 경로가 없다 |
| J10 | AI에게 메뉴 추천받기 | ✅ | `recommend_meal` — 메뉴를 좁혀 말하면(일식→초밥) keyword에 그대로 넣어 재검색, 시각만 말하면 그때 있을 장소(`placeAt`: 그 시간대 활동 → 직전 도착 일정 순) 주변에서 찾는다. 반경 0 폴백(기본 1000m). "결과에 없는 가게를 지어내면 안 된다"고 못 박음 — 실기기 확인(할루시네이션 없음) |
| J11 | 홈에서 서비스 선택 | ✅ | `RootView` — be on-time sir / be full sir 카드 |

## K. 화면 조작

| # | 상황 | 상태 | 근거 |
|---|---|---|---|
| K1 | 월간 ⇄ 일간 전환 | ✅ | 헤더 화살표 버튼 |
| K2 | 월간·주간·일간 좌우 스와이프 | ✅ | `SwipePager` |
| K3 | 스와이프 중 날짜가 잘못 선택됨 | ✅ | `@GestureState`로 "한 번이라도 움직였는지" 추적(ContentView.swift:35) |
| K4 | 블록 위에서 세로 스크롤 | ✅ | UIKit `shouldRecognizeSimultaneouslyWith` |
| K5 | 블록 탭 → 상세 | ✅ | 좌표 기반 히트테스트 — `span(for:)`이 렌더링과 공유하는 단일 출처 |
| K6 | 겹친 블록 중 짧은 것 선택 | ✅ | 지속시간 최소 우선 |
| K7 | 꾹 눌러 드래그로 시간 이동 | ✅ | 반복이면 "전체/이 일정만" 확인(EventDetailView.swift:314 `confirmationDialog`) |
| K8 | 활동을 옮기면 묶인 이동도 이동 | ✅ | `linkedActivityId` |
| K9 | 자정을 넘기는 블록 표시 | ✅ | `span(for:)`이 도착일 0시부터 그림 |
| K10 | 빠르게 연속 스와이프 | ⚠️ | `SwipePager`의 `asyncAfter(0.22)` 경합 가능(ContentView.swift:807) — 실사용 재현 안 됨 |

## L. 연동·예외 상황

| # | 상황 | 상태 | 근거 |
|---|---|---|---|
| L1 | 구글 캘린더 등록·삭제 전파 | ✅ | `syncWithGoogle` + 삭제 묘비 |
| L2 | 캘린더에 중복 생성 | ✅ | `reconcileActivities` + `fetchBesirItems` |
| L3 | 캘린더 앱에서 알림 중복 | ✅ | `reminders` 해제 + 기존 항목 PATCH |
| L4 | 삭제한 일정이 되살아남 | ✅ | `deleted_gcal_ids.json` 묘비 |
| L5 | 앱 안 켜도 동기화 | ⚠️ | 백그라운드 실행 시점은 iOS가 결정, 강제 종료 시 미실행 |
| L6 | 다른 앱에서 텍스트·이미지 공유 | ✅ | Share Extension → `handleShared` (실기기 1회 성공) |
| L7 | 이동시간 계산 실패 | ✅ | 빨간 블록으로 표시(숨기지 않음), 탭·삭제 가능, AI 목록에서도 ⚠️ 표시. 등록 요약은 "출발·도착 시각이 비어 있어요"라고 성공인 척하지 않음 |
| L8 | 장소 검색 결과 0건 | ✅ | 빈 목록, 크래시 없음 |
| L9 | 위치 권한 거부 | ⚠️ | 출발지 수동 지정으로 우회 가능. 안내 문구는 이제 나온다("위치 권한을 켜주세요" — create_schedule·recommend_meal)지만 권한 설정 화면으로 연결해 주지는 않는다 |
| L10 | 오프라인 | ⚠️ | 로컬 데이터는 정상, 이동시간·AI는 실패 문구만 |
| L11 | AI 일일 할당량 소진 | ✅ | `classify`(AIAssistant.swift:448-462)가 **HTTP 429 + OpenAI 자체 마커**(`insufficient_quota`/`usage_limit_`) 조합으로만 쿼터 소진을 판정 — 일시적 rate limit(429 전부)과 구분하고, 백엔드가 바뀌어도 옛 마커 문자열이 남지 않게 상태코드와 묶는다(707b9af). 감지되면 재시도하지 않고 즉시 안내(`callAI`가 재시도 루프 중단). 안내문(:307-310)은 한도 소진 사실과 수동 등록 대안(+ 버튼)만 말한다 — **초기화 시각을 의도적으로 적지 않는다**: OpenAI의 한도가 언제 풀리는지 앱이 알 수 없는데 단정하면 옛 결함(Workers AI 시절의 "한국 시간 오전 9시" 안내가 백엔드 교체 후에도 살아 있던 것)이 재현된다. (빌드로 검증 — 소진이 재현되지 않아 이번 회차 실기기 미관찰) |
| L12 | 앱 강제 종료 후 복귀 | ✅ | 모든 상태가 JSON 영속화(events·activities·meals·즐겨찾기·설정·대화 기록·lastRecurrenceId). 목록 번호(seriesNumbers)만 저장하지 않는다 — 재개 후 목록을 다시 보면 재발급된다(D2) |
| L13 | 기기 잠금 중 백그라운드 동기화 | ✅ | 키체인 `AfterFirstUnlock` |

---

## 요약 — 2026-09-15 (SPEC-ONTIME-001 완료 시점)

이전 체크리스트(2026-09-11, Day 7) 이후의 변화를 반영했다. 사이에 있던 일: Workers AI 폴백
경로 삭제(860e121 — 백엔드가 luna 하나로 확정), 그리고 SPEC-ONTIME-001 다섯 커밋.

| 커밋 | 고친 것 | 대응 행 |
|---|---|---|
| c728996 | 반복 일정 인자 가드 재작성 — "8주 평일"이 7건만 생기던 문제(nth_week_of_month 잘못 채움) | B1·B5 |
| b303f41 | 저장된 여유·알림이 모델의 0으로 무시되던 문제 + 등록 요약에 적용값 출력 | A9·G2·H1 |
| 545704c | INTEGER 인자의 0 — 반경은 기본값 폴백, 반복 수정의 0은 되묻기 | D3·I4·J10 |
| 615d875 | 목록 번호 다리 — 옛 대화에서 만든 반복 그룹도 수정 가능하게 | C1·D2·L12 |
| 707b9af | await 인덱스 재사용 3곳, 시리즈 멤버십 단일 출처(`recurringSeries`), 죽은 쿼터 감지 교정 | D6·D7·F4·L11 |

이번에 발견해 고친 체크리스트 자체의 오류: **헤더의 도구 목록이 10개라고 하고 `recommend_meal`을
빠뜨리고 있었다**(실제 11개), **J1의 "목적지 주변" 섹션이 이미 활동 상세로 옮겨갔는데 이동 상세로
적혀 있었다**, **요약 표가 트리에 없는 `activityConflicts`를 인용하고 있었다**(F6 행 본문과도
모순), J8이 be rich sir를 Phase 4라고 적고 있었다(Phase 3).

**⚠️ 현존**: D2(번호 다리 — 앱 쪽은 검증, 모델의 실제 준수 미관찰), I3/L5(백그라운드 실행 시점은
iOS 몫), K10(스와이프 경합, 재현 안 됨), L9(권한 거부 안내는 있으나 설정 연결 없음), L10(오프라인).
**❌ 현존**: J8(예산 — Phase 3 이후), J9(배달·요리 — Day 8 조사 대기). 둘 다 뒤 Day에 계획된
작업이라 새 결함이 아니다.

**다음 예정**(plan.md, 2026-09-15 사용자 승인): Phase 1.5 — 얕은 메모리(`remember_fact`/
`forget_fact`/`ai_memory.json`/선호 필드)를 걷어내고 **앱 주도 선택형 되묻기**(빈 인자를 앱이
감지해 선택 칩으로 묻는 방식)로 교체. G1·G2·G3 행은 이 작업 뒤 다시 쓰인다.

## 검증 방법

이번 회차에 **직접 실행해 관찰한 것**(2026-09-15, HEAD 707b9af + 작업 트리):

- 가드 드라이버: `cat Shared/AIAssistant.swift Tools/GuardDriver.swift > /tmp/gd.swift && swiftc … && /tmp/gd`
  → **59/59 통과**(exit 0). 모델 없이 결정적 — N 섹션이 D6(전 회차 0 판정·편차 안내)과
  D7(음수 버퍼 클램프)의 단언을, F~I 섹션이 D2의 번호 배정·폴백·유지 안내를 담는다.
- 프록시: `cd proxy && npm test` → **7/7 통과**.
- iOS 빌드: `xcodebuild -scheme besir-iOS -destination 'platform=iOS Simulator,name=iPhone 17' …`
  → **BUILD SUCCEEDED**, appintentsmetadataprocessor 공지 제외 **Swift 소스 경고 0건**.
- macOS 빌드: 동일 → **BUILD SUCCEEDED, 경고 0건**.
- 도구 11개는 `toolsJSON()` 선언을 직접 읽어 이름까지 확인. 각 행의 근거 심볼
  (`conflicts`, `rescheduleNearestNotifications`, `reconcileActivities`, `span(for:)`, `SwipePager`,
  `deleted_gcal_ids`, `AfterFirstUnlock` 등)은 전부 현재 트리에서 존재 확인 — 옛 요약이 인용하던
  `activityConflicts`는 트리에 없음(위 요약 참고).
- 충돌 판정 경계 8종·한국 공휴일 2026/2027 대조는 이전 회차의 검증을 그대로 인용(해당 코드 미변경).
- 참고: 실기기 빌드에서 나오는 "All interface orientations must be supported" 경고는
  `project.yml`이 Portrait만 선언(iPad 항목 없음)해서 이전부터 있던 것이며 시뮬레이터 빌드에는
  나타나지 않는다. 이번 회차 새 경고가 아니다.

**빌드·드라이버로만 참(실사용 미관찰)**: A9(등록 요약 문구), B4의 새 상한 가드, D2의 모델 준수,
D6·D7, L11의 새 안내문.

### 실기기에서 직접 확인할 목록 (순서대로)

1. **번호 다리(가장 중요)** — 앱을 다시 켜거나 새 대화를 시작한 뒤 "반복 일정 목록 보여줘" →
   각 줄의 `[반복 n]` 확인 → "2번 그룹 자동차로 바꿔줘". 모델이 series_number에 2를 실제로 넣는지,
   옛 대화에서 만든 그룹이 진짜로 수정되는지.
2. 방금 만든 반복 그룹에 "여유 0으로 바꿔줘" → 되묻기("정말 여유 없이가 맞을 때만…") →
   "응, 0으로 맞아" → `confirm_zero` 왕복으로 **한 번에** 반영되는지.
3. 반복 회차 하나를 드래그로 여유 바꾼 뒤 "전체 여유 0으로 바꿔줘" → "회차마다 N~M분으로
   제각각" 안내가 뜨는지.
4. "6개월간 평일로" → 26주 상한 안내("최대 26주까지만…")가 도구를 거쳐 실제로 오는지
   (지난번엔 선언문만으로 대답했다).
5. 일정 하나 등록 → 요약의 "도착 여유 N분 · 출발 N분 전 알림"이 저장된 기본값과 일치하는지.
6. 활동 상세(이동 상세 아님) 열어 "주변 맛집" 섹션 확인 + 맛집/카페 전환.
7. (기회 되면) AI 할당량 소진 상황 — 초기화 시각이 **없는** 새 안내문인지.
