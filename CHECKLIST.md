# besir 사용자 요구사항 체크리스트

사용자가 besir의 **전체 기능**을 써보면서 마주칠 수 있는 상황을 모아, **현재 코드가 실제로
처리 가능한지** 검증한 표. 2026-09-16 최종 판 — HEAD `7b2e274` 위의 **커밋되지 않은 작업 트리**
(plan.md Phase 1.6 마무리). 같은 날 결함 열여섯(A~P)과 카드 확장 ①, 드라이버 오염 사건 1건이
세 번의 파동으로 들어왔고(사용자 기기 확인 → 코드 검사 → 사용자 재확인), **마지막 재확인에서
사용자가 오늘 수정 전부를 시뮬레이터에서 확인했다("모두 확인완료")**. 이 문서는 처음으로
**관찰을 근거로 닫는다**.

**2026-09-24 Day 닫기 판 (card t9 / UI통일 Day 종결).** 트리 `57f874f`(= origin/master, 카드
t1~t8 병합 상태) + 이 카드의 1줄 수리(`AddEventView.swift:119` 저장 버튼 이중 탭 가드). 이 판이
처음으로 두 일을 함께 한다: (1) **인용 의미 전수 감사** — 표 113행의 근거 인용 228조각을 전부
뽑아 인용 줄 본문과 대조하고 11행 13조각을 고쳤다(아래 t9 기준선 문단). (2) **드라이버 검증 절
재작성** — t8 경화 뒤의 사실(샌드박스 격리)로 "드라이버는 실제 앱 데이터에 쓴다" 문장을 정정하고,
낡은 GuardDriver 좌표 11 끝점을 옮기고, "네트워크를 전혀 안 탄다" 가설을 코드 추적으로
정정했다(모델 API 할당량은 안 씀이 **참**, 회당 카카오·ODsay 최대 ~5회). 행은 F7 하나를 더했고
기존 판정은 한 칸도 바꾸지 않았다(✅ 103·⚠️ 8·❌ 2·— 1).

**줄번호 기준선 (2026-09-18, 카드 t1 / `SPEC-UIKIT-001`).** 본문의 코드 근거를 컴포넌트 추출 이후
트리에서 재실측했다. `AIAssistant.swift` 인용 168건이 −95·−117만큼 밀렸고(옛 줄과 새 줄의 본문이
바이트 단위로 같은지 전수 대조해 확인했다), AI 카드 뷰를 가리키던 11건은 `Shared/EditCardView.swift`로
주소를 옮겼다. 판정(✅/⚠️/❌)은 한 칸도 건드리지 않았다 — t1은 동작을 바꾸지 않는 카드이고 바뀐 것은
근거가 사는 자리뿐이다. 다른 파일(`Store.swift` 등)을 가리키던 30건은 그대로 두었다.

**줄번호 기준선 (2026-09-20, 카드 t2 / `SPEC-UIKIT-002`, sync).** `AddEventView` 전환·디바운서 단일화로
`AIAssistant.swift`(구간별 −2·−4)·`EditCardView.swift`(구간별 +3~+38) 인용을 다시 맞췄다 — 파일명 인용과
붙임 참조까지 101조각. 95조각은 옛 줄(`434610e`)과 새 줄(HEAD)의 본문을 바이트 단위로 대조해 이동을 기계
확인했고, 6조각은 사람이 실측한 좌표다: 재작성된 `searchPlaces`(:762-783)와 350ms 상수·같은 질의 스킵은
새 집 `EditCard.swift`(:234·:257)로 주소를 옮겼고, `sanitizeModelArgs`(:685-699)와 `remember_fact` 제거
사유 주석(:1820)은 **이 카드 이전부터 틀어져 있던 인용**을 바로잡은 것이다. 판정은 한 칸도 건드리지
않았다(✅ 102·⚠️ 8·❌ 2 — 옛판과 동일). 다른 파일(`Store.swift` 등) 인용은 보호했다.

**줄번호 기준선 (2026-09-20, 카드 t4 / `SPEC-UIKIT-004`, sync).** `EventDetailView` 크롬 통일·`BesirTime`
이사로 민 인용을 다시 맞췄다. `EditCard.swift`의 신규 멤버 삽입(+39 — 두 헝크, 옛 33·54 뒤)이 350ms 상수와
같은 질의 스킵을 **:234→:273·:257→:296**으로 밀었고(P4 행과 재확인 블록), `EventDetailView.swift` 재작성이
캘린더 블록·재시도를 **:110-146→:108-144·:148-153→:146-151**로 옮겼다(N2 행과 재확인 블록 — 헝크 지도로
산출한 뒤 현재 트리에서 블록 경계를 줄번호 부여 실측). 치환 3파일(`AIAssistant`·`AddEventView`·
`EditCardView`)은 제자리 줄 치환이라 그물이 0이다(numstat 2/2·5/5·1/1) — 바뀐 줄(:826·:885·:325·:335·
:476·:531·:554·:168)이 이 문서가 인용하는 줄과 한 곳도 겹치지 않음을 대조로 확인했다. 판정은 한 칸도
건드리지 않았고, 세는 명령 값도 불변이다(EventDetailView 1·EditCard 2·AIAssistant 6·EditCardView 6·
AddEventView 0).

**줄번호 기준선 (2026-09-22, 카드 t3 / `SPEC-UIKIT-003`, sync).** `AddActivityView`·`ActivityDetailView`
전환으로 민 인용을 다시 맞췄다. **이 카드가 가장 크게 갈아엎은 두 화면은 드리프트를 만들지 않았다** —
`AddActivityView`(+464/−102)·`ActivityDetailView`(+249/−55)를 가리키는 인용이 이 문서에 0건이고, 활동
행들(A3·A4·G9·O4)은 `AIAssistant.swift`·`Store.swift`를 가리키는데 그 두 파일은 이 카드의 diff 밖이다.
민 것은 공유 컴포넌트 두 파일뿐이다. `EditCard.swift`(+27/−6)는 350ms 상수와 같은 질의 스킵을
**:273→:294·:296→:317**로 균일하게 +21 밀었고(P4 행과 재확인 블록), `EditCardView.swift`(+36/−15)는
구간마다 다르게 밀었다 — `@ScaledMetric`(:31-35)과 "말씀하신 대로"(:50-54)는 **무변경**, `[장소 검색]`
칩 **:122-125→:125-128**·여유 흐림 캡션 **:142-149→:145-152**(+3), `datetimeRow`~`chip` 머리말
**:181-242→:186-263**, `placeSearchEditor` **:297-325→:318-346**·0건 문구 **:313-318→:334-339**·후보 행
**:328-349→:349-370**·`ChipFlow` **:461-509→:482-530**(+21).

확인 버튼의 basis 가드만 **:218-224→:231-245**로, 민 폭이 아니라 **길이가 바뀌었다**. REQ-001의
`if field.anchored` 분기가 그 클로저 안에서 8줄을 늘렸기 때문이다 — 균일 이동으로 셈한 첫 값(+18,
`:236-242`)은 `if field.anchored {` 한 줄을 가리키는 오답이었고, 새 좌표 열 곳을 **전부 본문으로
되짚어 확인하는 과정에서** 잡혔다. 산술로 민 좌표는 산술로 검증되지 않는다. 판정은 한 칸도 건드리지
않았다(✅ 102·⚠️ 8·❌ 2·— 1 — 옛판과 동일). 다른 파일 인용은 보호했다.

파일별 증감은 **sync 종결 트리에서 실측한 값**이다. 초안은 이 자리에 `progress.md` §E.2가 적어 둔
+392/−104·+33/−7·+30/−14를 실측 없이 옮겨 적었는데, 그 수치는 M5 커밋(`b83e260`) 이전 측정이었고
sync의 `--deep` 렌즈가 넷 중 셋을 반증했다. 이 프로젝트가 이미 데인 자리다 — **세거나 실측하지 않은
수치는 옮겨 적지 않는다.** 위 값은 MAJOR-A 수정(줄 신원 키)까지 포함한 최종 트리 기준이다.

**줄번호 기준선 (2026-09-23, 카드 t6 / `SPEC-UIKIT-005`, sync).** 장소 좌표 사전의 이름-키 충돌 수리로
민 인용을 다시 맞췄다. 이 카드가 고친 두 파일 중 **드리프트를 만든 쪽은 하나뿐이다** —
`AddEventView.swift`(+40/−14)를 가리키는 줄번호 인용이 이 문서에 0건이고(파일명 인용 둘은 그대로),
민 것은 `AIAssistant.swift`(+41/−4)뿐이다. 헝크가 셋이라 구간마다 폭이 다르다: 사전 선언 위 주석이
늘어 **+3**(:46~:746), `choose(field:place:)` 앞 삽입까지 더해 **+14**(:747), 새 `confirmedPlaceKey`
선언까지 더해 **+37**(:750~). 154조각 중 149조각은 옛 줄(`c5396b3`)과 새 줄(HEAD)의 본문을 바이트
단위로 대조해 이동을 기계 확인했고, 2조각(`choose(field:place:)` :747-750 → **:761-765**)은 이 카드가
그 함수를 다시 썼으므로 사람이 실측한 좌표다.

**귀속을 산술보다 먼저 확인했다.** 이 문서의 바레 인용(`:123` 꼴)은 앞에 적힌 파일을 잇는데, 그 잇는
규칙을 기계에 맡겼다가 첫 판에서 남의 파일 인용 다섯을 밀어 버렸다 — `span(for:on:)` :543(ContentView),
`Store.swift:257·:639`의 뒤쪽, GuardDriver N·Z·P절 :1248-1621이 그것이다. 되돌리고, 인용마다 바로 앞
심볼을 옛 트리 전 파일에서 찾아 귀속을 정했다. 그 다음 **기계가 `AIAssistant`로 본 156조각을
하나씩 앞뒤 문맥과 함께 읽어** 둘을 걸러냈다(고쳐 쓰는 쪽이 그 156이므로 망가질 수 있는 것도
거기까지다). 나머지 113조각은 파일 귀속 목록으로만 훑었고 문맥까지 읽지는 않았다 — 그쪽의
오귀속은 인용이 낡은 채 남는 것이지 남의 인용이 밀리는 것은 아니다. 산술은 귀속을 검증하지 못한다.

**대조하다 잡힌 t6 이전의 오인용 3건**(t2가 `sanitizeModelArgs`·`remember_fact` 주석에 한 것과 같은
처리다). A1의 "기준 판정"은 등록 요약 문구를 가리키고 있었다 → 기준 결정 블록 **:1303-1311**.
A1의 `isSamePlace` 거부는 `conflictPrompt` 선언을 가리키고 있었다 → 가드와 거부 문구 **:1334-1336**
(같은 가드를 G10 행이 :1334로 맞게 적고 있었다 — 한 문서가 같은 자리를 두 값으로 말하고 있었다).
N2의 `calendarPendingNote`는 이벤트 생성 인자를 가리키고 있었다 → 선언부 **:1420-1424**(이 행이 함께
적은 세 호출점 :1409·:1562-1564·:1697-1699은 맞았다).

**본문을 고친 행은 하나다 — P3.** 나머지 행은 숫자만 바뀌었고 그 사실을 기계로 확인했다(바뀐 줄에서
숫자 외 문자 변화 0건). P3만은 t6가 근거 문장 자체를 낡게 만들었다 — "이름은 인자에"가 이름이
겹치는 경우에 더는 참이 아니다(그 경우가 바로 이 카드의 주제다). 열쇠와 갈래를 적어 고쳤고,
판정은 그대로 ✅다. 판정은 한 칸도 건드리지 않았다(✅ 102·⚠️ 8·❌ 2 — 옛판과 동일).
다른 파일 인용은 보호했다.

**이 sync가 하지 않은 것.** 인용의 **의미**를 전수 감사하지는 않았다. 위 3건은 바이트 대조 중에
걸린 것이고, 같은 부류가 더 있는지는 세지 않았다 — 심볼이 인용 구간 안에 있는지 보는 기계 점검에서
14건이 걸렸는데 그중 11건은 "선언이 아니라 호출점·본문을 가리키는" 정당한 인용임을 확인했고,
나머지가 위 3건이다. 심볼이 붙지 않은 인용은 이 점검이 닿지 않는다. 전수 의미 감사는 ux-check의
체크리스트 재작성 몫으로 넘긴다.

**줄번호 기준선 (2026-09-23, 카드 t5 / `SPEC-UIKIT-006`, sync).** 데드 코드 삭제로 민 인용을 다시 맞췄다.
드리프트를 만든 파일은 셋이다 — `Store.swift`는 헝크 넷이라 구간마다 폭이 다르다(**−14** :190~245 ·
**−13** :246~420 · **−12** :421~1317 · **−28** :1334~, :175까지는 제자리), `GoogleCalendarService.swift`와
`ContentView.swift`는 맨 위 `import` 한 줄이 빠져 균일하게 **−1**이다. `LocationManager.swift`·
`AddActivityView.swift`를 가리키는 줄번호 인용은 0건이고, 지운 세 함수(`addActivity`·`deleteExpired`·
`openLocationSettings`)를 가리키던 인용도 0건이라 근거를 잃은 행은 없다. 세 파일 인용의 끝점 **89개**는
옛 줄(`00ab661`)과 새 줄(HEAD)의 본문을 바이트 단위로 대조해 확인했고(75개 이동·14개 제자리, 쌍점 없이
이어 붙은 `·558` 둘은 따로 대조), 다른 파일 인용 **264개**는 숫자가 그대로임을 기계로 확인했다.

**귀속을 기계에 맡겼다면 열다섯이 틀렸다.** "같은 줄에서 앞에 나온 파일을 잇는다"는 1차 규칙이
`Store` 뒤에 붙은 `AIAssistant` 인용 아홉(B4 `weeksField` :640-646, D7의 세 경로 :1316-1317·:1515-1516·
:1602-1603, F1 `ConflictAsk` :102-106·:1345-1347·:1356, L12 :131-133, L14 :1426-1428)을 `Store`로 보았고,
파일명을 `.swift` 없이 적은 재확인 블록의 `ContentView` 인용 다섯(`span` :543·:558 · `overlapsDay` 호출
:93·:106 · `SwipePager` :800)과 K9의 `span(for:on:)` :543을 다른 파일로 보았다. 인용마다 바로 앞 심볼이
인용 줄 근처에 실제로 있는 파일을 옛 트리 전 파일에서 찾아 귀속을 정한 뒤에 옮겼다.

**대조 중 잡힌 이전 오인용 넷.** 넷 다 이 카드가 만든 것이 아니다. **L3** — t1 sync(`c156c4d`)가
`Store` 동기화 2-4단계(알림 해제 루프) :1455-1459를 `AIAssistant` 인용으로 보고 −95 밀어 415줄 파일의
:1360-1364가 됐다 → `Store.swift:1444-1448`. **I2** — "푸시가 건별 존중"의 :788은 이 문서가 쓰인 트리에서도
`}` 한 줄이었다 → 푸시 쪽 건별 가드 **:884**(주석이 이 행의 제목 "이건 캘린더에 올리지 마"를 그대로 적고
있다). **A3** :519-520 → **:522-523**, **L14** :1426-1428 → **:1463-1465** — 둘 다 t6가 `AIAssistant`를
+3·+37 밀 때 따라가지 못한 인용이고(`c5396b3`의 옛 줄과 바이트 대조로 확인), L14는 같은 줄의
`Store.swift:54` 때문에 `Store`로 귀속돼 있었다.

숫자 외 문자가 바뀐 줄은 L3 하나다(`Store.swift` 파일명을 넣었다). 판정은 한 칸도 건드리지 않았다
(✅ 102·⚠️ 8·❌ 2 — 옛판과 동일). 이 머리말의 카드별 기준선 문단은 그날 좌표의 기록이라 옮기지
않았다(위 t6 문단의 `span(for:on:)` :543 등). 이 sync도 인용의 의미를 전수 감사하지는 않았다 — 의미를
읽은 것은 바뀐 세 파일의 본문 인용과 귀속 점검에 걸린 것까지이고, 심볼이 붙지 않은 나머지 인용은
여전히 ux-check의 체크리스트 재작성 몫이다.

**줄번호·의미 기준선 (2026-09-24, 카드 t9 / Day 닫기, ux-check 재생성).** 이 판은 좌표 이동
점검이 아니라 **의미 전수 감사**를 처음으로 돌렸다 — t5 sync가 넘긴 몫이다(위 t5 문단 끝).
방법: 표 113행에서 `:숫자` 인용을 전부 추출해 파일 귀속(행 안에서 앞에 나온 `.swift` 파일을 잇고
없으면 행의 소재 기본값) 뒤 해당 줄 본문을 덤프하는 스크립트로 **228조각**을 한 줄씩 읽었고,
프롬프트 규칙 블록(`AIAssistant.swift:1032-1076`)은 줄 지도를 직접 그려 다시 맞췄다.
**고친 것 11행 13조각**: 프롬프트 규칙 좌표 일곱(A5 :1039→:1035 · C2 :1048→:1043-1046 ·
D4 :1070→:1066 · E1 :1074-1076→:1070-1071 · G3 :1053→:1049 · G10 :1044-1045→:1040-1042 ·
G15 :1054→:1050 — 전부 인용 줄이 규칙 텍스트가 아니라 다른 규칙을 가리키고 있었다), F1 소비
좌표 :1356→:1393(`conflictConfirmAsk = nil` — :1356은 plannedArrival 줄이었다), G16 두 좌표
(:832-837·:883-885는 531줄 파일 범위 밖 — 재탭 접두 교체는 `tapBasis` EditCardView.swift:
175-180 → `rechooseTimeBasis` AIAssistant.swift:869, 교착 방지는 AIAssistant.swift:917-919),
G10·G15의 선언부 좌표(:1111-1114 → 선언 :1097·:1099 + required 비움 :1107-1110 — :1111은
닫는 괄호였다), P1 `maxPlaceSuggestions` 귀속 정정(숫자 :792는 맞고 사는 집은
AIAssistant.swift다 — 행 앞의 EditCardView를 잇는 문서 규칙이 잘못 귀속시켰다). **늘린 것**:
F7(저장 버튼 이중 탭 — 이 카드의 1줄 수리). **드라이버 검증 절**(아래)의 GuardDriver 낡은
좌표 11 끝점(t8 sync §E.4.5가 세어 둔 목록)도 이 판에서 옮겼다: :1258·:1393→:1511·:1646,
:1247·:1522→:1500·:1775, :1239-1241·:1254-1256→:1492-1494·:1507-1509,
:1167-1168·:1242→:1420-1421·:1495 — 새 좌표 여덟 자리의 본문을 `sed`로 직접 대조(SAME)했다.
t7·t8은 CHECKLIST 인용을 각각 0건·7 끝점만 움직여 기준선 문단을 남기지 않았다(기록은 각 SPEC
progress.md).

**이 문서가 어제 판과 다른 점.** 2026-09-15 판은 가드 드라이버 88/88과 양쪽 빌드 초록을 근거로
거의 모든 행에 ✅를 달았고, 다음 날 관찰이 그중 세 ✅를 반증했다(F1·G10·K9). 오늘 아침 판은
165/165 초록을 근거로 닫았다가 저녁 전사에서 K·M·L이 깨졌다. 이 최종 판은 초록이 아니라
**관찰**을 근거로 닫는다 — 다만 "모두 확인완료"라는 말을 모든 칸에 붙이지 않는다. 무엇을
봤는지 구분해서 적는다:

- 「드:X절」 — 가드 드라이버 X절이 모델 없이 실제 실행 경로를 돌려 단언(기준선 2026-09-16 **205/205** →
  2026-09-24 **212/212** — t8 경화가 머리말 단언 7을 더했다)
- 「빌드」 — iOS·macOS 양쪽 빌드 초록(경고 0). **컴파일만 증명한다**
- 「관찰(날짜)」 — 실기기·시뮬레이터에서 **이름 붙은 증거**와 함께 확인됨(토큰·인용 문구·특정 장소·
  측정 수치·화면 목격 보고 중 하나 이상)
- 「사용자 보고(2026-09-16)」 — 사용자가 확인 목록을 수행하고 통과라고 답했다. **개별 증거는
  남지 않았다.** 미관찰보다는 강하고 관찰보다는 약다 — 해당 행은 다음 Day 목록에 그대로 남는다
- 「문자열」 — 드라이버의 프롬프트 문구 **존재** 확인(U절·Y절 L항). 행동 검증이 아니다

범례: ✅ 됨(결정적 검증 또는 관찰로 앎) · ⚠️ 부분적/우회 필요/앱이 강제 못 함/검증 수단 부족 · ❌ 안 됨.
✅라도 기기 미관찰이면 근거에 그대로 적는다 — "드라이버가 증명"과 "기기에서 봤다"는 다른 말이다.

현재 AI 도구 9개: `create_schedule`, `create_activity`, `create_recurring_schedule`,
`update_recurring_schedule`, `update_schedule`, `list_schedules`, `recommend_meal`,
`check_travel_time`, `delete_schedule` — `remember_fact`·`forget_fact`는 제거됐다(11 → 9).

---

## A. 단발성 일정 만들기

| # | 사용자 요구 | 상태 | 근거 |
|---|---|---|---|
| A1 | "내일 3시까지 강남역 가야 해" | ✅ | `create_schedule(arrival_iso)` — 기준 판정(AIAssistant.swift:1303-1311) 뒤 출발 역산(Store.swift:1012 — 산식의 단일 출처, 3중 복제는 @MX:DEBT로 표시됨). 출발지와 목적지가 사실상 같은 곳(50m)이면 등록 거부(`isSamePlace`, AIAssistant.swift:1334-1336). 〔빌드·코드, 미관찰〕 |
| A2 | "6시에 집에서 바로 출발할래" | ✅ | `create_schedule(departure_iso)` — 출발 기준, 버퍼 개념 없음(:1307-1309, `anchor == .departure`면 buffer 0 강제 :1316·Store.swift:589). 〔드:V절 ①-3〕 |
| A3 | "8시부터 10시까지 강남에서 친구 만나" (머무는 활동만) | ✅ | `create_activity` — 주황색 활동 블록. **이동을 만들지 않으면 카드에 이동수단 줄이 안 생긴다**(askFields의 `guard outbound \|\| back else break`, :522-523). 〔드:T절 D2〕 |
| A4 | "가서 놀고 집까지 오는 것도 잡아줘" | ✅ | `create_activity(travel_from_query, return_to_query)` — 활동+이동이 **묶여서** 생성(선언 대칭화 :1122-1123). 카드의 가는 편 출발지 줄(outboundOriginField :603-612, "가는 편 없음" 탈출 칩 — 토큰은 실행부 :1476-1477에서 그 구간을 안 만드는 값으로 품). 〔드:R절 + **관찰(2026-09-16)** — `가는 편 자동차 · 오는 편 대중교통`이 한 호출에 실림(`travel_mode_this_time=car` + `return_mode_this_time=transit`, 서로 다른 값 = 두 줄을 따로 골랐다는 결정적 증거). "가는 편 없음"→편도 전환은 **사용자 보고**(개별 증거 없음) → 확인 목록 (b) 8〕 |
| A5 | "다음 주 화요일 오후 2시 병원" | ✅ | 시스템 프롬프트에 현재 시각 주입(:1035) + `parseDate`(:2467-2479). 〔빌드·코드〕 |
| A6 | 카톡 일정표 스크린샷 공유 | ✅ | Share Extension → `SharedInbox.drain()` → `handleShared`(:273-281) → 이미지 파싱〔관찰(2026-09-10)〕. 여러 건을 한 턴에 등록할 때도 **카드는 한 장**(pendingAsk :651-679). 제목·목적지를 비워두면 그 줄도 같은 카드에 온다(①) |
| A7 | 한 번에 여러 일정 등록 | ✅ | `runLoop`이 한 턴에 여러 툴 호출 처리(5회 상한, :380). 〔빌드·코드〕 |
| A8 | "지금 출발하면 몇 시에 도착해?" (등록 없이 조회만) | ✅ | `check_travel_time`(:2155-2177) — 세 가지 수단을 한 번에. 출발지 폴백이 살아 있는 **유일한 곳**(호출 :2158, 정의 :2346의 `orDefault:` :2367-2371). 등록 경로는 카드로 묻는다(G10). 〔빌드·코드〕 |
| A9 | 등록 결과에 적용된 여유·알림이 바로 보임 | ✅ | 등록 요약이 출발지·여유·알림까지 출력(:1399-1408). 결함 J 수정: 요약이 `제목+목적지 .last` 되찾기로 **같은 이름의 더 늦은 회차**를 집어 거짓 시각을 말하던 것을, `addEvent`가 반환하는 방금 만든 이벤트로 고침(:1380-1390; Store.swift:567-570 — 되찾기 패턴 `Shared/` 전역 0건, 팀 리드 grep). 〔드:J절 + **사용자 보고(2026-09-16)** — 확인 목록 7 수행·통과 답변(개별 증거 없음) → 확인 목록 (b) 5〕 |

## B. 반복 일정

| # | 사용자 요구 | 상태 | 근거 |
|---|---|---|---|
| B1 | "매주 평일 9시~6시 회사" | ✅ | `create_recurring_schedule` — 출근·복귀·활동 블록 자동 생성. 요일 3개 이상 + 주기 인자면 되묻기(`recurringArgumentIssue` :1761). 〔드:T절 D1 + **관찰(2026-09-16)** — 반복 생성에서 카드가 실제로 떴다: 이어진 호출의 `origin_query=__current_location__`는 **카드만 만들 수 있는 값**이다(결함 D 수정의 결정적 증거)〕 |
| B2 | "월수금만" | ✅ | `weekdays`(:1587). 〔빌드·코드〕 |
| B3 | "점심에 밖에서 먹어" | ✅ | `lunch_place_query`(:1652-1682) — 같은 건물이면 이동 생략, 검색 실패 시 이동만 생략. 〔빌드·코드〕 |
| B4 | "6개월간 계속" | ✅ | 최대 26주(`Store.maxRecurrenceWeeks`, Store.swift:47). 기간은 카드의 반복 기간 줄(weeksField :640-646 — 칩 4/8/12/26주, 직접입력 1~26 거부). 생성부 이중 안전장치(Store.swift:244·:640). 2026-09-16 사고: 선언에 없는 `weeks:8`이 카드를 우회해 118건 등록(관찰) → 방어 `sanitizeModelArgs`(G2). 〔드:T절 D1 + 관찰(카드 경유 등록) + **사용자 보고** — 칩 선택→건수 확인(개별 증거 없음) → 확인 목록 (b) 9〕 |
| B5 | "매월 첫째 주 월요일" | ✅ | `nth_week_of_month` — `nthWeekArgument`(:1720-1723)가 유일한 읽기 지점. 0은 "매월 아님"(이 모델은 INTEGER에 0을 채움). 〔빌드·코드〕 |
| B6 | "격주로" | ✅ | `every_n_weeks` — `everyNWeeksArgument`(:1740-1742). 〔빌드·코드〕 |
| B7 | "공휴일은 빼고" | ✅ | `skip_holidays`(:1600) — `KoreanHolidays`, `.chinese` 달력 계산(Models.swift:316-403)〔2026·2027 대조 검증 완료(과거 스크립트)〕. 〔빌드·코드〕 |

## C. 조회

| # | 사용자 요구 | 상태 | 근거 |
|---|---|---|---|
| C1 | "지금 등록된 일정 뭐 있어?" | ✅ | `list_schedules`(:1956-2081) — `[반복 n]` 번호(seriesTag :2028-2034) + 회차 수 + 안내문(:2079). 빈 결과엔 전체 건수·가장 가까운 일정을 함께 알림(:1984-1997). 〔드:T절 D3이 번호 배정 경로 사용〕 |
| C2 | "오늘/이번 주 일정만 알려줘" | ✅ | `list_schedules(from_date, to_date)` — to는 23:59:59까지(:1964-1966). "이번 주"는 오늘이 들어가는 주(프롬프트 규칙 4, :1043-1046). 〔빌드·코드〕 |
| C3 | "내일 몇 시에 나가야 해?" | ✅ | 출력에 **출발 시각 포함**(leg() :2020-2023). 계산 실패 회차는 ⚠️ 표시(:2043). 〔빌드·코드〕 |
| C4 | "이번 주에 몇 건 있어?" | ✅ | C2와 동일. 〔빌드·코드〕 |

## D. 수정

| # | 사용자 요구 | 상태 | 근거 |
|---|---|---|---|
| D1 | "방금 만든 반복 일정 자동차로 바꿔줘" | ✅ | `lastRecurrenceId`를 대화 기록과 함께 저장(:137-140). `update_recurring_schedule` → `updateRecurringSeries`(Store.swift:728). 〔빌드·코드〕 |
| D2 | "저번 주에 만들어둔 반복 일정 자동차로 바꿔줘" (앱 재시작 후) | ⚠️ | 목록 번호 다리 — 앱 쪽은 결정적 검증됨(같은 그룹 같은 번호 :2028-2034·번호 재사용 없음·폴백 :1815·무효 거부 :1807-1811). **모델이 [n]을 series_number에 넣는 것은 여전히 한 번도 관찰된 적 없다**(확인 목록 13을 사용자가 수행했다고 답했으나 대화 복사 증거는 남지 않았다). → 확인 목록 (b) 1 〔드:과거 회차 + T절 D3〕 |
| D3 | "도착 여유 20분으로 늘려줘" (반복 그룹) | ✅ | 0이 오면 되묻는다(`zeroUpdateIssue` :1867-1918) — 35건 0화 사고 방지. 확인은 `confirm_zero` 왕복으로만. 〔드:B·N절〕 |
| D4 | "그 약속 4시로 미뤄줘" | ✅ | `update_schedule(new_arrival_iso / new_departure_iso)`(:2181-2257). 여러 건이면 date만 추가해 재호출(프롬프트 :1066). 〔빌드·코드〕 |
| D5 | "장소 바꿔줘" / "제목 바꿔줘" | ✅ | `update_schedule(new_place_query, new_title, new_mode)`(:2208-2243). 활동은 묶인 이동도 따라 이동(:2220-2225). 〔빌드·코드〕 |
| D6 | 회차마다 드래그로 여유를 따로 바꾼 시리즈에 "전체 여유 0으로" | ✅ | 0 판정이 **전 회차**를 본다(`zeroUpdateIssue`가 시리즈 전체 값 수집 :1875-1881). 〔드:N절〕 |
| D7 | 모델이 말도 안 되는 값을 보내도 (여유 -5분 등) | ✅ | `clampBuffer`/`clampNotifyLead`(Store.swift:62·66)로 네 경로 통일: 단발(:1316-1317)·활동(:1515-1516)·반복(:1602-1603)·수정(Store.swift:745-746). 〔드:O절〕 |
| D8 | 화면에서 블록 드래그로 시간 이동 | ✅ | 꾹 눌러 드래그. 반복이면 "전체/이 일정만"(ContentView.swift:780-788). 〔빌드·코드〕 |

## E. 삭제

| # | 사용자 요구 | 상태 | 근거 |
|---|---|---|---|
| E1 | "강남 약속 취소해줘" | ✅ | `delete_schedule(title_query)` — 텍스트 확인 뒤 1회만 호출(프롬프트 :1070-1071). 〔빌드·코드〕 |
| E2 | "반복 일정 전체 삭제" | ✅ | 제목 일치분만 삭제(:2305-2310). 〔빌드·코드〕 |
| E3 | "이번 주 화요일 것만 빼줘" | ✅ | `date` + `whole_series:false` — 날짜 없으면 되묻는다(:2298-2300). 〔빌드·코드〕 |
| E4 | "오늘 일정 다 지워줘" | ✅ | `date`만으로 삭제 가능. 낱개 5건 초과면 `confirm_many` 재확인(:2294-2296). 〔빌드·코드〕 |
| E5 | 삭제한 게 동기화로 되살아남 | ✅ | 삭제 묘비(`deleted_gcal_ids.json`, Store.swift:83·:97-102). 〔빌드·코드〕 |

## F. 충돌·시간 계산

| # | 사용자 요구 | 상태 | 근거 |
|---|---|---|---|
| F1 | 기존 일정과 겹치면 알려주기 | ✅ | `Store.conflicts`(Store.swift:514-533) — `create_schedule`이 등록 전에 검사. 결함 C 수정: 첫 호출의 `on_conflict:"ignore"`로 검사가 건너뛰어지던 것을 `ConflictAsk` 게이트(:102-106·:1345-1347 — 앱이 물은 조합만 답으로, 등록 성공 시 소비 :1393)로 막음. 〔드:S절(C1~C3 + 전제·소비) + **사용자 보고(2026-09-16)** — 확인 목록 5 수행·통과 답변(개별 증거 없음) → 확인 목록 (b) 4〕 |
| F2 | "늦게 도착해도 되니 끝나고 바로 출발" | ✅ | `on_conflict:"late_arrival"` — 겹침 끝 시각 출발 전환(:1360-1364), 늦는 분까지 요약에 명시(:1406-1408). 〔드:S절 C3 + 사용자 보고(같은 목록 5)〕 |
| F3 | "겹쳐도 그냥 등록해" | ✅ | `on_conflict:"ignore"` — 물은 뒤에만 통과(:1353). 〔드:S절 C2 + 사용자 보고(같은 목록 5)〕 |
| F4 | 실시간 교통상황 반영 | ✅ | `refreshUpcomingEstimates`(Store.swift:1245-1271) — 출발 2시간 이내 재계산. 반복은 첫 회차만 조회 후 복사(Store.swift:648-678). 〔빌드·코드〕 |
| F5 | 수동(+ 메뉴)으로 만들 때도 충돌 경고 | ✅ | `AddEventView` 겹침 배너 — `ConflictBanner`(사용 :71-72, `currentConflicts` :646, 선언 :719). 저장은 안 막는다(경고가 선택을 대신하지 않는다). UI통일 Day(t2a)가 이 화면을 EditCard 문법으로 전환한 뒤에도 유지(2026-09-24 좌표 재확인). 〔빌드·코드〕 |
| F6 | 활동 블록끼리 겹침 경고 | — | **일부러 넣지 않음**(2026-09-11 사용자 결정). 동시 진행이 정상인 경우가 있어 경고가 방해가 된다. 활동 생성 시 만들어지는 **이동 다리**의 겹침은 알려준다(executeCreateActivity :1545-1558 — 만들기는 막지 않고 문구로). |
| F7 | 저장 버튼을 빠르게 두 번 눌러도 일정이 두 배로 생기지 않음 | ✅ | **이번 Day 닫기에서 수리(card t9)** — 추가 모드에서 두 번 탭하면 `addEvent`가 결정론적으로 2건 실행되던 구멍(2026-09-24 code-safety 전수 검사 확정: 진입 가드가 `saving` 재진입 검사가 없었다)을 `AddEventView.swift:138` `.disabled(!(card?.isReady ?? false) \|\| saving)` 1줄로 막음. 쌍둥이 `AddActivityView.swift:509`는 원래 온전했다. 〔빌드·코드(수리 전후 무경고) — 이중 탭 화면 실측은 확인 목록 (b) 11〕 |

## G. 요청마다의 값 · 앱 주도 되묻기 카드

되묻는 주체가 모델이 아니라 **앱**이다. 이 모델은 선언된 선택 인자를 비워두지 못하고 전부
채운다(b303f41 — 35건 사고). 그래서 되물을 인자(이동수단·도착 여유·알림·반복 기간)는 툴 선언에서 뺐다.

**전제 정정(2026-09-16).** "선언에서 빼면 모델이 보낼 수 없다"는 **거짓으로 밝혀졌다** — 선언에
없는 네 인자가 실려 118건이 등록됐다. 선언 축소는 힌트이고, 방어는 `sanitizeModelArgs`
(AIAssistant.swift:688-702, 호출 :423, 화이트리스트는 선언에서 :706-716). 오후 코드 검사는
**빈 문자열** 변형(H)을 더했다 — 부재 판정은 nil이 아니라 `filled()`로 한다(:468-471).

| # | 사용자 요구 | 상태 | 근거 |
|---|---|---|---|
| G1 | "자동차로 가자"·"여유 20분"처럼 말한 값 | ✅ | 문장 파서가 결정적으로 읽음(`statedArguments` :338-344 → `fillStated` :720-737). 결함 B 수정: 발화마다 **병합**(:291), **툴 실행 시 소거**(:1278), 실패 시 유지(:308-311), 새 대화 초기화(:217). 명시값은 "말씀하신 대로"로 보임(:360-368, 카드 EditCardView.swift:50-54). 〔드:Q절 + **사용자 보고(2026-09-16)** — 확인 목록 3 수행·통과 답변(개별 증거 없음) → 확인 목록 (b) 3〕 |
| G2 | 값을 안 말하면 아무 값이나 넣지 않고 물어봄 | ✅ | 부재는 카드가 됨(`askFields` :467-544). 결함 D·H 방어: 선언 밖 인자는 `sanitizeModelArgs`가 버리고, "비움"이 빈 문자열이어도 `filled()`가 비었다로 읽음 — 부재 판정을 속일 수 없음. 카드 미통과 호출은 `missingAskedArguments`(:1262-1269)가 막음. 〔드:T절 + W1 + **관찰(2026-09-16)** — 반복 생성에서 카드가 떴고 사용자가 출발지 줄에서 "현재 위치"를 골랐다: 이어진 호출의 `origin_query=__current_location__` 토큰은 **카드만 만드는 값**이다〕 |
| G3 | "앞으로 자동차로 해줘" (기억 요청) | ✅ | **기억은 없다 — 매번 물음이 원칙**(REQ-041). 결함 F 수정(프롬프트 규칙 7, :1049 — 미래 약속 금지 + 정직 안내). 〔드:Q절(무기억)·U절(문자열) + **관찰(2026-09-16)** — 모델이 실제로 답했다: *"자동차 설정은 앞으로가 아니라 **이번 일정 요청에만** 적용할 수 있어요. 이후 일정도 자동차로 원하시면 매번 말씀해 주세요."*〕 |
| G4 | 빠진 값이 여러 개면 한 번에 묻기 | ✅ | **카드 한 장**에 전부(`pendingAsk` :648-676). 〔드:P절〕 |
| G5 | 확인 버튼을 누르면 딱 한 번 등록 | ✅ | `confirmAsk` :878 → `resolvePendingAsk` :898 → `runToolCalls` :445 — 정확히 1회. 〔드:P절 + V절 ①-2〕 |
| G6 | 칩에 없는 값 직접 입력 | ✅ | 모든 줄에 직접입력(submitCustom :841-855, `accepts`가 빈 값·범위 밖 거부). **장소 줄의 직접입력은 `[장소 검색]`으로 바뀌었다(결함 P — P절 참조)**. 이동수단 줄에는 없음(modeField :616-620 — 세 칩이 전체 집합). 〔드:V절 ①-1〕 |
| G7 | "여유 없이" — 진짜 0분 | ✅ | 카드의 **진짜 `0분` 칩**(bufferField :624-629). 알림 줄의 0은 출발 시각 알림(:633-638). 〔드:과거 P절 구성〕 |
| G8 | 왕복은 가는 편·오는 편을 따로 | ✅ | 왕복에서 `가는 편`·`오는 편` 수단 줄이 각각(:530-531). 〔드:R절 + **관찰(2026-09-16)** — 서로 다른 값(car/transit)이 한 호출에 실린 것으로 두 줄의 독립 선택이 확인됨(A4와 같은 관찰)〕 |
| G9 | 머무는 활동에 교통 질문 안 함 | ✅ | 이동 없는 활동은 묻지 않는다(:522-523). 복귀는 출발 기준이라 여유 줄도 가는 편이 있을 때만(:535-539). 〔드:T절 D2〕 |
| G10 | 출발지를 안 말했을 때 | ⚠️ | **앱이 강제할 수 없다(2026-09-16 사용자 결정 — 프롬프트 전용).** 카드의 출발지 줄(:493-494)은 origin_query가 비었을 때만 뜨는데, origin_query는 선언된 인자라 모델이 지어내 보내면 우회된다. 프롬프트 규칙 3(:1040-1042)〔문자열 — U절〕. destination_query·title도 같은 노출(①로 required는 비웠지만 선언 유지 — title :1097·destination_query :1099, required 비움 :1107-1110). 최소 방어: 모델이 채워온 제목·목적지는 카드에 출처 표시(:664-676, 드:V절), 실행부 가드(isSamePlace :1334). **지어낸 출발지가 검색에 성공하면 조용히 등록되는 경로는 여전히 열려 있다.** 확인 목록 8a 사용자 보고. → 개별 증거 필요(수용된 갭, 확인 목록 (b) 2) |
| G11 | 반복 일정처럼 줄이 다섯 이상이면 | ✅ | **행을 숨기지 않는다** — 칩 줄바꿈(`ChipFlow`, EditCardView.swift:482-530), 카드 전체를 채팅 ScrollView가 담음. 최악 일곱 줄(드:V절 ①-1, Z절 O-6이 활동 최악 일곱 줄 재확인), 접기·캡 없음. `@ScaledMetric`(EditCardView.swift:31-35). 〔빌드 + 드:V절·Z절. 화면 감각은 사용자 보고 수준〕 |
| G12 | 카드가 떠 있는데 다른 얘기를 함 | ✅ | 카드 무효 — "진행하지 않았어요"로 바뀜(`cancelPendingAsk` :945-950, 호출 :285). 〔드:Q절·P절 P-6〕 |
| G13 | 카드를 반쯤 고른 채 앱 종료 | ✅ | 보류 상태는 디스크에 남지 않음(saveHistory 필터 :134-136). 〔드:Q절〕 |
| G14 | "그거 기억해줘"/"잊어버려" | ✅ | 기억 기능 자체가 없다 — 도구·저장·설정 UI 전부 제거. 재확인: grep → **코드 참조 0건**(남은 1건은 제거 사유 주석, AIAssistant.swift:1857). 〔드:Q절(Mirror 런타임 검사) + 관찰(2026-09-16) — 기억 불가 안내 문구가 실제로 나왔다(G3 인용문과 같은 관찰 계열)〕 |
| G15 | "일정 생성" 한마디로, 탭만으로 완결 (①) | ⚠️ | 카드 확장: 제목·목적지·시각 줄까지 물음(askFields :487-498, required 비움 :1107-1110). 결함 H 수정: 시각 유무를 `filled()`로(:495-498) — `arrival_iso:""`에도 시각 줄이 뜬다〔드:W1〕. 결함 L: `일정 생성` 한마디에 모델이 **툴을 부르지 않고 말로 되물었다** — 카드가 있어도 모델이 그 카드에 도달시켜 주지 않으면 소용없다. 고침은 절대 규칙 8(:1050 — "정보가 부족해도 말로 되묻지 말고 create_schedule을 호출, 빈 인자로") **프롬프트뿐**. 앱 쪽 결정적 강제는 검토됐고 기각됐다: 발화에서 "생성 의도"를 판별해 빈 호출을 합성하는 것은 그 판별 자체가 "앱이 지어내는 값"이 된다 — **E와 같은 계열의 수용된 갭**이다〔문자열 — Y절 L항 존재 확인〕. 규칙 8 추가 후 **사용자 보고** 있음(말로 되묻지 않고 툴 호출, 개별 증거 없음) → 확인 목록 (b) 2 |
| G16 | 시각 줄 — 도착 기준인지 출발 기준인지 내가 정함 | ✅ | 하이브리드: 기준 칩 2개 + DatePicker(EditCardView.swift:186-263). 기준은 **둘 다 미선택 시작**, 기준 없으면 확인 잠김(datetimeEditor의 basis 가드 EditCardView.swift:231-245), 재탭 접두 교체(`tapBasis` EditCardView.swift:175-180 → `rechooseTimeBasis` AIAssistant.swift:869 — 커밋된 시각이면 같은 시각으로 접두만 바꿔 재확정). 출발 기준 확정 시 여유 줄 흐림 + 캡션(EditCardView.swift:145-152), 실리지 않고 교착 없음(AIAssistant.swift:917-919 — 여유가 필요 없는 시각에 확인이 막히지 않게 한다). 〔드:V절 ①-1~①-3 + 사용자 보고〕 |
| G17 | 즐겨찾기에 없는 일반명사로 장소를 말했다 ("회사"·"학교" 등) | ✅ | **결함 K 수정(2026-09-16 저녁 관찰 → 수정)**: 사용자가 `회사`라고 말했는데 즐겨찾기에 없자 검색 **첫 결과**를 확인 없이 채택, **'농업회사법인 화조원'으로 118건 등록**(관찰). 고침: `genericPlaceWords` 스냅숏(집·회사·학교·사무실·우리집, :2397 — `placeNotFound` :2327-2337과 **같은 목록 공유**)을 즐겨찾기 검사 뒤·검색 앞 게이트로(`resolveDestination(_:creation:)` :2431, `resolveOrigin` :2362), **생성 경로 5개 호출점만** `creation: true`(:1329·:1472·:1483·:1611·:1657). 조회 3곳(check_travel_time :2161·recommend_meal :2099·update_schedule :2209)은 무변경. 〔드:Y절 K 5건 + Z절 O-1~O-8 + **관찰(2026-09-16)** — 사용자가 `사무실`을 말해 목적지 줄이 뜨고(O1), `[장소 검색]`에서 후보를 골라 **'가산3차 SK V1센터'로 등록**까지 이어진 사슬(K 게이트가 좌표 있는 값은 통과시킨다는 것의 기기 증명). **한계(행의 일부로 적는다)**: (1) 목록은 스냅숏이고 완전하지 않다(:2395-2396 — 학원·직장 등 목록 밖 일반명사는 여전히 검색으로 조용히 해석된다). (2) `update_schedule`의 `new_place_query`(:2208)는 같은 노출인데 이번 범위 밖으로 남았다 — 조용한 확장이 아니라 **알려진 잔여**다〕 |

## H. 알림

| # | 사용자 요구 | 상태 | 근거 |
|---|---|---|---|
| H1 | "출발 30분 전에 알려줘" | ✅ | 발화값은 파서가(:341-342), 안 말했으면 카드 알림 줄(:633-638 — 칩 + 직접입력 0~1440분). "알림 필요 없어"면 줄이 안 생김(:481). 〔빌드·코드 — 실제 수신은 확인 목록 (c) 1〕 |
| H2 | "이 일정은 알림 필요 없어" | ✅ | `notifyEnabled` + AI `notify_enabled`(:1389) + 토글. 예약 코드 `scheduleDepartureNotification`(Store.swift:1041-1051) 한 곳. 〔빌드·코드〕 |
| H3 | 반복 일정 뒤쪽 회차 알림 누락 | ✅ | 64건 제한 대응 — `rescheduleNearestNotifications`(60건, Store.swift:1217-1241). 〔빌드·코드〕 |
| H4 | besir에서만 알림(캘린더 앱 중복 제거) | ✅ | `reminders` 해제 + PATCH 정리(GoogleCalendarService.swift:78·:123-124·:170). 〔빌드·코드 — 캘린더 앱 모습은 확인 목록 (c) 2〕 |

## I. 연동

| # | 사용자 요구 | 상태 | 근거 |
|---|---|---|---|
| I1 | 구글 캘린더 자동 등록 | ✅ | `autoAddToCalendar` → 업로드는 등록 뒤의 직렬 큐(`enqueueCalendarUpload` Store.swift:817-852 — 요소 단위 반복 대입 없이 복사본에서 한 번). 대량 변경은 쓰기 묶음(2ea30c9). **상세는 N절** — 등록이 업로드를 기다리지 않게 바뀌었다(결함 N). 〔빌드·코드 + 드:N절〕 |
| I2 | "이건 캘린더에 올리지 마" | ✅ | `syncToCalendar`/`wantsCalendarSync`(Models.swift:195·:512) + AI `add_to_calendar`(:1390·:1518) + 토글. 큐·푸시가 건별 존중(Store.swift:825·:884). 〔빌드·코드〕 |
| I3 | 앱 안 켜도 동기화 | ⚠️ | `BGTaskScheduler`(App.swift:20·:43) 있으나 iOS가 실행 시점 결정, 강제 종료 시 미실행. 〔빌드·코드〕 |
| I4 | "근처 맛집 추천해줘" | ✅ | `recommend_meal`(:2089-2134) + "주변 맛집"(J1) + be full sir. 반경 0이면 기본 1000m(:2094). 〔빌드·코드〕 |

## J. 맛집 추천 (be full sir)

*(2026-09-16 판 작성 당시 ActivityDetailView·FullSirView는 그날 변경분이 아니었다. 이후
t3(2026-09-22)가 ActivityDetailView를 EditCard 크롬으로 전환했는데 "주변 맛집" 섹션은 그대로
옮겨갔다 — 2026-09-24 재확인: ActivityDetailView.swift:31의 이유 주석("실기기 테스트 피드백으로
이동 일정 상세에서 옮겨옴"). FullSirView는 이 Day가 건드리지 않았다.)*

| # | 사용자 요구 | 상태 | 근거 |
|---|---|---|---|
| J1 | 일정 상세에서 목적지 주변 맛집 보기 | ✅ | `ActivityDetailView` "주변 맛집" → `PlaceSearch.nearbyPlaces`(FD6, 거리순). 〔빌드·코드〕 |
| J2 | 카페도 보기 | ✅ | 맛집/카페 세그먼트(FD6/CE7). 〔빌드·코드〕 |
| J3 | 업종·거리·주소 확인 | ✅ | "한식 · 230m" + 도로명 주소. 〔빌드·코드〕 |
| J4 | 카카오맵에서 자세히 보기 | ✅ | 각 행의 `place_url` 링크. 〔빌드·코드〕 |
| J5 | 주변에 결과가 없을 때 | ✅ | "찾지 못했어요" 빈 상태 — AI도 재검색 제안. 〔빌드·코드〕 |
| J6 | 추천을 골라 일정으로 등록 | ✅ | `FullSirView` "일정으로 추가" → `addActivityWithTravel` + `MealLog.activityId`. AI는 `log_as_meal:true`(:1521-1524). 〔빌드·코드〕 |
| J7 | 먹은 것 기록(`MealLog`) | ✅ | "먹었어요" + "최근 먹은 것". 일정 삭제 시 안 먹은 기록도 제거(`removeUpcomingMeals` Store.swift:437-443). 〔빌드·코드〕 |
| J8 | 예산 기반 추천 | ❌ | be rich sir(Phase 3) 이후 |
| J9 | 배달·요리 카테고리 | ❌ | Day 8(조사 Day) 남음 |
| J10 | AI에게 메뉴 추천받기 | ✅ | `recommend_meal` — keyword 재검색, 시각만 말하면 그 장소 주변. 반경 0 폴백〔관찰(2026-09-15) — 할루시네이션 없음〕 |
| J11 | 홈에서 서비스 선택 | ✅ | `RootView` 카드. 〔빌드·코드〕 |

## K. 화면 조작

| # | 상황 | 상태 | 근거 |
|---|---|---|---|
| K1 | 월간 ⇄ 일간 전환 | ✅ | 헤더 화살표 버튼. 〔빌드·코드〕 |
| K2 | 월간·주간·일간 좌우 스와이프 | ✅ | `SwipePager`(ContentView.swift:799). 〔빌드·코드〕 |
| K3 | 스와이프 중 날짜가 잘못 선택됨 | ✅ | `@GestureState` 추적. 〔빌드·코드〕 |
| K4 | 블록 위에서 세로 스크롤 | ✅ | UIKit 동시 인식. 〔빌드·코드〕 |
| K5 | 블록 탭 → 상세 | ✅ | 히트테스트와 렌더링이 `span(for:on:)`(ContentView.swift:542·557) 공유 — 계약 5의 원래 사례. 〔빌드·코드〕 |
| K6 | 겹친 블록 중 짧은 것 선택 | ✅ | 지속시간 최소 우선. 〔빌드·코드〕 |
| K7 | 꾹 눌러 드래그로 시간 이동 | ✅ | 반복이면 "전체/이 일정만"(ContentView.swift:780-788). 〔빌드·코드〕 |
| K8 | 활동을 옮기면 묶인 이동도 이동 | ✅ | `linkedActivityId`(Models.swift:190) + `linkedLegs`(Store.swift:1118-1132). 〔빌드·코드〕 |
| K9 | 자정을 넘기는 블록 표시 | ✅ | 결함 G·G-2 수정: 겹침 나열(ContentView.swift:92·:105, `Store.overlapsDay` Store.swift:38) + 그리는 날로 자르기(`span(for:on:)` :542·557) + 점 같은 판정(`recomputeDaysWithSchedule` Store.swift:132·`dayKeys` :157). 〔드:X절 10건 + W3 종단 1건 + **사용자 보고(2026-09-16)** — 확인 목록 6 수행·통과 답변. **커버리지 경계**: 드라이버가 재는 건 "어느 날에 나열되고 점이 켜지는가"까지 — 잘린 블록의 높이·위치는 ContentView 산술이라 기기 영역이고, 개별 증거는 없음 → 확인 목록 (b) 6〕 |
| K10 | 빠르게 연속 스와이프 | ⚠️ | `asyncAfter(0.22)` 경합 가능 — 재현 안 됨. 〔빌드·코드〕 |

## L. 연동·예외 상황

| # | 상황 | 상태 | 근거 |
|---|---|---|---|
| L1 | 구글 캘린더 등록·삭제 전파 | ✅ | `syncWithGoogle`(Store.swift:1336-1404) + 묘비. 〔빌드·코드〕 |
| L2 | 캘린더에 중복 생성 | ✅ | `reconcileActivities`(Store.swift:1412-1473) + `fetchBesirItems`. 〔빌드·코드〕 |
| L3 | 캘린더 앱에서 알림 중복 | ✅ | `reminders` 해제 + PATCH(GoogleCalendarService.swift:123-124·Store.swift:1453-1457). 〔빌드·코드〕 |
| L4 | 삭제한 일정이 되살아남 | ✅ | `deleted_gcal_ids.json`(Store.swift:83). 〔빌드·코드〕 |
| L5 | 앱 안 켜도 동기화 | ⚠️ | 백그라운드 시점은 iOS 몫(I3와 같은 사실, App.swift:20-43). 〔빌드·코드〕 |
| L6 | 다른 앱에서 텍스트·이미지 공유 | ✅ | Share Extension → `handleShared`〔관찰(2026-09-10)〕 |
| L7 | 이동시간 계산 실패 | ✅ | 빨간 블록(숨기지 않음), 탭·삭제 가능, 목록 ⚠️(:2043). 등록 요약은 "출발·도착 시각이 비어 있어요"(:1401-1405) — 요약이 방금 만든 이벤트를 직접 보므로(A9 결함 J) 참으로 말한다. 〔빌드·코드〕 |
| L8 | 장소 검색 결과 0건 | ✅ | 빈 목록, 크래시 없음. 카드의 장소 검색 줄도 "후보를 찾지 못했어요"(P5). 〔빌드·코드〕 |
| L9 | 위치 권한 거부 | ⚠️ | 수동 지정으로 우회. 안내 문구는 나옴(:1325·:2110)지만 설정 연결 없음. 카드 출발지 줄은 권한 없이도 고를 수 있음(G10). 〔빌드·코드〕 |
| L10 | 오프라인 | ⚠️ | 로컬 데이터 정상 — 결함 X1 수정(t23): 콜드 스타트 오프라인에서 `fetchBesirItems`가 실패를 빈 결과로 삼켜 sync가 gid 있는 이동 일정을 전부 지우고 알림까지 취소하던 것을, 조회 실패(토큰·HTTP 비200·깨진 본문)를 **예외로** 바꿔 sync 조기 복귀로 막음(GoogleCalendarService.swift:106-133·Store.swift:1365-1368 — 정말 빈 캘린더(200+items 부재)는 빈 결과 유지). 250건 초과 조회 누락(X2)도 `nextPageToken` 순회로 해소(`:135-144` — 드:X2절 5단언). 이동시간·AI는 실패 문구만. 카드 장소 검색의 빈 결과 문구는 오프라인을 함께 말한다(P5). 런타임 확인은 .moai/reports/t23/progress.md §5 시뮬레이터 절차(운영자 몫). 〔빌드·코드·드:X2절 — 미관찰〕 |
| L11 | AI 일일 할당량 소진 | ✅ | `classify`(:997-1011) — 429 + OpenAI 마커 조합만 쿼터로 판정. 안내문(:322-325)은 초기화 시각을 적지 않음. 〔빌드 — 미관찰 → 확인 목록 (c) 3(기회)〕 |
| L12 | 앱 강제 종료 후 복귀 | ✅ | JSON 영속화(Store.swift:70-81). 카드 보류 중 종료 포함(:131-133)〔드:Q절〕. 번호만 재발급(D2). 〔빌드+드:Q절〕 |
| L13 | 기기 잠금 중 백그라운드 동기화 | ✅ | 키체인 `AfterFirstUnlock`(GoogleCalendarService.swift:379). 〔빌드·코드〕 |
| L14 | 모델이 터무니없이 긴 기간을 보냈을 때 (end_iso 9999년 등) | ✅ | 결함 I 수정: 9999년이 `parseDate`를 통과해 `dayKeys`가 didSet 안에서 약 290만 일을 걷으며 **앱을 켤 때마다 얼리고 지울 수조차 없게 만들던 것**을, 366일 상한(Store.swift:54, 걷기 :157-172의 `keys.count < maxIntervalDays`) + **생성 시점 가드**(executeCreateActivity :1463-1465 — 카드보다 먼저, 366일 초과 저장 안 함+되묻기)로 막음. 〔드:W2·W3 3건 — 기기 시험은 목록에 없어 미수행〕 |
| L15 | 이동 질의를 장소로 못 찾았을 때 조용히 넘어가지 않음 | ✅ | **결함 M 수정(2026-09-16 저녁 관찰 → 수정)**: 카드에서 가는 편 자동차·오는 편 대중교통까지 고른 왕복이 **이동 0건**으로 만들어지고 요약이 성공 문구만 남겼다(events.json 직독). 고침: `executeCreateActivity`가 **비어 있지 않은 질의의 해석 실패**를 모아 요약 끝에 드러낸다(수집 :1491-1502, ⚠️ 문구 :1556-1560). **"가는 편 없음" 토큰과 빈 값은 실패로 세지 않는다** — 명시적 안 만들기와 실패는 다른 사건이다. 장소 질의(place_query)도 같은 클래스로 포함. 〔드:Y절 M 3건 + **사용자 보고(2026-09-16)** — 확인 목록 4의 M 부분 수행·통과 답변(개별 증거 없음) → 확인 목록 (b) 7. **K와의 맞물림**: K가 "엉뚱한 곳으로 만드는" 사고를 "안 만들고 알리는" 사고로 바꾸고(O가 다시 "대화 안에서 고르게" 만들고), M이 그 "안 만들었음"이 요약에 묻히지 않게 막는다 — 앞뒤 짝이다〕 |

---

*(아래 세 절은 2026-09-16 마지막 파동의 결함 N·O·P다. 절 글자를 결함 글자에서 따왔기 때문에
M절은 비었다 — 결함 M의 행은 L15에 있다.)*

## N. 캘린더 업로드 상태 (결함 N)

**무엇이었나.** 등록이 구글 캘린더 업로드를 기다렸다 — 58회차짜리 반복 하나에 사용자가 그
58번의 순차 업로드를 다 기다린 것이 실제 원인이다(팀 리드 측정: 저장된 58건 전부
`googleEventId` 보유, 계정 연결됨). **정직한 기록**: 처음 "계정이 미연결인데 헛시도한다"고
진단했으나 **틀렸다** — 그 가드 버그는 진짜지만(아래 N3) 이 증상의 원인이 아니었고, 계정을
붙이지 않은 사용자에게 터질 **잠복 버그**였다.

| # | 사용자 요구 | 상태 | 근거 |
|---|---|---|---|
| N1 | 일정 등록이 즉시 끝난다 (58건 반복도 기다리지 않음) | ✅ | 등록은 로컬에서 끝내 즉시 응답하고(`addEvent` 저장 뒤 반환 Store.swift:606-609), 업로드는 `enqueueCalendarUpload`가 **뒤에서 직렬**로 돈다(Store.swift:817-852 — 전용 Task가 앞 작업을 기다린다 :800·:845-851). 〔드:N절(간접) + **관찰(2026-09-16)** — 사용자: **"눈에 띄게 빨라졌어요"**〕 |
| N2 | 올라가는 중·실패가 보이고 다시 시도할 수 있다 | ✅ | 상태가 보이는 자리 둘: 일정 상세 — pending 라벨·failed 문구 + "구글 캘린더에 추가" 버튼(EventDetailView.swift:121-157, 재시도 :159-164); 설정 — 대기·실패 합계 + "다시 시도"(SettingsView.swift:51-69, `retryFailedCalendarUploads` Store.swift:866-869). 등록 요약의 대기 문구 `calendarPendingNote`(AIAssistant.swift:1420-1424 — 판정의 단일 출처는 레코드의 pending 표시, 세 생성 경로에 붙임 :1409·:1562-1564·:1697-1699). 〔드:N절 N-3·P절 P-7. 화면 전환(올리는 중→등록됨) 목격은 개별 증거 없음 → 확인 목록 (b) 10〕 |
| N3 | 계정이 연결돼 있지 않으면 쓰기를 시도하지 않음 | ✅ | 모든 쓰기 경로가 `googleConnected`(Store.swift:494 = `hasGoogleCalendar && gcal.isConnected`)로 막힌다: enqueue :818·push :880·:915·수정 :777·:985. 예전엔 `hasGoogleCalendar`(= 클라이언트 ID 유무, 항상 참)만 봐서 미연결 기기에서 건마다 로그인 시도 + 조용한 실패였다(push 주석 :877-879). 연결 진입점·상세의 수동 추가 버튼·설정 UI는 의도적으로 제외(사용자 동작 경로 — 자동 쓰기 경로와 달리 사용자가 결과를 직접 본다). 〔드:N절 N-1. 단 "ID는 있는데 계정만 없다" 반쪽은 드라이버가 clientID 비움으로만 시험하므로 기기 확인 영역〕 |
| N4 | 상태의 진실 — 성공은 어디에 기록되나 | ✅ | `CalendarUploadState`는 **pending·failed 두 case뿐**(Models.swift:132-151), 성공의 단일 출처는 `googleEventId`다(계약 5 — 성공 시 상태를 nil로 되돌림 Store.swift:892·:925). 필드는 Optional이라 **calendarUpload 키가 없는 옛 JSON이 그대로 읽힌다**(Models.swift:180-183 — 비-Optional 기본값이면 옛 파일이 통째로 디코딩 실패해 일정이 사라진다). pending은 디스크에 먼저 남는다(Store.swift:825 — 여기서 죽어도 "안 올라감"이 보인다). 〔드:N절 N-3·N-4(사용자 시뮬레이터에 실제로 저장돼 있던 레코드 키 구성으로 검증)〕 |

## O. 일반명사를 말했을 때 대화가 끊기지 않음 (결함 O)

**무엇이었나.** K 게이트가 생긴 뒤 "회사"처럼 즐겨찾기 없는 일반명사를 말하면 실행부가
"즐겨찾기에 추가해 주세요"로 되돌려 보내 **대화가 거기서 끊겼다** — 사용자가 채팅을 떠나 ⭐
화면에 등록하고 처음부터 다시 말해야 했다. 고침: 그 장소 줄을 **카드에 띄운다**(줄 이름은 그대로
두고 **캡션**으로 이유를 적는다). 줄 이름을 상황마다 바꾸지 않은 근거: 그 문자열을 카드 줄·실행부
거부 안내(placeNotFound :2327)·캡션(unknownPlaceNote :2415-2417)이 같이 쓰기 때문이다
(originField 주석 :547-549 — 세 자리가 서로 다른 말을 하게 된다).

| # | 사용자 요구 | 상태 | 근거 |
|---|---|---|---|
| O1 | 즐겨찾기 없는 일반명사를 말했을 때 대화가 끊기지 않고 카드로 | ✅ | 값이 차 있는데 앱이 풀 수 없으면 그 줄이 카드에 뜬다(askFields의 `unknownPlace` :476-479 → destinationField/originField(unknown:) :491-494·:565-570). 캡션: *"besir가 '회사' 위치를 몰라요. 고르면 이번 일정에 쓰고, ⭐에 추가해 두면 다음부터 이름만으로 돼요."*(:2415-2417). 칩(즐겨찾기)·장소 검색이 같은 줄에 있다 — **이 줄에서 고르면 이번 등록은 대화 안에서 끝난다**. 〔드:Z절 O-1~O-8 + **관찰(2026-09-16)** — `사무실`에 목적지 줄이 실제로 떴고 직접입력(장소 검색)도 열려 있었다〕 |
| O2 | 카드에서 고르면 그 값으로 정확히 1회 등록 | ✅ | 칩·검색 후보로 고르면 `resolvePendingAsk`가 1회 실행(O-2). 즐겨찾기가 있으면 줄이 아예 안 생긴다(O-3 — 카드가 길어지지 않음), 진짜 장소명은 게이트 밖(O-4). 〔드:Z절 + 관찰 — 후보에서 **'가산3차 SK V1센터'를 골라 그 장소로 등록**까지 확인(P1과 같은 관찰)〕 |
| O3 | 실행부 가드는 그대로 — 카드를 지나지 않은 호출은 여전히 막힘 | ✅ | 카드 먼저, 가드 다음: `resolveDestination(creation:)`의 게이트(:2431)와 `placeNotFound`(:2327)는 살아 있다. `missingAskedArguments`는 note 붙은 줄을 "비어 있음"으로 세지 않는다(:1263-1267 — 문구가 거짓이 되지 않게). 〔드:Z절 O-5〕 |
| O4 | 활동·반복 일정도 같다 | ✅ | place_query·travel_from_query·return_to_query가 각각 자기 줄로(:516-518), 반복(118건 사고의 도구)도 같은 줄(:509). 이동 없는 활동도 못 푸는 장소면 묻는다 — 위치 없는 활동으로 조용히 등록되던 자리(M보다 앞에서 막힌다). 사용자가 말한 출발지에는 "가는 편 없음" 칩을 붙이지 않는다(:606-608). 〔드:Z절 O-6·O-8〕 |

## P. 장소 줄 직접입력 — 주소 검색 (결함 P)

**무엇이었나.** 장소 줄의 `[직접입력]`이 빈 칸이었다 — 무엇을 적든 확인을 누른 **뒤에야** 실행부가
"'ㅁㄴㅇㄹ' 위치를 찾지 못했어요"로 되돌리는 구조였다(사용자가 `ㅁㄴㅇㄹ`를 넣어 시험하며 드러났다:
"위치 입력칸에 주소 검색 기능이 없음"). 고침: `[직접입력]`이 `[장소 검색]`이 되어 후보를 띄우고
**탭해서 고른다** — 고르는 순간 좌표까지 확정돼 확인 뒤에 실패할 자리가 사라진다.

| # | 사용자 요구 | 상태 | 근거 |
|---|---|---|---|
| P1 | 장소를 검색해서 고른다 | ✅ | 칩 이름이 장소 줄에서 `[장소 검색]`으로(EditCardView.swift:125-128), 입력마다 후보가 그 줄에만 얹힌다(placeSearchEditor EditCardView.swift:318-346, 후보 행 EditCardView.swift:349-370 — 이름+주소, 탭하면 확정). 후보 5건 상한(`maxPlaceSuggestions` AIAssistant.swift:792 — 카드가 화면 밖으로 길어지지 않게). 〔드:P절 P-1 + **관찰(2026-09-16)** — 후보가 떠서 '가산3차 SK V1센터'를 골랐다〕 |
| P2 | 일반명사 거절과 검색은 다르다 — 기준은 좌표의 유무 | ✅ | 자유 텍스트로 일반명사를 확정하는 건 거절(submitCustom :849-852 — 거절된 자리에 캡션 붙음), 검색 결과에서 고른 같은 이름(진짜 상호, 좌표 있음)은 통과(`choose(field:place:)` :761-765 → `confirmedPlaces`). `사무실` 타이핑은 검색되고, 자유 텍스트 확정은 거절되고, 후보 탭은 통과한다. 〔드:P절 P-2 + **관찰(2026-09-16)** — 검색과 거절 둘 다 확인됨〕 |
| P3 | 고른 지점이 그대로 등록된다 (이름이 재검색되지 않음) | ✅ | **열쇠는 인자에, 좌표는 옆 표에**(`confirmedPlaces` :52-55) — 열쇠는 보통 이름 그대로지만, 같은 이름이 다른 좌표에 이미 잡혀 있으면 `confirmedPlaceKey` :778이 `이름 · 주소` → `이름 · 좌표` 순으로 갈라 준다(카드 t6). 이름이 겹칠 때 뒤 선택이 앞 좌표를 덮던 것을 닫은 자리이고, 갈린 열쇠는 확정 칩·거품 줄에 그대로 보인다 — 이름만 보내면 실행부가 다시 검색해 모호한 이름이 다른 지점으로 풀린다('회사'→농업회사법인 화조원, K와 같은 부류). 실행부는 고른 좌표 그대로 쓴다(resolveOrigin :2359·resolveDestination :2430). 즐겨찾기와 이름이 겹치면 즐겨찾기가 이긴다(오래 사는 설정 쪽 — "집이라고 했는데 어제 고른 카페" 방지). 〔드:P절 P-1·P-3〕 |
| P4 | 한글을 빨리 쳐도 검색이 폭주하지 않는다 | ✅ | 입력이 멈춘 뒤 한 번만 부른다 — **350ms 묶음**(searchPlaces :799-820, 지연 상수 EditCard.swift:294 — 한글 조합이 한 글자를 완성하는 간격보다 길어 "가→강→강남"이 한 번으로 묶인다) + 같은 질의 재호출 차단(EditCard.swift:317, 지우고 다시 친 경우). 카카오 일일 할당량 보호(외부 한도로 이미 데인 이 프로젝트의 상수 원칙). 검색 중에도 카드의 다른 줄은 조작 가능(확인 잠금은 chosen만 본다). 〔드:P절 P-4·P-5〕 |
| P5 | 카드가 사라진 뒤 돌아온 결과·0건 처리 | ✅ | 결과는 돌아온 뒤 **줄 id로 자리를 다시 찾아** 없으면 조용히 버린다(setLookup :826-830 — H1 부류: await 앞뒤로 자리를 믿지 않는다. 줄 id는 카드마다 새로 만들어져 늦은 결과가 다른 카드에 앉을 수 없다). 0건·오프라인은 "후보를 찾지 못했어요. 다른 이름이나 주소로 적어보세요(인터넷이 끊겨 있을 때도 이렇게 보여요)"(EditCardView.swift:334-339 — PlaceSearch가 둘 다 빈 배열로 돌려주어 구분 못 하는 것을 구분한 척하지 않고 함께 말함). 〔드:P절 P-6·P-4의 빈 문구〕 |

---

## 요약 — 2026-09-16 최종 (Phase 1.6: 실기기 회귀 수정 + 카드 확장 + 업로드 상태)

**상태**: HEAD `7b2e274` + 커밋되지 않은 작업 트리. 이 문서의 줄번호는 이 트리 기준이며 커밋 후 다시
맞춰야 한다. 하루를 **결함 16건(A~P) + 카드 확장 ① + 드라이버 오염 사건 1건**, 세 번의 파동으로
정리한다: **① 사용자 기기 확인**(결함을 발견한 관찰) → **② 코드 검사**(관찰 뒤에 남은 구멍을 읽기로
찾음) → **③ 사용자 재확인**("모두 확인완료" — 수정된 동작을 관찰로 닫음).

**1차 — 사용자 기기 확인(결함 발견).** 2026-09-15 판(88/88 드라이버 + 빌드 초록)을 관찰이 뒤집은
여덟에, 저녁 시뮬레이터 재전사의 셋, 마지막 재확인 중 드러난 둘(O·P)이 더해졌다:

| 결함 | 증상(관찰) | 고침 | 대응 행 |
|---|---|---|---|
| A | 왕복이 편도로 퇴화 | 선언 대칭화 + 가는 편 출발지 줄 | A4·G8 |
| B | 되묻는 턴에 말한 값 증발 | statedArgs 병합+소거 | G1 |
| C | 첫 호출의 ignore로 겹침 검사 skip | ConflictAsk 게이트 | F1·F2·F3 |
| D | **미선언 인자 통과 → 118건 등록** | sanitizeModelArgs | G2·B4 |
| E | 출발지 지어냄 | 프롬프트 전용(사용자 결정) | G10 ⚠️ |
| F | 못 지킬 기억 약속 | 프롬프트 규칙 7 | G3 |
| G·G-2 | 자정 반쪽 표시 | 겹침 나열+자르기+점 통일 | K9 |
| ① | "일정 생성" 불가·목적지 지어냄 | 카드 제목·목적지·시각 줄 | G15·G16 |
| K | `회사`가 검색 첫 결과 **'농업회사법인 화조원'**으로 118건 등록 | 일반명사 게이트(생성 5개 호출점) | G17 |
| M | 왕복이 **이동 0건**으로 등록, 요약 침묵 | 해석 실패 ⚠️ 요약 가시화 | L15 |
| L | `일정 생성` 한마디에 **말로 되묻고 툴 안 부름** | 프롬프트 규칙 8(수용된 갭) | G15 ⚠️ |
| O | 일반명사를 말하면 **대화가 끊기고** ⭐ 화면에 다녀와야 했다 | 못 푼 장소 줄을 카드로 + 캡션 | O1~O4 |
| P | 장소 직접입력이 빈 칸 — `ㅁㄴㅇㄹ`는 **확인 뒤에야** 실패 | `[장소 검색]` 후보 탭 + 좌표 확정 | P1~P5 |

**2차 — 코드 검사(관찰 뒤에 남은 구멍).** 초록인 트리에서 나왔다: H(빈 문자열 시각 — `filled()`),
I(end_iso 9999년 앱 동결 — 366일 상한+생성 가드, L14), J(거짓 요약 — `addEvent` 반환, A9).
그리고 **N** — 등록이 캘린더 업로드를 붙잡던 것(58회차 순차 대기; 초기 진단은 틀렸고 실측으로
바로잡았다 — N절 머리글). 고침: 등록은 로컬에서 끝내고 업로드는 직렬 큐로, 모든 쓰기 경로
`googleConnected` 게이트, 상태는 pending·failed 둘뿐(성공의 단일 출처는 gid).

**3차 — 사용자 재확인.** 사용자가 시뮬레이터에서 오늘 수정 전부를 확인했다("모두 확인완료").
이 문서는 그 말을 있는 그대로 등급 나눠 적었다(관찰 = 이름 붙은 증거, 사용자 보고 = 개별 증거 없음).

**관찰로 닫힌 것(이름 붙은 증거)** — 이 판의 핵심 진전:
- **D**: 카드가 떴다 — `origin_query=__current_location__` 토큰(G2·B1·B4).
- **A**: `가는 편 car · 오는 편 transit`, 서로 다른 값이 한 호출에(A4·G8).
- **F**: *"…이번 일정 요청에만 적용할 수 있어요… 매번 말씀해 주세요."* — 못 지킬 약속을 하지
  않는 행동(G3·G14).
- **K·O·P 한 사슬**: `사무실` → 목적지 줄+캡션이 뜨고(O1) → `[장소 검색]`에서 후보를 골라
  **'가산3차 SK V1센터'로 등록**(O2·P1·P2·G17), 자유 텍스트 거절도 확인(P2).
- **N**: "눈에 띄게 빨라졌어요"(N1).

**가장 중요한 정정.** "선언에서 빼면 못 보낸다"는 거짓이었다(결함 D). 방어는 앱의 입력 정화다.

**이번 판의 규칙(독자가 가정하면 안 되는 것).**
1. **초록 ≠ 기기 동작** — 하루 세 번 증명. ✅는 "드라이버 증명"·"관찰"·"사용자 보고"로 한정한다.
2. **"모두 확인완료"는 개별 증거가 아니다** — 관찰(이름 붙은 증거)과 사용자 보고(통과 답변만)를
   나눠 적고, 사용자 보고 행은 다음 Day 목록에 남긴다.
3. **U절·Y절 L항(프롬프트 규칙)은 문자열 존재 확인** — E·L이 그 계열이다(F는 관찰로 닫힘).
4. **X절은 나열·점 판정까지만** — span 산술은 기기 확인(K9).
5. **결함 16건 중 관찰로 닫힌 건 D·A·F·K·O·P·N(7건)** — 나머지의 수정은 드라이버·빌드·사용자
   보고 수준이다.

**⚠️ 현존**: D2(번호 다리 — 모델 준수 미관찰), G10(출발지·목적지·제목 지어내기 — 앱 강제 불가),
G15(① 도달 — 프롬프트 전용, 수용된 갭), G17 한계(스냅숏 불완전·update_schedule new_place_query
잔여), I3/L5(iOS 시점), K10(스와이프 경합), L9(설정 연결 없음), L10(오프라인).
**❌ 현존**: J8·J9(계획된 뒤 Day 작업).

**다음 Day 예정**(plan.md): 일정·활동 화면 UI 통일.

## 요약 — 2026-09-24 (UI통일 Day 닫기, card t9)

**상태**: 트리 `57f874f`(= origin/master) + 이 카드의 1줄 수리(AddEventView.swift:119). Day 전체는
카드 여덟 장(t1~t8)으로 닫았다 — 컴포넌트 추출과 AI 카드 전환(t1), AddEventView 전환·디바운서
단일화(t2), 활동 화면 전환(t3), EventDetailView 크롬·시각 포매터 단일화(t4), 데드 코드 정리(t5),
장소 좌표 사전 충돌 수리(t6), 편집 출발지 덮어쓰기 수리(t7), 드라이버 구조적 경화(t8). 사용자
관점에서 행이 거의 늘지 않은 이유: 이 Day는 **같은 요구를 같은 화면 문법으로 통일**한 Day이기
때문이다 — 새 사용자 상황은 F7(저장 버튼 이중 탭, 이 카드 수리) 하나다. 판정 집계 **✅ 103·⚠️ 8·
❌ 2·— 1**(2026-09-16 판 ✅ 102·⚠️ 8·❌ 2·— 1에서 F7 +1). 세는 방법: 표 행의 상태 칸을 파이썬으로
집계 — `re.match(r'^\| [A-Z][0-9]+ \|', line)`로 행을 골라 `\\|` 이스케이프를 보호한 뒤 4번째
필드를 센다(114행).

**이 판의 세 일.** (1) **인용 의미 전수 감사** — 228조각 대조, 11행 13조각 정정(위 t9 문단).
(2) **드라이버 검증 절을 t8 이후의 사실로 재작성** — "드라이버는 실제 앱 데이터에 쓴다" 정정,
낡은 GuardDriver 좌표 11 끝점 이동, O-1 정정("모델 API 할당량 안 씀"은 참, "네트워크를 전혀 안
탄다"는 코드상 성립하지 않는다 — 회당 최대 약 5회). (3) **이월 목록 전 항목 배치** — 아래 카드
후보·기각·수용과 확인 목록 3분할.

**⚠️ 현존(이 Day에도 변함없음)**: D2(번호 다리 — 모델 준수 미관찰), G10(출발지·목적지·제목
지어내기 — 앱 강제 불가), G15(① 도달 — 프롬프트 전용), I3/L5(iOS 백그라운드 시점), K10(스와이프
경합, 재현 안 됨), L9(위치 권한 거부 — 설정 연결 없음), L10(오프라인).
**❌ 현존**: J8·J9(계획된 뒤 Day 작업).
**수용된 갭(운영자 2026-09-24)**: AC-005·AC-006의 값 기록 절 미충족(t7 시뮬레이터 판정은 확보,
N₀·X·D 수치 로그 없이 수용). G10·G15의 모델 동작 갭도 수용 상태 그대로.

**다음 Day 카드 후보(앱 결함 칸으로 올리지 않는다 — 배치가 확정된 이월 목록)**:

1. **드라이버 위생 — 처리됨(2026-09-24, 카드 t10)**: O-1 시작 블록에서 `proxyBaseURL`·`appToken`을
   비워 프록시 경로 차단 + O-2 절마다 파일 백업 9쌍 제거(favorites 메모리 쌍은 절 격리 기능이라
   유지). 재게이트 결과는 아래 기준선 블록(t10 행) 참조.
2. **AddEventView 경합·표시 묶음 — 처리됨(2026-09-24, 카드 t11)**: 후속 20① recomputeEstimates await 경합(:464-474 — 늦게 끝난 옛
   좌표의 값이 모드 소요시간·예상 문구·배너에 남음, 저장값은 Store가 재계산해 무결) · 후속 20④
   gatedRows 인자 여섯 2벌(:179-182 ↔ :201-204) · N1 save()가 travelSecondsHint를 안 넘김(저장마다
   조회 1회 추가 + 폼 예상/저장값 어긋남 창 — AI 경로는 이미 넘긴다) · 후속 19 셋(값 있는 줄의
   스피너·권한 거부 시 출발지 줄 실종에 안내 없음·주석 범위).
3. **무취소 Task 부류 — 처리됨(2026-09-25, 카드 t12)**: 후속 12 nearby onChange(ActivityDetailView :362) + 이번 전수 검사 신규
   EventDetailView.loadTransitPath(:72·:153-168) — 늦게 도착한 옛 경로가 지도 폴리라인·환승 안내를
   덮어 마지막 상태와 어긋남. 표시만 틀리는 부류지만 "화면이 거짓말하는" 계열.
4. **Store.updateActivity 조용한 실패 — 처리됨(2026-09-25, 카드 t13)**: 활동 편집 뒤 재등록 실패가
   상태 기록 없이 삼켜지던 것(당시 좌표 :270-279)을 인라인 `try? createEvent` 삭제·업로드 큐 경유
   재등록으로 닫았다(updateActivity :263-293 — pending/failed 기록과 성공의 gid는 큐가 단일 출처로
   담당, updateEvent :945-992·updateRecurringSeries와 같은 문턱 세 개. code-safety 권고로 같은 계열의
   sync 2-5 성공 시 pending 미해제 — "올리는 중" 영구 좌초 — 도 함께 닫음, Store.swift:1468-1470).
   2026-09-25 sync 판정 D2·D3 수리 반영: 재등록은 autoAdd와 무관하게(운영자 결정), gid는
   원격 삭제 뒤 비교-후-소거로, sync 2-2·이벤트 가져오기는 실시간 묘비까지 본다(:1433·:1396).
5. **Theme/계약 6 묶음(ui-design)** — ConflictBanner 우회 3건(AddEventView :732·:735·:740) +
   AddActivityView :574 + ActivityDetailView 다섯 줄(:376·:384·:398·:401·:405) + 후속 16
   AIChatView:114.
6. **문구·주석 묶음** — 후속 18(주석 과적용 2건)·19③(주석 범위)·B5 "이동 수단/이동수단" 표기·
   **D-3 고지**("과거 일정에는 알림이 안 걸린다"는 사실을 화면이 말하게 하기 — 사용자가 (i)를
   골라 수락한 갭).
7. **장소 해석 UX** — U-4(이름→좌표 해석이 첫 검색 결과를 무조건 택함 — "스타벅스 홍대점"이
   대학로점으로 등록된 2026-09-23 관측) · AC-009 7·9번(재질문에 카드 장소 줄이 안 뜨는 것,
   2026-09-23 관측 FAIL) · U-2(장소 검색 재오픈 시 옛 선택 강조 잔존) · t7발 O-1 가설
   (Store.swift:358 출발지 폴백 `?? current.destination` — 출발지 없는 일정을 AI로 고치면
   출발지=목적지 0분 창. `isSamePlace` 가드는 create 경로 전용이라 여기 안 걸린다
   (AIAssistant.swift:1650 주석). 코드 읽기 가설, 미재현).
8. **U-1 편집 카드 통일**(가장 큰 카드) — 이동일정 편집창을 생성 시의 카드와 같은 문법으로(시간을
   고치면 활동과 분리되는 것·가는 편만 만들고 오는 편을 나중에 더할 방법이 없는 것을 함께 해결)
   + 삭제 연계 규칙(이동 삭제는 그것만, 활동 삭제는 묶인 이동도 함께).
9. **U-3 대화 메모리**(장기) — AI가 다시 물 때마다 이미 답한 내용을 다시 입력하게 된다 + 새 일정
   요청이 옛 일정 기억을 덮어쓰는 버그 가능성(운영자 지적).
10. **프록시 `/claude/messages` 죽은 경로 제거** — 앱 호출 0건(앱의 프록시 호출은 `/ai/chat` 한
    곳). ANTHROPIC 시크릿·redactSecrets 항목도 제거와 함께.

**기각(갭으로 올리지 않는다)**: 후속 20③(빈 이름 칩 — 출발지 이름 생산자 둘 다 빈 문자열을 내지
않는다), 후속 13(저장 게이트 약화 — `executeCreateActivity` :1449가 빈 제목을 거부, 수동·AI·편집
세 생성 경로 전부 도달 불가. 이 카드에서 plan.md 노트로 종결했다).

## 검증 방법

**기준선(2026-09-24, 카드 t10 드라이버 위생 — 이 레인이 `WT-driver-hygiene`(296b579 + t10 diff) 트리에서 직접 측정):**

- 가드 드라이버 — **신선 컴파일 후 전체 실행 exit 0 · `212/212 통과`(✗ 0)**. 불변식 9/9 ✓(t10부터
  첫 검사가 프록시 설정 비어 있음을 함께 잰다), 실제 지원 디렉터리 바이트 대조 통과(실행 전후
  `shasum -a 256` 동일 — events·activities `4f53cda1…`·config `e2698db8…`, t9 측정값과 같음),
  샌드박스 자가 삭제·잔여 0. **차단 관측**: 수리 재게이트(N1·N2 반영 트리) 실행 중 소켓 표집
  0건 — PID 한정(`lsof -nP -a -p`)·이름(`+c 0` 절단 해제) 표집, 0.2초 간격, **양성 대조로 이
  방법이 소켓을 보는지 먼저 확인**(저속 curl ESTABLISHED 관측). sync 독립 재실측(0.2초×38회)도
  0건. 한계: 프로세스 자신의 소켓만 보이고 MapKit처럼 시스템 데몬을 거치는 요청은 안 보인다 —
  차단의 주 근거는 코드 경로(프록시 필드 비움)다. 1차 run 레인의 이름 grep 표집("초당 1회 0건")은
  lsof COMMAND 9글자 절단으로 원리상 못 보는 표집이었음 — sync 판정 D1이 지목, 위 관측으로 대체.
  참고: 차단 뒤 완주가 **9초**(sync 재실측·본 재게이트 동일) — t5 시절 433초는 죽은 DNS를 향한
  프록시 조회 타임아웃 대기였던 것으로 풀이된다.
- iOS·macOS 빌드 → **BUILD SUCCEEDED, swift 경고 0/0**(검증 스킬의 경고 필터 그대로).
- 프록시 → **7/7 통과**.

**기준선(2026-09-24, Day 닫기 card t9 — 이 카드 레인이 `57f874f` + 1줄 수리 트리에서 직접 측정):**

- 가드 드라이버 — **샌드박스 격리 상태로 두 번 신선 컴파일·실행, 두 번 다 exit 0 · `212/212 통과`**.
  불변식 줄 9/9 ✓, 실제 지원 디렉터리 바이트 대조 통과(실행 전후 `shasum -a 256` 동일 —
  events `4f53cda1…`·config `e2698db8…`·activities `4f53cda1…`), 샌드박스 정상 종료로 자동 삭제.
- iOS·macOS 빌드 → **BUILD SUCCEEDED, swift 경고 0/0** — 1줄 수리 전과 후 두 번 모두 측정.
- 프록시 → **7/7 통과**.
- **네트워크 관측**: 전체 실행 하나를 0.1초 간격 lsof로 136회 표집 — 소켓 0건. 다만 이것은
  구조적 부재의 증거가 아니다(아래 O-1 정정: 최대 ~5회 가능, 표집이 짧은 연결을 놓쳤을 수 있다).
- 이 회차 ux-check가 직접 관찰한 것은 아래 별도 블록(인용 감사·좌표 대조 — 드라이버·빌드는
  돌리지 않았다).

**기준선(2026-09-16, 그날 트리에서 팀 리드가 직접 실행·측정 — 역사 기록, 재실행 없이 인용):**
- 가드 드라이버 → **205/205 통과, exit 0**. 소스 `drvCheck` 호출부 201곳(루프 실행 포함 205).
  절 구성: …P·Q(B)·R(A)·S(C)·T(D)·U(문자열)·V(①)·W(H·I)·X(G·G-2)·J(요약)·Y(K·M·L)·
  **N(캘린더 가드·상태)·Z(결함 O)·P(결함 P)**.
- iOS·macOS 빌드 → **BUILD SUCCEEDED, 경고 0**(appintentsmetadataprocessor 공지 제외).
- **iPhone 17 Pro 시뮬레이터에 이 빌드 설치·실행, 기존 데이터 보존 확인(일정 39·활동 19·즐겨찾기 2)**.
  아래 확인 목록의 사용자 재확인은 이 앱에서 이뤄졌다.
- **드라이버 실행 뒤 로컬 레코드의 `googleEventId` 0건 증가** — 푸시가 일어나지 않았다는 잰
  증거(오염 사건 이후의 측정).

**212/212은 밀폐된 검사가 아니다 — 다만 2026-09-24 t10부터 드라이버의 프록시 경로는 구조적으로
막혀 있다.** 일부 단언(J절 등)은 MapKit에 의존한다. O-1 정정(2026-09-24 코드 추적)이 밝힌 과거
사실: 샌드박스 시드가 프록시를 끄지 않아 config.json 없는 샌드박스에서 `AppConfig.load()`가 내장
기본값(Config.swift — 살아 있는 프록시 주소·앱 토큰)을 줬고, 카카오 키워드 1회(P-4)·ODsay 3회
(Y-2·Z O-2·P-1의 대중교통 추정)·ipwho.is 폴백까지 회당 최대 약 5회가 가능했다(t10 재추적: O절
생성 4회·V절 확정 2회의 transit 추정과 P-6의 카카오 조회까지 더해져 실제로는 이보다 많았다 —
계상이 관대했을 뿐 차단 필요성은 커진다). t10이 시작
블록에서 `proxyBaseURL`·`appToken`을 비워 프록시를 경유하는 조회를 전부 MapKit 폴백으로
돌렸다(프록시는 안 탄다 — MapKit 자체는 Apple 서버로 가는 요청이다. 단언 판정은 그대로 — P-4
라벨은 〔MapKit 단독 — 네트워크 의존〕로 정리). 남는 프록시 밖 경로는
ipwho.is 직접 폴백 하나다(`fresh()`의 `useCurrentLocation`,
LocationManager.swift:95-123 — 유효 표집(양성 대조 확인)과 sync 재실측 모두 관측 0건, 이론상 가능).
모델 API(OpenAI)는 구조적으로
0회다(callAI를 부르는 경로가 드라이버에 없다). 빨간 J절을 코드 결함으로 오진하지 말 것(환경
변화 신호).

**드라이버 오염 사건(2026-09-16) — 검증 도구 자체에 대한 기록.** 체크리스트 행이 아니라 이
문서의 검증 수단에 생긴 사건이므로 여기에 남긴다.

- **무슨 일.** 드라이버 N절이 한 필드를 되돌리려고 `store.config = AppConfig.load()`로 디스크
  설정을 **통째로** 다시 읽었고, 개발 기기의 실제 config.json(연결됨·자동 등록 켬)에서 딸려온
  `autoAddToCalendar=true`가 머리말이 세운 불변식(:173)을 조용히 되돌렸다. 그 뒤 모든 절이 만든
  시험용 일정이 **실제 캘린더 업로드 경로**를 탔다. 사용자 캘린더 확인 결과 **실제 피해는
  없었으나**(사용자 확인: "없음"), 막은 이유가 우리가 세운 안전장치가 아니었다.
- **안전장치(2026-09-24 현재 — t8 경화 + t10 네트워크 차단).** 2026-09-16 사고 때의 "세 겹"은
  그날의 사고 서술이고, 현재의 방어는 **격리가 앞선다**: ① **실행마다 임시 홈 샌드박스** —
  `CFFIXED_USER_HOME`을 `besir-gd-<uuid>` 홈으로 지정하고(GuardDriver.swift:264), 지원 디렉터리가
  그 안을 가리키지 않으면 시작을 거부한다(exit 2, :269-274). ② 머리말에서 `autoAddToCalendar =
  false`(:311)와 `googleClientID = ""`(:317 — 키체인 게이트), **프록시 주소·앱 토큰**(:318-326 —
  t10, 카카오·ODsay 프록시 경로 차단)을 비우고, 전역 불변식(근거 주석 :329-337, 단언
  호출 :360·:1492·:1755 세 자리)이 도는 내내 그대로임을 잰다. ③ N·Z·P절 머리의 재설정
  (:1503·:1630 — 봉쇄: 되돌린 clientID는 그대로 두고 자동 업로드만 다시 끈다). ④ 절 경계 단언
  (:1492 N절 뒤·:1755 꼬리 backstop — **탐지**, `drvAssertGlobalInvariants` :338-356). ④는
  **일부러 복구하지 않는다** — 스스로 고치면 breach가 또 안 보인다. 복구는 각 절의 방어선(③)이
  맡는다. ⑤ **시작·종료 실측 대조** — 실제 지원 디렉터리의 이름·바이트를 실행 시작과 종료에
  비교해 다르면 exit 3. ⑥ **내부 시한**(기본 900초, 인자로 덮음 — 넘으면 exit 124, 시한 상수
  :174), 정상 종료 시 샌드박스는 스스로 지운다(:214). 종료 코드: 0 통과 · 1 단언 실패 · 2 재지정
  실패 · 3 실측 변화 · 124 시한.
- **근절 확인(2026-09-16 회차 ux-check 직접 grep — 좌표는 2026-09-24 현행으로 갱신)**: 파일
  전체에서 `AppConfig.load()` **호출**과 `store.config` **통째 대입은 0건**(남은 것은 사고 서술
  주석 :1488-1490·:1499-1501뿐). N절은 clientID 한 필드만 비우고 그 필드만 되돌린다
  (:1416-1417·:1491).
- **음성 대조(담당자 실행, 팀 리드 보고로 인용)**: 플래그를 일시로 켰을 때 단언이 실제로
  빨개지는지 확인했다(**204/205** — 불변식 단언이 잡음). "절대 빨개지지 않는 단언은 없느니만
  못하다"는 것이 이번 사고의 교훈이다.
- **드라이버는 더 이상 이 맥의 실제 앱 데이터에 쓰지 않는다(2026-09-23 2차 사고 → t8 경화).**
  모든 읽기·쓰기는 실행마다 새로 만드는 샌드박스(`besir-gd-<uuid>` 임시 홈) 안에서 풀리고, 실제
  지원 디렉터리는 시작·종료 바이트 대조로 **읽어서 지키는** 대상이다(다르면 exit 3). 2026-09-16의
  "V-출발·W-하룻밤 잔재" 같은 일(그날은 팀 리드가 직접 정리했다)은 구조적으로 막혔다. **수용된
  잔여(REQ-001 설계)**: 시한 초과·크래시로 끝난 실행은 샌드박스를 하나 남길 수 있다(지우는 경로는
  정상 종료·시작 거부 둘뿐이다). t8의 짧은 기한 실행이 하나를 남겼고 이 카드가 치웠다 — 2026-09-24
  관측: 잔존 0건(`find /var/folders -maxdepth 3 -name 'besir-gd-*'` 빈 출력).

**이번 회차에 ux-check가 직접 관찰한 것(2026-09-16, 같은 트리):**
- 근거 심볼·줄번호 전수 재확인(Read·grep) — N·O·P 수정으로 AIAssistant·AIChatView·Store·
  Models·EventDetailView·SettingsView·GuardDriver가 전부 움직였기 때문. 핵심 새 좌표:
  `enqueueCalendarUpload` Store.swift:817·`googleConnected` :494·상세 버튼 EventDetailView.swift:
  108-144·설정 블록 SettingsView.swift:51-69·`CalendarUploadState` Models.swift:132-151·
  `unknownPlace` AIAssistant.swift:571-574·캡션 :2415-2417·`searchPlaces` :799-820(지연 상수 EditCard.swift:294)·
  `confirmedPlaces` :52·`choose(field:place:)` :761-765·`[장소 검색]` 칩 EditCardView.swift:125-128.
  기존 행의 좌표도 전부 현재 트리로 다시 맞췄다(각 행 참조).
- GuardDriver N·Z·P절(:1406-1782) 열람 — N-1~N-4·O-1~O-8·P-1~P-7 단언 확인.
- grep: `AppConfig.load()`·`store.config` 통째 대입 0건(위), `remember_fact`/`forget_fact` 코드
  참조 0건(주석 1건, AIAssistant.swift:1857), reminders PATCH GoogleCalendarService.swift:123-124,
  ContentView span :542·:557·overlapsDay 호출 :92·:105·SwipePager :799, AddEventView.swift 존재.
- 드라이버·빌드는 돌리지 않았다(팀 리드 지시 — 기준선은 위의 팀 리드 측정을 인용).

**이번 회차(2026-09-24, card t9)에 ux-check가 직접 관찰한 것:**

- **인용 의미 전수 감사** — 표 113행의 인용 228조각을 추출·대조(위 t9 기준선 문단), 11행 13조각
  정정. 프롬프트 규칙 블록(:1032-1076)은 줄 지도를 직접 그어 규칙 번호와 좌표를 맞췄다.
- **GuardDriver 새 좌표 바이트 대조**(`sed`로 본문 확인) — :1511·:1646 재설정, :1500·:1775 경계
  단언, :1492-1494·:1507-1509 사고 주석, :1420-1421·:1495 clientID 비움·되돌림, :309·:315·:318-325·
  :326-342 현행 유지 확인. :173은 이제 빈 줄이다(사고 서술 속 ":173"은 그날 좌표 기록으로 둔다).
- **이 카드의 1줄 수리 확인** — `grep -n 'disabled(!(card?.isReady ?? false) || saving)'` →
  AddEventView.swift:119·AddActivityView.swift:509. 쌍둥이 좌표는 509다(카드 디스패치 문서의
  :510은 한 줄 틀린 좌표).
- ConflictBanner 현행 좌표 — AddEventView.swift:56-57(사용)·:549(`currentConflicts`)·:614(선언).
- 잔여 샌드박스 0건(위), AI 도구 9개 현행 유지(`toolsJSON`의 `"name":` grep 9건).
- 드라이버·빌드·프록시는 이 회차에 돌리지 않았다(임무 제약: CHECKLIST.md만 편집) — 위
  2026-09-24 기준선 수치는 이 카드 레인의 측정을 인용한다.

**관찰(2026-09-16)과 사용자 보고의 구분**: 관찰 7건(D·A·F·K·O·P·N — 이름 붙은 증거, 위 요약),
사용자 보고 = 확인 목록 1~15 수행·통과 답변(개별 증거 없음 — 아래 목록으로 이월). 2차(H·I·J·N)는
코드 검사 발견 — 각 행에 그렇게 적었다.

### Day 닫기 확인 목록 — 셋으로 갈라 배치 (2026-09-24, card t9)

> **⚠️ 배포 전제**: iOS 서명 프로파일이 **2026-09-25 만료**고 등록 팀(DFEME8ZQT9)이 유령이다 —
> 실기기 확인 전에 서명 팀 재결정이 먼저다(배포·서명은 리드 몫, 이 검사 다음).

**(a) 기계 검증 완료 — 이 카드 레인이 이미 재측정(2026-09-24, 사용자 행동 필요 없음).**
드라이버 두 실행 exit 0·212/212·불변식 9/9 ✓·실제 데이터 해시 무변경·샌드박스 자동 삭제 ·
iOS·macOS swift 경고 0/0(1줄 수리 전후) · 프록시 7/7 · 네트워크 표집 136샘플 0소켓(구조적 부재
아님 — O-1 정정 참조) · code-safety 전수 검사(우선 표면 10개: H1 await 무효화 0건·H2 신규 1건·
H3 외부 한도 0건·H4 복제 늘지 않음) · 이 문서의 인용 전수 감사 228조각. 아래 둘은 이 목록이
대신할 수 없는 것들이다.

**(b) 시뮬레이터(iPhone 17 Pro)에서 운영자가 직접** — 입력 문구·기대 결과·초기화 시점을 정확히.
도구 호출 인자를 보려면 채팅 화면의 **대화 복사 버튼**(테스트용, 출시 전 제거) — `[도구 호출]` 줄에
모델이 보낸 인자가 나온다.

1. **[D2 — 계속 미관찰]** 초기화: 앱 재시작 직후. 반복 그룹 2개를 각각 만든 뒤
   ```
   반복 일정 목록 보여줘
   ```
   → 이어서 `2번 반복 그룹 자동차로 바꿔줘` → 대화 복사의 `[도구 호출]`에서 **series_number에 2가
   실제로 들어갔는지**. 이 한 건이 D2의 마지막 남은 미관찰이다.
2. **[G15·G10 — 도달 확인]** `일정 하나 만들어줘` → 말로 되묻지 않고 툴을 부르는지, 카드가 일곱
   줄로 뜨는지, 시각 줄(`arrival_iso:""`에도), 기준 칩 둘 다 미선택 시작·기준 없으면 확인 잠김,
   출발 기준 확정 시 여유 줄 흐림, 목적지·출발지를 지어내지 않는지(G10), 전부 고르면 정확히 1회 등록.
3. **[B — 사용자 보고만 있음]** `내일 오후 3시까지 강남역 자동차로 가고 여유 20분으로 잡아줘`
   → 카드에 수단·여유 줄이 없고 "말씀하신 대로" 표시. 되묻는 턴에 `3시`만 답해도 값을 다시 물으면 재발.
4. **[C — 사용자 보고만 있음]** 이동 두 건을 물리게(목적지는 진짜 장소명으로 — 일반명사는 O의
   카드가 뜬다): `내일 오전 9시까지 강남역 가야 해` 이어서 `내일 오전 9시 30까지 강남역 가야 해`
   → 겹침 안내 → "그냥 등록해" 1건 / "늦게 도착해도 되니 끝나고 바로 출발할래" 첫 일정 끝난 뒤 출발.
5. **[J — 사용자 보고만 있음]** 같은 제목·목적지로 늦은 회차를 먼저 등록하고 이른 회차를 나중에
   → 두 번째 요약이 **이른 회차의 시각**을 말하는지(늦은 회차 시각이면 재발).
6. **[G·G-2 — 잘린 모양은 화면 영역]** `오늘 밤 11시부터 내일 새벽 1시까지 집에서 영화 보기`
   → 양쪽 날짜에 모두 블록(잘린 반쪽의 높이·위치까지), 월간·주간 점도 이틀 다.
7. **[M — 사용자 보고만 있음]** 왕복 만들기에서 가는 편 출발지 직접입력에 즐겨찾기·목록 밖
   일반명사(예: `학원`) → 요약에 `⚠️ … 찾지 못해 그 부분은 만들지 못했어요`.
8. **[A 잔여 — 사용자 보고만 있음]** 왕복 카드에서 "가는 편 없음" 칩 → 오는 다리만 1건.
9. **[B4 잔여 — 사용자 보고만 있음]** 반복 카드에서 `4주` 칩 선택 → `이번 달에 등록된 일정 몇
   건이야?`로 건수(4주×5일×구간 수준, 118건쯤이면 실패).
10. **[N 잔여 — 화면 전환 목격 없음]** 연결된 상태에서 일정 등록 → 일정 상세의 "캘린더에 올리는
    중이에요"가 "구글 캘린더에 등록됨"으로 바뀌는 것, 설정의 대기 합계. (실패·재시도는 일부러
    실패를 만들어야 하므로 기회 되면.)
11. **[F7 — 이번 수리]** 수동 추가 화면에서 값 전부 채운 뒤 저장 버튼을 **빠르게 두 번 탭** →
    일정·알림·캘린더 대기 항목이 1건씩만 생기는지(두 번째 탭은 "저장 중" 잠김이어야 한다).
12. **[UI통일 F1 잔여]** 시각 줄에서 값을 확정한 뒤 기준 칩을 다시 탭해 반대 기준으로 재확정 →
    시각 칩을 다시 열 때 바퀴(DatePicker)가 **확정된 시각**에 앉는지(수리 확인이 코드 추적
    뿐이었다 — 화면 재현은 이번이 처음).
13. **[L12]** 시뮬레이터에서 앱 종료 → 재시작 → 반쯤 고른 카드·등록 잔재 없음.
14. **[AC-009 13번]** 보임새 대조 문구를 리드가 재설명한 뒤 판정(2026-09-23 운영자 세션에서
    문구 이해가 안 됐다).

**(c) 실기기 전용(시뮬레이터로도 안 되는 것)** — 배포 전제(위 경고) 해소 뒤.

1. 실제 출발 알림 수신(백그라운드·잠금 포함) + **과거 시각 일정은 알림이 안 걸리는 것**(D-3(i)
   — "과거 일정에는 알림이 안 걸린다" 화면 고지 후보와 함께 확인).
2. 캘린더 앱에서 올라간 모습 — 등록 문구·미연결 문구, 다른 기기 전파(N의 끝).
3. AI 일일 할당량 소진 안내문(초기화 시각 없는 문구 — 기회 되면).
4. 제스처 감각 — 특히 Stepper를 대체한 직접입력(여유·알림 분)의 불편 여부.
5. VoiceOver 낭독 4종(줄 이름+사유 결합·토글 칩 선택 발표·재계산 버튼 이름·배너 통째 낭독)+
   AC-010 16번(시뮬레이터에서 무반응 — 실기기로 이월).
6. t4 S4 `shortTimeFmt` 대중교통 단계 표기(시뮬레이터에선 경로 계산이 안 돼 렌더링 미확인 —
   "오후 3:05"꼴 분 0 채움).
7. 카드 크롬 실제 질감(raised+스트로크 vs material 감각 — t4 크롬 패스분).
8. t7 실기기 셋(SPEC-UIKIT-007 §3.3): 편집 뒤 출발 알림 재예약·구글 캘린더 항목 반영·macOS 편집
   시트.
