# besir 사용자 요구사항 체크리스트

사용자가 besir의 **전체 기능**을 써보면서 마주칠 수 있는 상황을 모아, **현재 코드가 실제로
처리 가능한지** 검증한 표. 2026-09-15 기준(SPEC-ASK-001 적용 시점 — 앱 주도 되묻기 카드 +
얕은 기억 제거. 커밋 7683700·b9ef208·abd1832, 게이트는 HEAD abd1832 작업 트리에서 재실행).

범례: ✅ 됨 · ⚠️ 부분적/우회 필요 · ❌ 안 됨

현재 AI 도구 9개: `create_schedule`, `create_activity`, `create_recurring_schedule`,
`update_recurring_schedule`, `update_schedule`, `list_schedules`, `recommend_meal`,
`check_travel_time`, `delete_schedule` — `remember_fact`·`forget_fact`는 제거됐다(11 → 9).

---

## A. 단발성 일정 만들기

| # | 사용자 요구 | 상태 | 근거 |
|---|---|---|---|
| A1 | "내일 3시까지 강남역 가야 해" | ✅ | `create_schedule(arrival_iso)` — 이동시간 역산해 출발 시각 계산. 출발지와 목적지가 사실상 같은 곳(50m)이면 등록 거부(`isSamePlace` — origin 누락으로 "집→집" 0분 일정이 생기던 사고 방지) |
| A2 | "6시에 집에서 바로 출발할래" | ✅ | `create_schedule(departure_iso)` — 출발 기준, 버퍼 개념 없음(`anchor == .departure`면 buffer 0 강제, AIAssistant.swift:1040) |
| A3 | "8시부터 10시까지 강남에서 친구 만나" (머무는 활동만) | ✅ | `create_activity` — 주황색 활동 블록. **이동을 만들지 않으면 카드에 이동수단 줄이 안 생긴다**(AIAssistant.swift:519-520 — 쓰지 않을 값을 묻지 않는다) |
| A4 | "가서 놀고 집까지 오는 것도 잡아줘" | ✅ | `create_activity(travel_from_query, return_to_query)` — 활동+이동 2건이 **서로 묶여서** 생성. 툴 선언이 "왕복이면 return_to_query를 빠뜨리지 마라"로 못 박음. **왕복 수단은 카드에서 가는 편·오는 편을 따로 묻는다**(Day 7 요청 보존, G8) |
| A5 | "다음 주 화요일 오후 2시 병원" | ✅ | 시스템 프롬프트에 현재 시각 주입 + `parseDate` |
| A6 | 카톡 일정표 스크린샷 공유 | ✅ | Share Extension → `SharedInbox.drain()` → `handleShared` → 이미지 파싱. 실기기 라이브 1회 성공 확인(2026-09-10). 이미지에서 일정 여러 건을 한 턴에 등록할 때도 **되묻기 카드는 한 장**(AIAssistant.swift:578-580) |
| A7 | 한 번에 여러 일정 등록 | ✅ | `runLoop`이 한 턴에 여러 툴 호출 처리(5회 상한) |
| A8 | "지금 출발하면 몇 시 도착해?" (등록 없이 조회만) | ✅ | `check_travel_time` — 자동차·대중교통·도보 **세 가지를 한 번에** 돌려주므로 수단을 묻지 않는다. 출발지 폴백(즐겨찾기 '집'→현재 위치)이 살아있는 **유일한 곳**이기도 하다(`orDefault: true` — AIAssistant.swift:1791, 정의 :1980·가드 :1991). 등록 경로는 조용한 대체 대신 카드로 묻는다(G10) |
| A9 | 등록 결과에 적용된 여유·알림이 바로 보임 | ✅ | 등록 요약이 "도착 여유 N분 … 출발 N분 전 알림 예약됨"까지 출력(AIAssistant.swift:1097-1106). 카드로 등록한 경우에도 같은 요약이 나간다 — 확인 버튼이 같은 실행부(`runToolCalls`, :476)를 탄다. 값이 틀리게 들어가도 요약에 없으면 사용자가 그 자리에서 보지 못한다 — 0짜리 35건 사고의 남은 절반. (빌드로 검증, 이번 회차 실기기 미관찰) |

## B. 반복 일정

| # | 사용자 요구 | 상태 | 근거 |
|---|---|---|---|
| B1 | "매주 평일 9시~6시 회사" | ✅ | `create_recurring_schedule` — 출근·복귀·활동 블록 자동 생성. **요일 3개 이상인데 주기 인자(매월 n번째 주·격주)가 함께 오면 만들기 전에 되묻는다**(`recurringArgumentIssue`, AIAssistant.swift:1394) — "평일"에 `nth_week_of_month:1`이 얹혀 한 달의 1~7일에만 일정이 생기던 사고(c728996) 방지. 확인(`confirm_recurrence`)은 앱이 **그 조합을 실제로 되물었을 때만** 유효(스스로 붙인 확인은 거부). 기간(weeks) 되묻기 갈래는 카드로 이관돼 사라졌다(:1385-1387) |
| B2 | "월수금만" | ✅ | `weekdays` |
| B3 | "점심에 밖에서 먹어" | ✅ | `lunch_place_query` — 점심 왕복 이동 + 점심 활동 블록. 같은 건물이면(비워두면) 이동 구간 생략, 장소 검색에 실패하면 이동만 생략하고 활동은 만듦 |
| B4 | "6개월간 계속" | ✅ | 최대 26주(`Store.maxRecurrenceWeeks`, Store.swift:36). 기간은 이제 **카드의 반복 기간 줄**에서 정한다 — 칩 4/8/12/26주(AIAssistant.swift:570-575), 직접입력은 1~26 바깥을 카드에서 거부(`AskField.accepts`, :84-86). 옛 "되묻기로 상한 안내" 갈래는 `weeks`를 툴 선언에서 뺀 순간 없어졌고, 상한 검사가 입력 단계로 이사했다. 생성부의 `min(max(weeks,1),26)` 이중 안전장치는 그대로(Store.swift:207) |
| B5 | "매월 첫째 주 월요일" | ✅ | `nth_week_of_month`(1~4, -1=마지막) — `nthWeekArgument`(AIAssistant.swift:1353)가 유일한 읽기 지점. 1~4·-1 밖의 값은 확인으로 통과시키지 않고 되돌린다(유효값 목록을 보여주면 모델이 아무거나 고르는 사고가 있었다). 0은 "매월 아님"으로 읽는다 — 이 모델은 선택 인자를 비우지 못해 INTEGER에 0을 채운다 |
| B6 | "격주로" | ✅ | `every_n_weeks`(2=격주) — `everyNWeeksArgument`(AIAssistant.swift:1373)가 유일한 읽기 지점 |
| B7 | "공휴일은 빼고" | ✅ | `skip_holidays` — `KoreanHolidays`. 음력 공휴일은 표가 아니라 `.chinese` 달력으로 계산해 연도 갱신 불필요(2026·2027 실제 값과 대조 검증 완료). 주기 인자와 조합돼도 B1의 조합 점검이 함께 돈다 |

## C. 조회

| # | 사용자 요구 | 상태 | 근거 |
|---|---|---|---|
| C1 | "지금 등록된 일정 뭐 있어?" | ✅ | `list_schedules` — 반복 그룹은 `[반복 n]` 번호 + 회차 수로 요약. 조건에 걸린 게 없으면 "없어요"로 끝내지 않고 전체 건수·가장 가까운 일정을 함께 알려 모델이 스스로 범위를 고치게 함(날짜 범위를 틀리게 잡아 "일정이 없다"고 잘라 말하던 사고 방지) |
| C2 | "오늘/이번 주 일정만 알려줘" | ✅ | `list_schedules(from_date, to_date)` — to는 그 날 23:59:59까지 포함. "이번 주"는 오늘이 들어가는 주로 잡으라고 프롬프트에 못 박음 |
| C3 | "내일 몇 시에 나가야 해?" | ✅ | 출력이 "8:30 출발 → 9:00 도착" 형태로 **출발 시각 포함**. 이동시간 계산 실패 회차는 ⚠️ 표시로 구분 |
| C4 | "이번 주에 몇 건 있어?" | ✅ | C2와 동일 |

## D. 수정

| # | 사용자 요구 | 상태 | 근거 |
|---|---|---|---|
| D1 | "방금 만든 반복 일정 자동차로 바꿔줘" | ✅ | `lastRecurrenceId`를 대화 기록(ai_history.json)과 함께 저장(AIAssistant.swift:184-187) — 앱을 껐다 켜도 유지. 재생성이 아니라 `update_recurring_schedule` → `updateRecurringSeries`로 그대로 갱신(다시 만들면 중복 등록) |
| D2 | "저번 주에 만들어둔 반복 일정 자동차로 바꿔줘" (옛 대화·앱 재시작 후) | ⚠️ | 목록 번호 다리(615d875): `list_schedules`가 그룹마다 `[반복 n]`을 붙이고 — 등원·복귀·점심이 **모두 같은 번호**를 달고(그룹 전체를 가리킨다), 재호출해도 같은 그룹은 같은 번호, 지워진 그룹의 번호는 재사용하지 않는다. `update_recurring_schedule(series_number)`이 그 그룹 전체를 고르고, 0이면 `lastRecurrenceId`로 폴백, 등록 안 된 번호·음수는 `list_schedules` 재조회로 되돌린다. 번호는 앱 재시작에 살아남지 않는다(무효 번호가 옛 그룹을 가리키는 쪽이 더 위험 — 저장 안 함은 의도적). 앱 쪽 동작은 가드 드라이버로 결정적 검증됐으나 **모델이 [n]을 실제로 series_number에 넣는 것은 실기기에서 아직 관찰 안 됨** — 2026-09-15 대화에서는 series_number=0으로 와서 폴백이 우연히 맞았다 |
| D3 | "도착 여유 20분으로 늘려줘" (반복 그룹) | ✅ | `update_recurring_schedule` → `updateRecurringSeries`. **0이 오면 곧이곧대로 적용하지 않고 되묻는다**(`zeroUpdateIssue`, AIAssistant.swift:1500, 안내문 :1531-1545) — "수단만 바꿔줘"에 0이 딸려와 통근 35건의 여유·알림이 통째로 0이 되던 사고 방지. 되묻기에는 현재값 복원 탈출구를 주고, 확인은 `confirm_zero` 왕복으로만 받는다(앱이 되묻은 조합에 한해 유효 — 스스로 붙인 확인은 무시). 번호로 온 호출의 되묻기 답에는 series_number를 그대로 두라고 덧붙인다(빼면 폴백이 엉뚱한 그룹을 고친다). 수정 결과는 어느 그룹을 고쳤는지 제목으로 말한다 |
| D4 | "그 약속 4시로 미뤄줘" | ✅ | `update_schedule(new_arrival_iso / new_departure_iso)`. 여러 건이 걸리면 날짜를 물어 **같은 인자에 date만 추가해** 재호출 |
| D5 | "장소 바꿔줘" / "제목 바꿔줘" | ✅ | `update_schedule(new_place_query, new_title, new_mode)`. 활동은 `new_start_iso`/`new_end_iso`로 옮기며 **묶인 이동도 따라 움직임** |
| D6 | 회차마다 드래그로 여유를 따로 바꾼 시리즈에 "전체 여유 0으로 바꿔줘" | ✅ | 0 판정이 **첫 회차가 아니라 전 회차**를 본다 — `zeroUpdateIssue`가 `recurringSeries` 전체에서 값을 수집해 편차가 있으면 "지금은 회차마다 N~M분으로 제각각이라, 확인하면 전부 0분으로 통일돼요"라고 사실대로 알린다. 첫 회차가 0이라고 "이미 0분이라 바뀌는 게 없어요"라고 거짓을 말하면 사용자가 사실이 아닌 전제로 confirm_zero를 누르게 된다. 가드 드라이버 N 섹션 단언(빌드로 검증, 실사용 미관찰) |
| D7 | 모델이 말도 안 되는 값을 보내도 (여유 -5분 등) | ✅ | **이번 회차에 완전해졌다** — 클램프가 `Store.clampBuffer`/`Store.clampNotifyLead`(Store.swift:44·48)로 통일되고 네 경로 전부에 걸린다: 단발 생성(AIAssistant.swift:1040-1041)·활동+왕복(:1170-1171)·반복 생성(:1239-1240)·반복 수정(Store.swift:696-698, 출발 기준 구간은 0 강제). 이전엔 "생성 경로 두 곳엔 클램프가 없다"는 구멍이 남아 있었는데 e10b292가 막았다. 가드 드라이버 O절이 네 경로 클램프 저장값을 검사 |
| D8 | 화면에서 블록 드래그로 시간 이동 | ✅ | 꾹 눌러 드래그. 반복이면 "전체/이 일정만" 선택(`confirmationDialog`) |

## E. 삭제

| # | 사용자 요구 | 상태 | 근거 |
|---|---|---|---|
| E1 | "강남 약속 취소해줘" | ✅ | `delete_schedule(title_query)` — 텍스트로 확인받은 뒤 1회만 호출(도구가 재확인 없이 실행) |
| E2 | "반복 일정 전체 삭제" | ✅ | 제목 일치분만 삭제 — 같은 그룹의 "점심 후 복귀" 같은 **다른 제목 구간까지 지워지는 사고 방지** |
| E3 | "이번 주 화요일 것만 빼줘" | ✅ | `date` + `whole_series:false` — 반복인데 날짜 없이 "이 회차만"이면 날짜를 되묻는다 |
| E4 | "오늘 일정 다 지워줘" | ✅ | `date`만으로도 삭제 가능(제목·날짜 중 최소 하나 필요). 반복 아닌 낱개가 5건 초과면 `confirm_many`로 재확인 |
| E5 | 삭제한 게 동기화로 되살아남 | ✅ | 삭제 묘비(`deleted_gcal_ids.json`) — 원격 부재를 다음 동기화가 확인한 뒤에만 묘비 정리(무한 누적 방지) |

## F. 충돌·시간 계산

| # | 사용자 요구 | 상태 | 근거 |
|---|---|---|---|
| F1 | 기존 일정과 겹치면 알려주기 | ✅ | `Store.conflicts`(Store.swift:470 — 활동·이동 모두 검사, 같은 반복 그룹 제외) — `create_schedule`이 등록 전에 검사하고 겹치면 선택지를 돌려줌 |
| F2 | "늦게 도착해도 되니 끝나고 바로 출발" | ✅ | `on_conflict:"late_arrival"` — 겹치는 일정이 끝난 시각에 출발하도록 전환, 몇 분 늦는지까지 요약에 명시 |
| F3 | "겹쳐도 그냥 등록해" | ✅ | `on_conflict:"ignore"` |
| F4 | 실시간 교통상황 반영 | ✅ | `refreshUpcomingEstimates`(Store.swift:1072) — 출발 2시간 이내 일정은 앱 진입 시 재계산. 반복 생성 시 첫 회차만 실제 조회하고 나머지는 그 값을 복사(임박한 회차는 그때 갱신) |
| F5 | 수동(+ 메뉴)으로 만들 때도 충돌 경고 | ✅ | `AddEventView` 겹침 배너(저장은 막지 않음 — 일부러 겹치게 잡는 경우가 있어서) |
| F6 | 활동 블록끼리 겹침 경고 | — | **일부러 넣지 않음**(2026-09-11 사용자 결정). `AddActivityView`에는 활동끼리 겹침 경고가 없고, AI `create_activity`도 이동 구간 겹침만 "막지 않고 알림"한다. 두 활동을 동시에 해야 하는 경우가 정상이라 경고가 방해가 된다 |

## G. 요청마다의 값 · 앱 주도 되묻기 카드 (SPEC-ASK-001)

되묻는 주체가 모델이 아니라 **앱**이다. 이 모델은 선언된 선택 인자를 비워두지 못하고 전부
채운다(b303f41 — 저장해둔 여유 10분이 0으로 덮여 여유 0짜리 35건이 등록되고도 아무도 몰랐다).
그래서 되물을 인자(이동수단·도착 여유·알림·반복 기간)는 툴 선언에서 아예 뺐다(AIAssistant.swift:820-824)
— 모델이 채울 수 없어야 앱이 "비었다"를 관측하고, 카드로 사용자에게 직접 묻는다.

| # | 사용자 요구 | 상태 | 근거 |
|---|---|---|---|
| G1 | "자동차로 가자"·"여유 20분"처럼 말한 값 | ✅ | 앱의 문장 파서가 모델을 거치지 않고 읽어 이번 요청의 인자로 채운다(`statedArguments`, AIAssistant.swift:372-378 → `fillStated`, :596-613). **명시된 값은 카드에 줄을 만들지 않는다**(REQ-015) — 대신 카드에 "말씀하신 대로: 이동수단 자동차 · 도착 여유 20분"으로 적어 보여준다(:394-402, AIChatView.swift:185-189). 줄이 사라진 자리를 사용자가 못 보면 그게 곧 조용한 적용이다. 파서는 낱말을 좁게 잡는다 — "기차역에서 만나"를 대중교통으로 오인하지 않는다(:384-390) |
| G2 | 값을 안 말하면 아무 값이나 넣지 않고 물어봄 | ✅ | 부재는 부재로 관측돼 카드가 된다(`askFields`, AIAssistant.swift:498-534). 저장된 기본값을 대신 넣던 세 겹의 방어(프롬프트·선언·실행부 기본값)는 선호 저장 자체와 함께 사라졌다 — 조용히 채워진 값이 사고의 원인이었다. 카드를 통과하지 않은 호출이 실행부에 도달하면 `missingAskedArguments`(:995-998)가 "아직 만들지 않았어요 — …이(가) 비어 있어요"로 세운다(도달 불가 경로의 이중 안전장치) |
| G3 | "앞으로 자동차로 해줘" (기억 요청) | ✅ | **기억은 더 없다 — 매번 물음이 원칙**(REQ-041). "앞으로~"든 "주로~"든 신호어 구분 없이 **이번 요청에만** 적용되고, 다음 요청에서 값을 말하지 않으면 카드가 다시 뜬다. 가드 드라이버 Q절이 "같은 인스턴스의 둘째 요청은 다시 묻는다"를 결정적으로 검증 |
| G4 | 빠진 값이 여러 개면 한 번에 묻기 | ✅ | 인자마다 줄이 하나인 **카드 한 장**에 전부 모은다(`pendingAsk`, AIAssistant.swift:581-592). 한 턴에 툴 호출이 여러 개(이미지로 일정 여러 건)여도 카드는 한 장 — 호출마다 끼어들면 같은 질문에 다섯 번 답하게 된다. 순차 질문(선택지 A)도 같은 질문을 하지만 일정 하나에 3~5번 끼어든다 |
| G5 | 확인 버튼을 누르면 딱 한 번 등록 | ✅ | 카드 확인이 보류해 둔 호출에 고른 값을 실어 **정확히 1회** 실행한다(`confirmAsk` :635 → `resolvePendingAsk` :655-684 → `runToolCalls` :476). 호출을 앱이 만들므로 선택과 값 사이에 모델이 임의 값을 넣을 틈이 없다. 카드는 "이동수단 자동차 · 도착 여유 0분" 요약 말풍선으로 바뀌고(:659-663), 같은 호출이 두 번 가지 않는다. 가드 드라이버 P절이 (a)~(e) 전 경로를 검증 — 두 번째 확인은 무동작 |
| G6 | 칩에 없는 값 직접 입력 | ✅ | 모든 줄에 `[직접입력]` 탈출구(AIChatView.swift:222-224). **예외 — 이동수단 줄에는 없다**: 세 칩(자동차·대중교통·도보)이 곧 전체 집합이라 열거 못할 값이 없다(`modeField` allowsCustom:false, AIAssistant.swift:546-550; 승인된 카드 그림과 일치). 빈 직접입력은 제출되지 않고(`accepts` :72), 범위 밖(여유 -5·999 등)도 카드에서 거부(:77-86) — "그 값은 쓸 수 없어요"(AIChatView.swift:230-234) |
| G7 | "여유 없이" — 진짜 0분 | ✅ | 문장 파서는 `v > 0`만 읽어 발화로는 0을 말할 길이 없었다(받아들여진 트레이드오프) — 카드의 **진짜 `0분` 칩**이 그 격차를 닫는다(`bufferField`, AIAssistant.swift:554-559). 알림 줄의 0은 "알림 없음"이 아니라 **출발 시각 알림**으로 적는다(:561-568) — 알림 자체를 끄는 건 발화 "알림 필요 없어"→`notify_enabled:false`, 그때는 그 줄이 아예 안 생긴다(:504) |
| G8 | 왕복은 가는 편·오는 편을 따로 | ✅ | `create_activity` 왕복에서 `가는 편`·`오는 편` 수단 줄이 각각 생긴다(AIAssistant.swift:521-524) — "갈 땐 지하철, 올 땐 택시"를 위한 Day 7 요청. 한쪽만 있는 이동이면 줄 하나 |
| G9 | 머무는 활동에 교통 질문 안 함 | ✅ | 이동을 만들지 않는 활동은 그 값들을 쓰지 않으므로 묻지 않는다(AIAssistant.swift:519-520 `guard outbound || back else break`). 복귀 구간은 출발 기준이라 여유 줄도 가는 편이 있을 때만(:529) |
| G10 | 출발지를 안 말했을 때 | ✅ | **조용한 '집' 대체는 이제 조회에만 있다** — 생성 도구는 출발지 줄이 카드에 생긴다(`originField`, AIAssistant.swift:537-542: 즐겨찾기 칩 + 현재 위치 + 직접입력). 프롬프트도 "말하지 않았으면 비워둬라 — 앱이 묻는다"로 못 박음(:779-780). 폴백은 `orDefault:` 파라미터 뒤로 가뒀고 켜는 곳은 `check_travel_time`(:1791) 하나뿐이다(등록이 아니라 조회라 되물 이유가 없다) |
| G11 | 반복 일정처럼 줄이 다섯 이상이면 | ✅ | 카드는 길어져도 되고 **행을 숨기지 않는다** — 칩은 가로스크롤 없이 줄바꿈으로 흘러 넘고(`ChipFlow`, AIChatView.swift:353-406), 카드 전체는 채팅 ScrollView가 담는다. 캡이나 접기로 줄을 숨기면 그 인자를 아예 정할 수 없거나 기본값이 조용히 적용되는 경로가 되살아난다. 칩 높이는 글자 크기를 따라 커진다(`@ScaledMetric`, :170-174 — 큰 글씨에서 잘리지 않게) |
| G12 | 카드가 떠 있는데 다른 얘기를 함 | ✅ | 새 발화가 오면 카드는 무효 — "물어본 값을 받지 못해서 그 등록은 진행하지 않았어요"로 바뀐다(`cancelPendingAsk`, AIAssistant.swift:324·688-692). 보류한 모델 턴은 히스토리에 넣은 적이 없으므로 흔적이 남지 않는다 |
| G13 | 카드를 반쯤 고른 채 앱 종료 | ✅ | 보류 상태는 어떤 형태로도 디스크에 남지 않는다(REQ-040) — 저장 시 카드 말풍선을 빼고 쓴다(`saveHistory` 필터, AIAssistant.swift:178-183). 다시 켜면 카드는 없고 등록도 안 돼 있고, 요청을 다시 하면 된다. 확인을 누르지 않은 값이 조용히 적용될 경로가 원천적으로 없다 |
| G14 | "그거 기억해줘"/"잊어버려" | ✅ | 기억 기능 자체가 없다 — `remember_fact`·`forget_fact` 툴, `ai_memory.json` 읽기·쓰기, `AppConfig`의 `preferredMode/preferredBuffer/preferredNotify`, 설정 화면의 기억 목록 UI가 전부 제거됐다(Shared/ 전역 grep 코드 참조 0건 — 남은 4건은 제거 사유 설명 주석. 기기에 남은 옛 `ai_memory.json`은 아무도 읽지 않으므로 영향 없음, REQ-002). "잊어버려"는 지울 기억이 없다 — 모델이 뭐라고 대답할지는 실기기 확인 목록 |

## H. 알림

| # | 사용자 요구 | 상태 | 근거 |
|---|---|---|---|
| H1 | "출발 30분 전에 알려줘" | ✅ | 발화로 말한 값은 파서가 이번 요청에 적용하고(`minutes(near:)`, AIAssistant.swift:376), 안 말했으면 **카드의 알림 줄**에서 고른다(`notifyField`, :563-568 — 출발 시각/10분/30분/1시간 전 칩 + 직접입력 0~1440분). "알림 필요 없어"면 줄 자체가 안 생긴다(:504). 등록 요약에 "출발 N분 전 알림 예약됨"으로 **적용된 값**을 보여준다(A9) |
| H2 | "이 일정은 알림 필요 없어" | ✅ | `ScheduledEvent.notifyEnabled` + AI `notify_enabled` + `AddEventView` 토글. 알림 예약 코드가 `scheduleDepartureNotification`(Store.swift:868) 한 곳에 통합돼 한 곳에서 처리 |
| H3 | 반복 일정 뒤쪽 회차 알림 누락 | ✅ | iOS 64건 제한 대응 — `rescheduleNearestNotifications`(가까운 60건만 실제 예약, Store.swift:1044)을 반복 생성·수정 직후와 포그라운드 진입 때 다시 실행 |
| H4 | besir에서만 알림(캘린더 앱 중복 제거) | ✅ | 구글 이벤트 `reminders` 해제 + 기존 항목도 동기화 시 `clearReminders`(PATCH)로 정리(GoogleCalendarService.swift:122, Store.swift:1295) |

## I. 연동

| # | 사용자 요구 | 상태 | 근거 |
|---|---|---|---|
| I1 | 구글 캘린더 자동 등록 | ✅ | `autoAddToCalendar`. 반복 34회차 수정 같은 대량 변경은 파일 쓰기를 묶어서 한다(2ea30c9 — 70회 → 4회) |
| I2 | "이건 캘린더에 올리지 마" (건별 선택) | ✅ | `syncToCalendar`/`wantsCalendarSync`(이동·활동 양쪽) + AI `add_to_calendar` + 화면 토글 |
| I3 | 앱 안 켜도 동기화 | ⚠️ | `BGTaskScheduler` 백그라운드 동기화 있으나(App.swift:20·43) **iOS가 실행 시점 결정**, 강제 종료 시 미실행 |
| I4 | "근처 맛집 추천해줘" | ✅ | `recommend_meal` 툴 + 활동 상세 "주변 맛집"(J1) + be full sir 화면. 반경은 `radius_meters`가 0(=안 정함)이면 **기본 1000m로 찾는다**(AIAssistant.swift:1727 — 0을 그대로 두면 카카오 하한 100m 검색이 돼 결과가 거의 안 나왔다) |

## J. 맛집 추천 (be full sir)

| # | 사용자 요구 | 상태 | 근거 |
|---|---|---|---|
| J1 | 일정 상세에서 목적지 주변 맛집 보기 | ✅ | `ActivityDetailView` "주변 맛집" 섹션(ActivityDetailView.swift:19·143) → `PlaceSearch.nearbyPlaces`(카카오 `category_group_code=FD6`, 거리순). 이동 일정 상세에는 더 이상 없음(2026-09-12 실기기 피드백 반영) |
| J2 | 카페도 보기 | ✅ | 맛집/카페 세그먼트(`MealCategoryFilter` FD6/CE7, ActivityDetailView.swift:22) |
| J3 | 업종·거리·주소 확인 | ✅ | "한식 · 230m" + 도로명 주소 |
| J4 | 카카오맵에서 자세히 보기 | ✅ | 각 행의 `place_url` 링크 |
| J5 | 주변에 결과가 없을 때 | ✅ | "찾지 못했어요" 빈 상태(크래시 없음) — AI 경로도 "반경을 넓히거나 다른 메뉴로 다시 찾아볼까요?"로 제안 |
| J6 | 추천을 골라 일정으로 등록 | ✅ | `FullSirView` → "일정으로 추가"(FullSirView.swift:392) → `addActivityWithTravel`(Store.swift:150) + `MealLog.activityId` 연결. AI 추천 경로는 `create_activity(log_as_meal:true)`로 같은 연결을 만든다 |
| J7 | 먹은 것 기록(`MealLog`) | ✅ | `FullSirView` "먹었어요"(:233) + "최근 먹은 것" 목록(삭제 가능). 일정을 지우면 아직 안 먹은 기록도 같이 사라진다(`removeUpcomingMeals`, Store.swift:393 — 이미 지난 식사는 실제 먹은 기록이라 남긴다) |
| J8 | 예산 기반 추천 | ❌ | be rich sir(Phase 3) 이후 — 지출 데이터가 생겨야 의미가 있다 |
| J9 | 배달·요리 카테고리 | ❌ | Day 8(조사 Day)이 여전히 남음 — 요기요·배민 개인 개발자 API 개방 여부 확인 후 결정. `MealCategory.delivery`·`.cooking`은 정의만 있고 이걸 만드는 UI·AI 경로가 없다 |
| J10 | AI에게 메뉴 추천받기 | ✅ | `recommend_meal` — 메뉴를 좁혀 말하면(일식→초밥) keyword에 그대로 넣어 재검색, 시각만 말하면 그때 있을 장소(`placeAt`: 그 시간대 활동 → 직전 도착 일정 순) 주변에서 찾는다. 반경 0 폴백(기본 1000m). "결과에 없는 가게를 지어내면 안 된다"고 못 박음 — 실기기 확인(할루시네이션 없음) |
| J11 | 홈에서 서비스 선택 | ✅ | `RootView` — be on-time sir / be full sir 카드 |

## K. 화면 조작

| # | 상황 | 상태 | 근거 |
|---|---|---|---|
| K1 | 월간 ⇄ 일간 전환 | ✅ | 헤더 화살표 버튼 |
| K2 | 월간·주간·일간 좌우 스와이프 | ✅ | `SwipePager`(ContentView.swift:770) |
| K3 | 스와이프 중 날짜가 잘못 선택됨 | ✅ | `@GestureState`로 "한 번이라도 움직였는지" 추적(ContentView.swift:35) |
| K4 | 블록 위에서 세로 스크롤 | ✅ | UIKit `shouldRecognizeSimultaneouslyWith` |
| K5 | 블록 탭 → 상세 | ✅ | 좌표 기반 히트테스트 — `span(for:)`(ContentView.swift:522·530)이 렌더링과 공유하는 단일 출처 |
| K6 | 겹친 블록 중 짧은 것 선택 | ✅ | 지속시간 최소 우선 |
| K7 | 꾹 눌러 드래그로 시간 이동 | ✅ | 반복이면 "전체/이 일정만" 확인(EventDetailView.swift:314 `confirmationDialog`) |
| K8 | 활동을 옮기면 묶인 이동도 이동 | ✅ | `linkedActivityId` |
| K9 | 자정을 넘기는 블록 표시 | ✅ | `span(for:)`이 도착일 0시부터 그림 |
| K10 | 빠르게 연속 스와이프 | ⚠️ | `SwipePager`의 `asyncAfter(0.22)` 경합 가능(ContentView.swift:807·814) — 실사용 재현 안 됨 |

## L. 연동·예외 상황

| # | 상황 | 상태 | 근거 |
|---|---|---|---|
| L1 | 구글 캘린더 등록·삭제 전파 | ✅ | `syncWithGoogle` + 삭제 묘비 |
| L2 | 캘린더에 중복 생성 | ✅ | `reconcileActivities`(Store.swift:1253) + `fetchBesirItems`(GoogleCalendarService.swift:102) |
| L3 | 캘린더 앱에서 알림 중복 | ✅ | `reminders` 해제 + 기존 항목 PATCH |
| L4 | 삭제한 일정이 되살아남 | ✅ | `deleted_gcal_ids.json` 묘비(Store.swift:65) |
| L5 | 앱 안 켜도 동기화 | ⚠️ | 백그라운드 실행 시점은 iOS가 결정, 강제 종료 시 미실행 |
| L6 | 다른 앱에서 텍스트·이미지 공유 | ✅ | Share Extension → `handleShared` (실기기 1회 성공) |
| L7 | 이동시간 계산 실패 | ✅ | 빨간 블록으로 표시(숨기지 않음), 탭·삭제 가능, AI 목록에서도 ⚠️ 표시. 등록 요약은 "출발·도착 시각이 비어 있어요"라고 성공인 척하지 않음(AIAssistant.swift:1102-1103) |
| L8 | 장소 검색 결과 0건 | ✅ | 빈 목록, 크래시 없음 |
| L9 | 위치 권한 거부 | ⚠️ | 출발지 수동 지정으로 우회 가능. 안내 문구는 나온다("위치 권한을 켜주세요" — AIAssistant.swift:1743)지만 권한 설정 화면으로 연결해 주지는 않는다. 카드의 출발지 줄은 즐겨찾기·직접입력으로 권한 없이도 고를 수 있다(G10) |
| L10 | 오프라인 | ⚠️ | 로컬 데이터는 정상, 이동시간·AI는 실패 문구만 |
| L11 | AI 일일 할당량 소진 | ✅ | `classify`(AIAssistant.swift:739-753)가 **HTTP 429 + OpenAI 자체 마커**(`insufficient_quota`/`usage_limit_`) 조합으로만 쿼터 소진을 판정(:746-748) — 일시적 rate limit과 구분한다. 감지되면 재시도하지 않고 즉시 안내(:722-723 재시도 루프 중단). 안내문(:352-360)은 한도 소진 사실과 수동 등록 대안(+ 버튼)만 말한다 — **초기화 시각을 의도적으로 적지 않는다**(한도가 언제 풀리는지 앱이 알 수 없는데 단정하면 옛 결함이 재현된다). (빌드로 검증 — 소진이 재현되지 않아 이번 회차 실기기 미관찰) |
| L12 | 앱 강제 종료 후 복귀 | ✅ | 모든 상태가 JSON 영속화(events·activities·meals·즐겨찾기·설정·대화 기록·lastRecurrenceId). **카드 보류 중 종료 포함**: 카드는 저장에서 빠지고(G13, AIAssistant.swift:178-183) 등록도 안 된 상태로 깨끗하다 — 미완성 인자가 담긴 모델 턴은 히스토리에 넣은 적이 없으므로(:456-458) 껐다 켠 뒤 대화도 깨지지 않는다. 목록 번호(seriesNumbers)만 저장하지 않는다 — 재개 후 목록을 다시 보면 재발급된다(D2) |
| L13 | 기기 잠금 중 백그라운드 동기화 | ✅ | 키체인 `AfterFirstUnlock` |

---

## 요약 — 2026-09-15 (SPEC-ASK-001 적용 시점)

이전 체크리스트(같은 날, SPEC-ONTIME-001 완료 직후 · 커밋 7b623c3) 이후의 변화를 반영했다.

| 커밋 | 고친 것 | 대응 행 |
|---|---|---|
| e10b292 | 생성 경로 두 곳(단발·활동·반복)에도 음수 클램프 적용 — `Store.clampBuffer`/`clampNotifyLead`로 네 경로 통일 | **D7 해소**(이전 회차에 남아 있던 "생성 경로엔 클램프가 없다" 구멍) |
| 2ea30c9 | 캘린더 등록·수정 파일 쓰기 묶음(34회차 수정 70회 → 4회) | I1 성능 |
| 7683700 | SPEC-ASK-001 M1~M3 — 얕은 기억 제거 + 앱 주도 되묻기 카드 | **G 절 전체 재작성**, A3·A4·B1·B4·H1·L12 갱신 |
| b9ef208 | M4 — 가드 드라이버 낡은 단언 정리 + 카드(P절 19건)·무기억(Q절 5건) 단언, 88/88 | 검증 방법 |
| abd1832 | M5 — SPEC-ONTIME-001 as-built 개정(툴 9개·ai_memory 삭제) + 착수 확정 읽기 3건 | 문서 정합 |

**G 절은 이번 회차의 핵심 변화다.** 옛 G1~G3(기억·선호 저장)은 "다음 회차에 이 행이 바뀐다"는
예고대로 통째로 다시 썼다 — `remember_fact`/`forget_fact`/`ai_memory.json`/선호 필드가 사라지고,
빈 인자는 카드 한 장으로 물으며(REQ-011~015), 어떤 값도 요청 사이에 넘어가지 않는다(REQ-040/041).
되묻는 주체가 앱이므로 가드 드라이버가 처음으로 "되묻기가 실제로 일어났다"를 결정적으로 증명할 수
있게 됐다(REQ-020 — 모델 주도였다면 불가능했다).

**⚠️ 현존**: D2(번호 다리 — 앱 쪽은 검증, 모델의 실제 준수 미관찰), I3/L5(백그라운드 실행 시점은
iOS 몫), K10(스와이프 경합, 재현 안 됨), L9(권한 거부 안내는 있으나 설정 연결 없음), L10(오프라인).
**❌ 현존**: J8(예산 — Phase 3 이후), J9(배달·요리 — Day 8 조사 대기). 둘 다 뒤 Day에 계획된
작업이라 새 결함이 아니다.

이번에 발견해 고친 체크리스트 자체의 오류: **D7이 "생성 경로엔 클램프가 없다"고 쓰고 있었으나
e10b292로 이미 막혀 있었다**(이전 회차가 707b9af 트리 기준으로 쓰여 7b623c3 직전 커밋을 놓침),
**A8의 출발지 폴백 서술이 '남아 있다'만 쓰고 '조회에만 남아 있다'는 제한을 못 박지 않았다**(G10과
모순될 뻔함), B4의 "새 가드가 실전에서 안 걸림" 노트는 갈래 자체가 카드로 이사해 낡았다.

**다음 예정**(plan.md): M6 — 실기기 배포(AC-008) 후 사용자 확인. be full sir Phase 1 계속.

## 검증 방법

이번 회차에 **직접 실행해 관찰한 것**(2026-09-15, HEAD `abd1832` + 작업 트리):

- 가드 드라이버: `cat Shared/AIAssistant.swift Tools/GuardDriver.swift > /tmp/gd.swift && swiftc … && /tmp/gd`
  → **88/88 통과**(exit 0). P절 REQ-020 카드 단언 19건이 (a) 부재 집합→카드 행 수 일치,
  (b) 발화 명시 값→행 없음, (c) 빈 집합→카드 없이 직접 실행, (d) 확인→수집값 전부 실린 호출
  정확히 1회(두 번째 확인 무동작), (e) 제거된 툴·인자·선호 필드 부재("툴은 9개다" 포함)를
  `fillStated`→`pendingAsk`→`choose`→`resolvePendingAsk` 실제 경로로 검증. Q절 무기억 회귀 5건은
  기기 데이터 디렉터리를 백업·복원해 저장 본문에 묻는 상태 키가 없는 것까지 확인.
- 프록시: `cd proxy && npm test` → **7/7 통과**(exit 0).
- iOS 빌드: `xcodebuild -scheme besir-iOS -destination 'platform=iOS Simulator,name=iPhone 17' -derivedDataPath build build`
  → **BUILD SUCCEEDED**(exit 0), appintentsmetadataprocessor 공지 제외 **Swift 소스 경고 0건**.
- macOS 빌드: 동일 → **BUILD SUCCEEDED**(exit 0), 경고 0건.
- 기억 제거 확인: `grep -rn "remember_fact\|forget_fact\|ai_memory\|preferredMode\|preferredBuffer\|preferredNotify" Shared/ ShareExtension/`
  → **코드 참조 0건**(남은 4건은 Config.swift:23·AIAssistant.swift:369·766·1490의 제거 사유 설명 주석).
  설정 화면의 기억 목록 UI도 grep 0건.
- 각 행의 근거 심볼과 줄번호(`askFields`, `pendingAsk`, `resolvePendingAsk`, `conflicts`,
  `clampBuffer`, `rescheduleNearestNotifications`, `span(for:)`, `SwipePager`, `deleted_gcal_ids`,
  `AfterFirstUnlock` 등)는 전부 이번 회차 트리에서 grep·Read로 존재 확인.
- 참고: 가드 드라이버를 호스트 swiftc로 컴파일하면 `CLGeocoder` macOS 26 지원 중단 경고가 뜬다
  (LocationManager.swift:25) — 드라이버 빌드 환경 이야기지 앱 빌드 게이트와 무관하다(xcodebuild
  양쪽 경고 0). 실기기 빌드의 "All interface orientations" 경고는 종전과 동일(project.yml가
  Portrait만 선언 — 시뮬레이터 빌드에 없음).

**빌드·드라이버로만 참(실사용 미관찰)**: 카드 흐름 전체(뜨는 모양·칩 탭·확인 1회 — P절이 로직을
검증했지 화면을 검증한 게 아니다), G11의 스크롤·큰 글씨 감각, "기억해줘" 발화에 대한 모델의
대답 문구, A9 요약 문구가 카드 경로에서도 그대로 나오는 것.

### 실기기에서 직접 확인할 목록 (순서대로)

1. **카드 기본 흐름(이번 Day의 핵심)** — "내일 오후 3시 강남역 가야 해"처럼 수단·여유·알림을 아무것도
   말하지 않고 등록 → 카드 한 장이 뜨는지(이동수단/출발지/도착 여유/알림 줄) → 칩을 하나씩 고를 때마다
   선택 표시가 바뀌고, **다 고르기 전엔 등록 버튼이 잠겨 있는지** → 누르면 정확히 그 값으로 **한 번만**
   등록되는지(요약의 여유·알림 값과 카드에서 고른 값 일치).
2. **진짜 0분** — 카드 도착 여유 줄의 `0분` 칩 → 등록 요약이 "도착 여유 0분"으로 나오는지.
3. **직접입력** — 도착 여유 줄 직접입력에 45 → 등록 반영. 999나 빈 값을 넣으면 "그 값은 쓸 수 없어요"가
   뜨고 등록 버튼이 계속 잠기는지. 이동수단 줄에는 직접입력 칩이 **없는지**(세 칩이 전부).
4. **왕복 따로** — "8시부터 10시까지 강남에서 친구 만나고 집에 올래" → 카드가 `가는 편`·`오는 편` 수단을
   따로 묻는지. 반면 "2시부터 6시까지 집에서 쉴래"(머무는 활동)에는 수단 줄이 아예 없는지.
5. **말한 값은 안 물음** — "내일 3시까지 강남역 **자동차로**, **여유 20분**으로" → 카드에 그 두 줄이
   없고 "말씀하신 대로: 이동수단 자동차 · 도착 여유 20분"이 적히는지.
6. **출발지 줄** — 출발지를 말하지 않은 등록에서 출발지 줄(즐겨찾기 칩 + 현재 위치 + 직접입력)이 뜨는지.
   "지금 출발하면 몇 시에 도착해?"(check_travel_time)에는 카드 없이 바로 세 가지 수단 결과가 나오는지.
7. **카드 무시·강제 종료** — 카드를 둔 채 다른 메시지를 보내면 "물어본 값을 받지 못해서 그 등록은
   진행하지 않았어요"로 바뀌는지. 카드를 반쯤 고른 채 앱을 죽였다 켜면 카드도 등록도 없는지(다시
   말하면 다시 카드가 뜨는지).
8. **매번 물음** — "앞으로 자동차로 해줘"로 등록한 뒤, 다음 등록에서 수단을 말하지 않으면 카드가
   **다시** 뜨는지(기억 없음). 덧붙여 "그거 기억해줘"라고만 했을 때 모델이 뭐라고 답하는지(당황스러운
   대답인지 확인).
9. **반복 카드 스크롤** — "평일 9시부터 6시까지 회사" → 줄 다섯 개짜리 카드가 잘리지 않고 전부
   보이는지(채팅 스크롤로 카드 전체를 담을 수 있는지). 기간 칩 4/8/12/26주.
10. **번호 다리(이전 회차 이월, 여전히 미관찰)** — 앱 재시작 후 "반복 일정 목록 보여줘" → `[반복 n]` →
    "2번 그룹 자동차로 바꿔줘" — 모델이 series_number에 2를 실제로 넣는지.
11. (이월) 방금 만든 반복 그룹에 "여유 0으로 바꿔줘" → 되묻기 → "응, 0으로 맞아" → `confirm_zero`
    왕복으로 한 번에 반영.
12. (기회 되면) AI 할당량 소진 안내문 — 초기화 시각이 **없는** 문구인지. 실제 알림 수신(출발 N분 전).
    캘린더 앱에 besir 항목이 알림 없이 보이는지.
